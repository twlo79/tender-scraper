# 標案爬蟲監測報告 2026-08-16

## 一、狀況總覽

整體狀態：⚠️ 有注意事項（執行本身正常；**新發現「國營台鐵」地區篩選漏洞已造成至少 4 次真實外洩**（07-17、07-28、08-07 三次已知＋今日新查出 07-17 一次前次報告未列入），修法已提出但延續 5 天未拍板；既有 url/date 欄位大量空白、停車場黑名單缺口、土地銀行/自來水處來源內容不符等問題均延續未修）
最近執行：2026-08-16 01:30（推播 0 筆 / 新增 0 筆 / 抓取 91 筆）

| 檢查項目 | 結果 | 說明 |
|---|---|---|
| 1. 今日已執行 | ✅ | `sent_log.json` 最新 key 為 `2026-08-16 01:30`，即今天 |
| 2. 最近一次 GitHub Actions 結論 | ✅ | run `31919706379`，2026-08-16T01:30:07Z～01:31:18Z，`workflow_dispatch` 觸發，`conclusion=success`；job log 逐行核對，全程無任何 WARNING/ERROR（除 Node.js 20 棄用提示，與程式無關），近 8 次執行（08-09～08-16）全部 `success` |
| 3. line_pushed 與 total_notify 一致性 | ✅ | `_summary`：`total_fetched=91`、`total_new=0`、`total_notify=0`、`line_pushed=false`，一致；job log 顯示「無新增案件，略過 LINE 推播」 |
| 4. 是否所有來源 fetched=0 | ✅ | 否，13 來源中 12 個有抓到資料，合計 91 筆，僅 Google Alerts 今日 0 筆 |
| 5. 個別來源連續 3 天以上 fetched=0 | ✅ | 無來源連續 3 天以上 0 筆；Google Alerts 僅今日單日 0 筆（前 6 天為 0/10/2/2/3/3），非連續故障 |
| 6. 來源抓取量暴跌/暴增 | ⚠️ | Google Alerts 今日 `fetched=0`，較近 7 天均值（20÷7≈2.86 筆）下降 100%；但 job log 該來源無任何錯誤訊息（`[Google Alerts] 0 筆` 為正常回傳，非連線失敗），且 08-10 當天同樣是 0，屬此來源固有的日常波動區間，非抓取異常。其餘 12 來源今日數字與 7 天均值差距皆在合理範圍內 |
| 7. 應擋卻漏網的案件 | ❌ | 本週（08-10～08-16）僅 3 筆推播（08-11 Google Alerts、08-12 教育部學產基金、08-13 郵局房地產出租），皆已於 08-15 報告核對為正常租賃案。但**逐筆核對 `sent_log.json` 全 30 天歷史後，發現「國營台鐵」曾 4 次推播實際上是新竹縣（3 次：07-17 香山區、07-28 新豐鄉、08-07 新豐舊站）/涉及非雙北的案件**，原因為 `scraper.py:195` 僅檢查標題+機關名是否含「臺北營業分處」這個**組織單位名稱**（其字面含「臺北」二字），而非案件**實際所在地**——台鐵「臺北營業分處」轄區橫跨新竹、桃園，導致這些案件文字上「符合」地區白名單但實際不是雙北案件。詳見「二、異常與處理」問題 1（本次最高優先） |
| 8. 標題為分類名稱／導覽文而非案名 | ⚠️ | 延續：`state.json` 中「國家住宅及都市更新中心」快取仍有「會員中心」「委員名單」「加入會員」3 筆導覽文；`_is_junk()` 過濾器（`scraper.py:113`）定義後從未被呼叫，仍是死代碼。今日無新增此類推播（皆因不含白名單詞未進入推播候選） |
| 9. url 指向列表頁而非個別公告頁 | ❌ | `state.json` 全庫 773 筆快取中 465 筆（60.2%）url 為空字串或列表頁；其中「台北市都發局」100%（33/33）、「國防部政治作戰局」100%（11/11）、「教育部學產基金」100%（4/4）、「政府採購網」95.7%（287/300）、「URC 國家住更中心」93.8%（44/45，含 23 筆列表頁+21 筆空）url 缺失，皆為走 Claude fallback 解析且未保留連結資訊所致（延續 08-15 問題 1，未修） |
| 10. date 欄位為空導致日期窗被跳過放行的項目 | ❌ | 全庫 773 筆中 460 筆（59.5%）date 為空；其中「台北自來水處」100%（55/55）、「土地銀行出租不動產」100%（35/35）全數空白，代表這兩來源完全不受 `DATE_WINDOW_DAYS=10` 限制（延續 08-15 問題 2、3，未修） |

## 二、異常與處理

### 問題 1：國營台鐵「臺北營業分處」地區篩選誤判，已確認 4 次真實外洩（新提升優先度，此前報告記錄不完整）

- **現象**：`scraper.py:1147-1153` 的通知流程對 `passes_filters()`（`scraper.py:796-814`）套用 `text = title + agency`，比對 `GLOBAL_FILTER["regions"] = ["台北","臺北","新北","瑠公管理處"]`。國營台鐵 parser（`parse_tra()`）產出的標題固定以「【臺北營業分處】」開頭，此字面本身含「臺北」二字，因此**無論案件實際地點是哪個縣市，只要屬台鐵臺北營業分處轄區就一定通過地區篩選**。核對 `sent_log.json` 全 30 天歷史，此漏洞已造成 **4 次真實 LINE 推播外洩**（皆非雙北案件）：
  ```
  2026-07-17  【臺北營業分處】【土地出租】【新竹市香山區美山段154及154之1地號部分土地】
  2026-07-28  【臺北營業分處】【土地出租】【新竹縣新豐鄉明新段111、113地號部分土地】
  2026-08-07  【臺北營業分處】【房地標租】【新竹縣新豐鄉新興路202號房地（新豐舊站）】
  ```
  （第 4 次為 07-24 的兩筆「臺北車站」廣告出租案，經核對地點本身即為雙北、非誤判，不計入外洩，僅前 3 筆為誤判外洩。08-11／08-12 報告僅記錄 07-28、08-07 兩次，07-17 這次是本次新查出、此前未被任何報告記載的第三次外洩。）
  再核對 `state.json` 目前快取（30 筆），仍有 **2 筆尚未推播但同樣誤判**、地點在新竹／桃園的案件（06-25 竹東鎮、06-30 楊梅區），因日期已超出 10 天窗口目前不會再次觸發，但同類新公告隨時可能在未來任何一次執行中重新出現並外洩（近 30 天發生頻率約每 10～20 天一次）。
- **定位**：`scraper.py:194-195`（`parse_tra()` 內的臨時地區過濾，僅檢查「臺北營業分處」字樣）+ `scraper.py:796-814`（`passes_filters()` 的全域地區白名單，同樣只做子字串比對，未區分「機關名稱含臺北」與「案件地點為台北」）。
- **建議修法**（未套用，來源專屬修正，風險最低）：
  ```diff
   def parse_tra() -> list[dict]:
       ...
       # 只保留臺北營業分處的標案
       items = [i for i in items if "臺北營業分處" in i.get("agency", "") + i.get("title", "")]
  +    # 臺北營業分處轄區橫跨新竹/桃園，僅靠機關名稱無法排除非雙北案件，需額外過濾標題中的其他縣市地名
  +    NON_TAIPEI_HINTS = ["新竹", "桃園", "台中", "臺中", "高雄", "台南", "臺南",
  +                         "苗栗", "彰化", "雲林", "嘉義", "屏東", "宜蘭", "花蓮",
  +                         "台東", "臺東", "基隆", "南投"]
  +    items = [i for i in items if not any(h in i.get("title", "") for h in NON_TAIPEI_HINTS)]
       log.info(f"  [國營台鐵] {len(items)} 筆（臺北營業分處）")
       return items
  ```
  已用目前 `state.json` 快取的 30 筆離線驗證：套用此 diff 後，5 筆新竹/桃園誤判項目會被排除，其餘 25 筆（含已知的臺北車站廣告案、雙北土地案）不受影響——**但這只是離線快取驗證，非即時網頁驗證**，套用前仍建議在有網路權限環境跑一次 `dry_run_all_regions.py --debug --source 國營台鐵` 覆核。
- **是否已修**：未修。本 session 網路出口對 `railway.gov.tw` 同樣被 egress proxy 阻擋（403 policy denial，見下方環境限制說明），無法即時驗證，依規則不自動套用程式碼變更。**因已有 3 次確認外洩且modification 明確、風險低，建議此項優先於其他待決事項核准套用。**

### 問題 2：多來源 url／date 欄位大量空白（延續 08-15 報告問題 1～3，今日核對比例）

- **現象**：`url` 空白/列表頁整體 60.2%（465/773 筆），`date` 空白整體 59.5%（460/773 筆），與 08-15 報告（url 部分來源 47%～100%、date 台北自來水處/土地銀行各 100%）比例一致，未有改善也未惡化。
- **定位與建議修法**：與 08-15 報告問題 1（`scraper.py:675-699` `parse_with_claude_fallback()` 未餵入連結資訊、`_is_junk` 未接線）、問題 2（`scraper.py` `parse_taipei_water()` 日期欄位無 fallback）、問題 3（土地銀行 date 欄位）完全相同，diff 內容請見 `qa_reports/2026-08-15.md` 問題 1～3，本次不重複貼出。
- **是否已修**：未修（延續第 2 天未拍板；本 session 同樣無法連線 `water.gov.taipei`／`landbank.com.tw`／`udd.gov.taipei` 驗證，見環境限制說明）。

### 問題 3：GLOBAL_FILTER 黑名單未涵蓋「停車」類關鍵字（延續 08-12／08-15 報告，第 5 天未拍板）

- **現象**：`state.json` 全庫掃描，含「停車場／停車位／停車格」關鍵字的標題共 **25 筆**，橫跨 5 個來源（國營台鐵 3、台北市財政局 3、國家住宅及都市更新中心 6、政府採購網 2、Google Alerts 11），其中多筆同時符合白名單「標租」與雙北地區篩選，例如：
  ```
  台北市財政局：公開標租臺北市內湖區康寧段四小段452-2地號市有土地作單一停車位使用案
  政府採購網：115年停車場標租經營管理案
  ```
  目前 `GLOBAL_FILTER["blacklist"]`（`scraper.py:69-75`）仍未包含任何「停車」相關詞，這批案件尚未推播純屬日期不在窗口內的僥倖。
- **建議修法**：與 08-15 報告建議 1 相同（`scraper.py:69-75` 新增 `"停車位使用", "停車場使用", "停車場經營", "停車場委託經營"`），diff 見該報告，本次不重複貼出。
- **是否已修**：未修（自 08-12 首次提出，本次為第 5 天未拍板）。

### 問題 4：土地銀行出租不動產／台北自來水處抓取內容與業務目標 100% 不符（延續 08-11 報告，第 6 天未拍板）

- **現象**：`state.json` 中「土地銀行出租不動產」全部 35 筆、「台北自來水處」全部 55 筆標題逐一核對，**依然 0 筆與不動產出租/租賃相關**——土地銀行為 ATM／網銀維護、行銷抽獎等客服公告；自來水處為財物/工程/勞務採購公告。兩來源近 31 天 `notify_sum` 皆為 0，且非因篩選正確運作，而是這兩個頁面本身抓到的內容從未包含真正的租賃公告。
- **定位與建議修法**：與 08-11 報告問題 2（土地銀行）、08-11 報告建議 1（自來水處）相同，diff／確認步驟見該報告，本次不重複貼出。
- **是否已修**：未修（自 08-11 首次提出，本次為第 6 天未拍板）。

### 問題 5：URC/國有財產署 導覽文/垃圾標題污染快取（延續 08-15 報告，第 2 天未拍板）

- **現象**：`_is_junk()`（`scraper.py:113`）定義後全檔案僅此一處出現，從未被任何 parser 或 `parse_with_claude_fallback()` 呼叫，是沒接上的死代碼。`state.json` 中「國家住宅及都市更新中心」仍有「會員中心」「委員名單」「加入會員」3 筆導覽文標題持續存在快取（因不含白名單詞未推播，屬快取污染而非推播風險）。
- **建議修法**：與 08-15 報告問題 1 相同（在 `parse_with_claude_fallback()` 套用 `_is_junk` 過濾），diff 見該報告。
- **是否已修**：未修。

## 三、優化建議

### 建議 1（方向 A）：驗證 Google Alerts／台北市都發局的 fetched-notify 落差為正常現象，非白名單過嚴（新完成驗證）

- **依據**：近 7 天 Google Alerts 累計 `fetched=20、new=18、notify=1`，落差看似極大。實際將現行 `whitelist`/`blacklist`/`regions` 規則套用在 `state.json` 現有 199 筆快取上重放比對：65 筆（32.7%）不含任何白名單關鍵字（多為與標案完全無關的新聞快訊雜訊，如「阿里山貴賓館整修完成獲國家卓越建設獎」）、100 筆（50.3%）雖含白名單詞但標題/機關名不含「台北/臺北/新北」（例如彰化縣、基隆市、台中市等其他縣市的合法標租公告，抽查近 20 筆全數為外縣市案件，無雙北案件被誤殺的跡象）、5 筆被黑名單擋下（皆為「開標結果」「自動販賣機」，判斷正確）、僅 29 筆（14.6%）三項全過。這代表 Google Alerts 的 18 筆「新增」中，多數是被地區篩選正確排除的外縣市快訊，並非白名單過嚴誤殺。台北市都發局同理：累計 `fetched=150、new=2、notify=0`，其快取為 08-12 API 恢復後一次性回溯的社會住宅商業設施歷史公告（多數已截標/已決標，日期民國110~115年不等），`new=2` 低是因為 07-28~08-16 期間官網本身鮮少發新公告，非篩選過嚴。
- **做法**：無需修改程式碼；建議往後若再懷疑某來源「落差過大」，優先用上述「離線重放篩選規則」的方式快速驗證是否為誤殺，而非直接假設白名單過嚴。
- **預期效果**：避免誤判正常運作的篩選規則、浪費人力去放寬不該放寬的白名單。

### 建議 2（方向 A，風險項）：停車場黑名單缺口（見問題 3，附最新數據）

- **依據**：見「二、異常與處理」問題 3，`state.json` 全庫現有 25 筆停車類標題符合現行白名單但未被黑名單擋下，其中至少 8 筆為雙北轄區案件。
- **做法**：核准套用 `scraper.py:69-75` 新增 4 個停車相關黑名單詞（diff 見 `qa_reports/2026-08-15.md` 建議 1）。
- **預期效果**：徹底排除已在資料中確認存在的雙北停車場誤推播風險，這批風險已連續 3 份報告（08-12/08-15/08-16）記錄在案但未拍板。

### 建議 3（方向 B）：土地銀行出租不動產／台北自來水處明確建議「移除」或「改抓正確分類頁」二選一，不建議維持現狀

- **依據**：見問題 4，兩來源合計每日抓取 4～8 筆（占每日總抓取量約 5～9%），但累積 60+ 天（土地銀行 35 筆快取、自來水處 55 筆快取）**無一筆與業務目標相關**，屬於「來源本身選錯分類頁」而非「篩選過嚴」——`GLOBAL_FILTER` 運作正確（0 筆通過白名單，未誤推），問題在於這兩個 parser 從一開始就沒有抓到正確的『出租/招租』分類頁面，而是抓到該機關的一般客服公告或財物採購公告。
- **做法**：
  1. 台北自來水處：至 `water.gov.taipei` 確認是否存在獨立的「非公用不動產出租」分類（`sms=` 參數可能不同於目前使用的 `B6EE39DA23E072F5`）；若無此類公告，建議直接移除來源。
  2. 土地銀行出租不動產：至 `landbank.com.tw/Bulletin/RentRealty` 確認實際租賃公告表格的 CSS class/id（目前 selector `table tbody tr, table tr, .bulletin-item, .list-item, article` 範圍過廣，疑似誤抓全站共用公告跑馬燈）；若表格為 JS 動態渲染，需改走 Claude fallback 或改用有頭瀏覽器抓取，否則建議移除。
  這兩項均需要在有網路權限的環境操作（本 session 網路出口全面封鎖 `*.gov.tw`／`landbank.com.tw`，見環境限制說明），無法本次代為完成。
- **預期效果**：若確認移除，每日節省 2 次無效 HTTP 請求；若確認改抓正確分類頁，則新增一個目前完全缺漏的雙北公有不動產供給管道。此建議已連續 6 天（08-11 起）未被拍板，建議本次明確二選一，避免無限期擱置。

## 四、環境限制說明

本次監測 session 的 sandbox 網路出口政策再次封鎖所有來源網域（`water.gov.taipei`、`udd.gov.taipei`、`railway.gov.tw` 等測試皆收到 gateway `403`／`policy denial`，僅 GitHub API 可正常存取），`dry_run_all_regions.py --debug` 與 `WebFetch` 均無法連線驗證任何來源的即時 HTML 結構。本報告所有定位與修法建議皆僅依據 `state.json`／`sent_log.json` 既有快取資料與程式碼閱讀完成，**問題 1（國營台鐵地區篩選）的 diff 已用離線快取資料驗證邏輯正確，但未經即時網頁驗證**，套用前務必在有網路權限環境（本機或人工觸發 GitHub Actions）用 `dry_run_all_regions.py --debug --source <來源>` 覆核。此限制已連續至少 3 份報告（08-15、08-16）出現，建議評估是否調整此 QA routine 執行環境的網路政策，否則「階段二」除錯永遠只能停留在離線資料分析層級。

## 五、待決事項

1. **【新／最高優先，已確認 3 次真實外洩，非理論風險】問題 1（國營台鐵地區篩選誤判）**：`scraper.py:194-195` 增加 `NON_TAIPEI_HINTS` 過濾的 diff 是否核准套用？此漏洞已造成 07-17、07-28、08-07 三次非雙北案件被實際推播到 LINE OA，且屬持續性風險（下次同類公告隨時可能再次外洩）。
2. **【已連續 5 天未拍板】問題 3（GLOBAL_FILTER 黑名單缺「停車」類關鍵字）**：diff 是否核准套用？目前純屬僥倖未爆發（25 筆候選項目中至少 8 筆雙北案件）。
3. **【已連續 6 天未拍板】問題 4（土地銀行出租不動產／台北自來水處抓取內容 100% 與業務目標不符）**：兩來源是否移除，或安排人工確認正確的公告分類頁？請明確二選一。
4. 問題 2（url/date 欄位大量空白，影響 60% 快取）、問題 5（`_is_junk` 死代碼未接線）的修法（見 `qa_reports/2026-08-15.md`）是否核准套用？
5. 國有財產署「地上權設定」類別是否納入業務範圍，需人工判斷（延續多份前次報告的待決事項）。
6. 本 QA routine 執行環境的網路出口政策是否調整，以便「階段二」除錯能做即時 HTML 驗證，而非僅依賴離線快取資料。
