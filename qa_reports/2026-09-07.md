# 標案爬蟲監測報告 2026-09-07

## 一、狀況總覽

整體狀態：⚠️ 有注意事項（今日執行本身正常、無新的連線全面失敗；延續多項已知、長期未拍板的結構性問題；政府採購網 fetched=0 天數由 3 天延長為 4 天；新觀察到 Google Alerts 今日單日 fetched 暴跌至 0）

最近執行：2026-09-07 01:30（推播 0 筆 / 新增 2 筆 / 抓取 86 筆）

| 檢查項目 | 結果 | 說明 |
|---|---|---|
| 1. 今日已執行 | ✅ | `sent_log.json` 最新 key 為 `2026-09-07 01:30`，即今天 |
| 2. 最近一次 GitHub Actions 結論 | ✅ | `daily.yml` run `34073212378`（run #104），2026-09-07T01:30:06Z，`conclusion=success`；job log 確認 13 個來源全數執行完畢、`state.json`／`sent_log.json` 皆成功 commit；近 6 次 run（09-02～09-07）全數 success |
| 3. line_pushed 與 total_notify 一致性 | ✅ | `_summary`：`total_fetched=86`、`total_new=2`、`total_notify=0`、`line_pushed=false`；job log 顯示「無新增案件，略過 LINE 推播」，數字吻合 |
| 4. 是否所有來源 fetched=0 | ✅ | 否，13 來源中 11 個 >0，政府採購網、Google Alerts 為 0 |
| 5. 個別來源連續 3 天以上 fetched=0 | ⚠️ | 政府採購網：09-04～09-07**連續第 4 天** fetched=0（前期穩定 2 筆/日），已超過 09-06 報告預告的 3 天觀察門檻，可觀測性 diff 仍未套用（見問題 1）。Google Alerts：今日（09-07）為近 7 天內**首次**降到 0（前 6 天為 1/6/1/9/2/3 筆），尚未達 3 天門檻，列入觀察，非結構性異常（見下方說明） |
| 6. 來源抓取量暴跌/暴增 | ⚠️ | Google Alerts 今日 0 筆，相較近 7 天均值（約 3.1 筆/日）為 100% 驟降，達 >50% 門檻；今日 job log 中該來源無 WARNING（HTTP 請求成功、僅該次 feed 無項目），研判為當日該關鍵字 Google Alert 本身無新通知，非爬蟲故障，建議僅觀察不列為待決事項。其餘 12 來源近 7 天皆與均值相差在 ±25% 內 |
| 7. 應擋卻漏網的案件 | ❌ | 延續性問題：國營台鐵地區篩選失效（見問題 1，`scraper.py:196-198`，程式碼今日未變動）；`GLOBAL_FILTER` 黑名單仍缺「停車場／停車位」關鍵字（見問題 3）。今日 `國營台鐵 new=1`、`notify=0`，未產生新的外洩推播案例，但缺陷本身仍在 |
| 8. 標題為分類名稱／導覽文而非案名 | ⚠️ | 今日推播為 0 筆，本身不涉及此問題；延續性樣本仍在：國有財產署「【招標公告】北區分署 115年第17批…」、國防部政治作戰局「公告115年臺北市等5縣市44宗不動產標租」皆為批次彙總標題 |
| 9. url 指向列表頁而非個別公告頁 | ❌ | 延續性問題：`state.json` 中政府採購網、國有財產署部分快取、國防部政治作戰局、教育部學產基金等來源歷史快取內仍有 `url=""` 或退回列表頁常數，程式碼未變動 |
| 10. date 欄位為空、導致日期窗被跳過放行 | ✅ | 今日抓取項目日期欄多數有值，未見異常放行案例 |

## 二、異常與處理

### 問題 1（延續，自 09-03 起第 5 天未修，唯一造成過真實外洩，業務衝擊最高）：國營台鐵地區篩選對此來源仍完全失效

- **現象**：`passes_filters()` 的地區檢查用 `title + agency` 一起比對，而國營台鐵每筆項目的 `agency` 固定為「臺北營業分處」（`scraper.py:192-195`），該字串恆含「臺北」，導致地區篩選對此來源恆定通過，形同虛設。累計已造成 2 次真實外洩（09-01 基隆市安樂區、09-03 桃園市桃園區），近 7 天（09-01～09-07）推播 4 筆中 2 筆非雙北。今日 `new=1`、`notify=0`，未產生新外洩，但程式碼未變動，缺陷仍在。
- **定位**：`scraper.py:196-198`。
- **建議修法**（scoped 在 `parse_tra()` 內，不影響其他來源，與 09-05／09-06 報告一致）：
  ```diff
       items = [i for i in items if "臺北營業分處" in i.get("agency", "") + i.get("title", "")]
  +    def _tra_is_shuangbei(title: str) -> bool:
  +        parts = re.findall(r"【(.*?)】", title)
  +        loc = parts[2] if len(parts) > 2 else title
  +        return any(k in loc for k in ("台北", "臺北", "新北"))
  +    items = [i for i in items if _tra_is_shuangbei(i["title"])]
       log.info(f"  [國營台鐵] {len(items)} 筆（臺北營業分處）")
       return items
  ```
- **驗證阻礙**：本環境對 `www.railway.gov.tw` 仍為 proxy 403，無法即時重跑驗證。
- **是否已修**：未修。

### 問題 2（延續，自 09-04 起第 4 天未修，已超過 3 天觀察門檻）：政府採購網連續 4 天 fetched=0，且程式無法區分「查無資料」與「頁面改版」

- **現象**：09-04～09-07 四天皆為 0 筆（前期 08-31～09-03 穩定 2 筆/日）。今日正式環境 job log 同樣無「連線失敗」WARNING（`[政府採購網/出租] 0 筆`／`[政府採購網/標租] 0 筆`），代表 HTTP 請求本身成功，但目前程式在找不到結果表格時完全不記錄任何線索。
- **定位**：`scraper.py:428-456`（`_pcc_fetch_keyword()`），已確認 `target_table` 為 `None` 分支目前無 log。
- **本環境驗證阻礙**：本 session 對 `web.pcc.gov.tw` 為 proxy 403（`ProxyError('Unable to connect to proxy', OSError('Tunnel connection failed: 403 Forbidden'))`），無法比對目前 HTML 結構是否變動。
- **建議修法**（先補強可觀測性）：
  ```diff
       if target_table:
           for row in target_table.find_all("tr")[1:]:
               ...
  +    else:
  +        log.warning(f"  [政府採購網/{keyword}] 找不到結果表格（頁面結構可能改版，"
  +                    f"或該關鍵字/日期區間查無資料），HTTP {r.status_code}，內容長度 {len(r.text)}")
  ```
- **是否已修**：未修。已連續 4 天 0 筆，建議優先套用此 diff，下次執行即可從 log 判斷成因，並安排人工開啟 `https://web.pcc.gov.tw/prkms/tender/common/basic/readTenderBasic` 比對頁面結構。

### 問題 3（延續，長期未拍板）：`GLOBAL_FILTER` 黑名單缺「停車場」；國有財產署 `skip_global_filter=True`

- 狀態與 09-06 報告一致，程式碼未變動（`scraper.py:69-75` 黑名單、`scraper.py:757` `skip_global_filter`）。`state.json` 內台北市財政局歷史快取仍有「作臨時路外停車場使用」「作單一停車位使用」等案件未被攔截。
- **建議修法**：同 09-06 報告 diff（新增 `"停車場"`、`"停車位"` 至 blacklist；國有財產署移除 `FNP_PAGES` 停車場分類頁並拿掉 `skip_global_filter`）。
- **是否已修**：未修。

### 問題 4（延續，長期未拍板）：安幸 ERP 匯入 header 值含換行字元

- **現象**：凡當日 `notify>0` 必失敗（累計 09-01、09-03、09-04、09-05 共 4/4 命中，錯誤訊息「Invalid leading whitespace...header value: '***\n'」）。今日與 09-06 皆 `notify=0`，未觸發，缺陷本身仍未修復。
- **定位**：`scraper.py:1147-1148`。
- **建議修法**：`url.strip()` / `key.strip()`（同前次報告），並需人工至 GitHub Secrets 重新貼上不含換行的 `ANXING_KEY`／`ANXING_URL`。
- **是否已修**：未修。

### 問題 5（延續，長期未拍板）：`SOURCES` 中各來源自訂 `whitelist`/`blacklist`/`regions` 從未生效（死碼），含 Google Alerts 的 `gov.tw` 全台放行死碼

- 狀態不變，`passes_filters()`（`scraper.py:808`）只讀取 `GLOBAL_FILTER`。近 7 天 Google Alerts `new=21` 但 `notify=2`（91% 被過濾掉），與此死碼直接相關。
- **建議修法**：同前次報告，`passes_filters(item, source_cfg=None)` 改為套用交集，或至少讓 Google Alerts 額外檢查 `item["url"]` 是否含 `.gov.tw`。
- **是否已修**：未修。

### 問題 6（延續，09-05 發現，今日無新發生）：國有財產署「115年第17批」重複紀錄仍原樣保留在 `state.json`

- **狀態確認**：直接檢查 `state.json['國有財產署']`，確認同一案件（`msgId=cfb240db54e5460c8b987bba9253b9db`）仍以兩筆紀錄存在——一筆是 09-04 `item_key()` 改版（commit `4e0deb6`／`cc2c1ba`，補上 `id` 欄位去重機制）之前寫入的舊格式（無 `id`），一筆是改版後補寫入的新格式（有 `id`）。兩者 title/date/url 完全相同，僅 `id` 欄位有無不同，`item_key()` 因此視為兩個不同 key，這正是造成 09-01、09-05 重複推播的根因，且該次修版**未回填清理舊格式紀錄**，缺陷資料仍留在快取內。今日 `國有財產署 new=0`，未再次觸發。
- **是否已修**：程式邏輯已於 09-04 部分修正（新抓到的案件會帶 `id`），但 `state.json` 內既有重複紀錄未清理，仍有風險。建議：人工評估是否需要手動去重 `state.json['國有財產署']` 中的重複項（保留任一筆即可，因兩者代表同一實際案件）。

## 三、優化建議

### 建議 1（方向 A）：政府採購網已連續 4 天 fetched=0，已超過觀察門檻，應優先套用可觀測性 diff

- **依據**：08-31～09-03 穩定 2 筆/日，09-04～09-07 連續 4 天 0 筆；近 7 天合計 `fetched=6`（過去同期通常 >10）。目前程式在找不到結果表格時不會留下任何 log 線索。
- **做法**：套用問題 2 的 diff（記錄 HTTP 狀態碼與內容長度），下次執行即可從 log 判斷是「找不到表格」還是「表格存在但查無資料」；並安排在有網路權限環境人工開啟查詢頁面比對 HTML 結構。
- **預期效果**：釐清政府採購網斷流原因，避免此來源長期沉默而不自知。

### 建議 2（方向 A）：國營台鐵地區誤判與黑名單缺「停車場」是兩個已有具體數字佐證、尚未套用的漏網高風險項

- **依據**：近 7 天國營台鐵推播 4 筆中 2 筆（50%）為非雙北案件（見問題 1）；`state.json` 台北市財政局歷史快取中已有停車場類案件，黑名單無法攔截（見問題 3）。
- **做法**：套用問題 1 的 `parse_tra()` diff ＋ 問題 3 的 blacklist diff；待有網路權限環境時用 `dry_run_all_regions.py --debug` 驗證。
- **預期效果**：國營台鐵不再誤放非雙北案件；停車場類土地標租不再有機會被推播。

### 建議 3（方向 B）：長期低產出來源近 7 天彙總數字如下，逐一判斷「移除」或「維持」

| 來源 | 7天fetched | 7天new | 7天notify | notify/fetched |
|---|---:|---:|---:|---:|
| 台北市都發局 | 210 | 1 | 0 | 0% |
| 農業部 瑠公管理處 | 105 | 0 | 0 | 0% |
| 國防部政治作戰局 | 77 | 0 | 0 | 0% |
| 台北市財政局 | 70 | 1 | 0 | 0% |
| 郵局房地產出租 | 26 | 2 | 0 | 0% |
| 台北自來水處 | 23 | 4 | 0 | 0% |
| Google Alerts | 22 | 21 | 2 | 9% |
| 國家住宅及都市更新中心 | 53 | 9 | 1 | 2% |
| 教育部學產基金 | 14 | 0 | 0 | 0% |
| 土地銀行出租不動產 | 14 | 3 | 0 | 0% |
| 國營台鐵 | 20 | 5 | 4 | 20% |
| 國有財產署 | 7 | 2 | 2 | 29% |
| 政府採購網 | 6 | 0 | 0 | 0% |

- **依據**：與 09-06 報告一致的結論——國防部政治作戰局連續多週 `new=0`，且標題為全國批次彙總格式（一筆記錄涵蓋 5～13 個縣市上百宗物件），無法從標題判斷雙北占比，內容形式與其他來源本質不同。其餘低產出來源（都發局、瑠公管理處、財政局、郵局、自來水處、教育部學產基金、土地銀行）近期 `state.json` 快取抽查仍為真實不動產標租/工程採購公告，研判為官方公告本身低頻，非抓錯頁面，**不建議移除**。
- **做法**：維持 09-06 報告的建議——**建議移除**國防部政治作戰局；**建議調整**台北自來水處（若有獨立不動產租賃分類頁可改抓）；其餘維持現狀，優先處理問題 1、2（過濾邏輯），而非移除來源。
- **預期效果**：同前次報告。

### 建議 4（方向 C）：暫不提出新來源候選

- 本次執行環境對外部網域（`web.pcc.gov.tw`、`www.railway.gov.tw` 等）仍為 proxy 403，且方向 A、B 已有明確、量化的待處理問題，延續前幾次報告判斷，暫緩新來源調查。

## 四、待決事項

1. 【延續，自 09-03 起第 5 天，唯一造成過真實外洩的結構性問題】問題 1（國營台鐵地區篩選失效）：是否核准 `parse_tra()` 的地點判定 diff？並授權在有網路權限環境驗證？
2. 【延續，自 09-04 起第 4 天，已超過 3 天觀察門檻】問題 2（政府採購網連續 4 天 fetched=0）：是否核准先套用可觀測性 diff？並安排人工或有網路權限環境比對 `web.pcc.gov.tw` 目前 HTML 結構？
3. 【延續，長期未拍板】問題 3（`GLOBAL_FILTER` 缺「停車場」黑名單 + 國有財產署 `skip_global_filter=True`）：diff 是否核准套用？
4. 【延續，長期未拍板，凡有推播必失敗 4/4】問題 4（安幸 ERP header 值未 `.strip()`）：是否請人工至 GitHub repo Secrets 重新設定 `ANXING_KEY`／`ANXING_URL`？程式碼端 `.strip()` 防禦修法是否核准？
5. 【延續，長期未拍板】問題 5（`SOURCES` 逐來源篩選設定從未生效，含 Google Alerts 的 `gov.tw` 全台放行死碼）：是否核准套用？
6. 【延續，自 09-05 發現】問題 6：國有財產署「115年第17批」在 `state.json` 中的重複紀錄（`id` 欄位補齊前後各一筆）是否需要人工手動清理？
7. 【延續，09-06 建議 3】是否核准將國防部政治作戰局自 `SOURCES` 移除（長期 `new=0` ＋標題為全國批次彙總、無法判斷雙北占比）？
8. 方向 C（新來源候選）：是否於下次有網路權限時再行補上，或由人工提供候選清單？
