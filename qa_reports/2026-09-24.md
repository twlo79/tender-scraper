# 標案爬蟲監測報告 2026-09-24

> **報告產生時間說明**：本次報告於 UTC 01:09（台灣時間 09:09）產生，`daily.yml` 由 Make.com 觸發的時間點約為 UTC 01:30（台灣時間 09:30），本次執行**尚未發生**。以下所有「今日」相關數據皆以**最近一次已完成執行（2026-09-23 01:30 UTC，run #120）**為準，並延續 09-21～09-23 報告已建立的問題編號（問題 1～23）繼續追蹤，本次新增問題 24～26。
>
> **事後更新（同日追加，第 1 次）**：問題 4（ANXING header 未 `.strip()`）已於報告發布後由使用者核准並套用修法，commit `c1e4402`，已 push 至 `claude/modest-volta-rgq8mv`。
>
> **事後更新（同日追加，第 2 次）**：使用者用獨立測試腳本（不經過 scraper.py，直接呼叫同樣的 POST 邏輯）實測 ERP 匯入，過程中發現並確認了**一個問題 4 之外、先前從未被發現的新問題**——見下方新增「問題 27」。`.strip()` 修法本身已用實測驗證有效（過程中 `ANXING_URL` 環境變數值意外多打一個前綴空白，仍正常送出，證實 `.strip()` 生效）。

## 一、狀況總覽

整體狀態：❌ 有異常（問題 1、3、5、6、7、9、10、11、13、15、17 全數延續未修；**問題 4（ANXING header）已套用 `.strip()` 修法並經實測驗證有效（commit `c1e4402`）**；**問題 27（新增，同日追加）**：修好問題 4 後，實測發現 `ANXING_URL` 這個 GitHub Secret 長期指向缺少 `/api/import/tenders` 路徑的錯誤網址——過去被問題 4 完全遮蔽而從未被發現，使用者已於本次對話中自行更新 Secret 並實測 `HTTP 200` 成功，**待 09-24 執行完整驗證閉環**；**問題 3 本次重新檢視後認定應提升 severity**：09-21 推播的「地上權設定」案件經業務定義重新核對，應歸類為真實誤報，而非先前報告認定的「非漏網」；**問題 23 本次發現其樂觀預測（aa7f25f 修復後隔日自動恢復）並未完全成立**：教育部學產基金在該修復生效之後的 09-23 執行仍重複推播「永春站」同一案，代表修復只解決單一方向，見新增問題 24；新增問題 25（國防部政治作戰局混入社會住宅導覽頁）、問題 26（土地銀行來源疑似抓到錯誤區塊，非招租公告）。

最近執行：2026-09-23 01:30（GitHub Actions run #120，`conclusion=success`）— 推播 4 筆 / 新增 118 筆 / 抓取 172 筆

| 檢查項目 | 結果 | 說明 |
|---|---|---|
| 1. 今日已執行 | ⚠️（尚未到執行時間，非異常） | `sent_log.json` 最新 key 為 `2026-09-23 01:30`；本次 QA 於台灣時間 09:09 執行，早於 daily.yml 約 09:30 的觸發時間，屬正常過渡態，非 Make.com 故障 |
| 2. 最近一次 GitHub Actions 結論 | ✅ | run #120（`35806532830`）`conclusion=success`；`list_workflow_runs` 近 10 次（run #111～#120，09-14～09-23）全數 `success` |
| 3. line_pushed 與 total_notify 一致性 | ✅（數量一致，但內容重複，見問題 14/24） | job log：`✅ LINE broadcast 成功（4 則）`，`sent_log._summary`：`total_notify=4, line_pushed=true`，數量吻合；但 4 則中 3 則對應同一筆瑠公管理處案件（自有 parser + Google Alerts ×2），實際僅 2 筆不同案件 |
| 4. 是否所有來源 fetched=0 | ✅ | 否，13 個來源皆 `fetched>0`（合計 172 筆） |
| 5. 個別來源連續 3 天以上 fetched=0 | ✅ | 目前無來源處於連續掛零狀態（政府採購網、國有財產署過去的斷流已中止且穩定） |
| 6. 來源抓取量暴跌/暴增 | ⚠️ | 政府採購網 1→53 筆（已知為問題 2 端點修復所致，非異常）；其餘來源 7 天趨勢平穩 |
| 7. 應擋卻漏網的案件 | ❌ | **09-21 推播「【地上權設定】北區分署 115年第5批 國有非公用不動產」不屬於「租賃/標租」業務範圍**（地上權設定是土地使用權設定，法律性質不同於租賃，通常由承租人於地上自建房屋並長期持有使用權），應視為真實誤報，詳見問題 3 更新 |
| 8. 標題為分類名稱／導覽文而非案名 | ❌ | 國防部政治作戰局 `state.json` 中存在「中央社宅申辦應備文件」「中央社宅招租線上申請操作懶人包」等純導覽/操作說明頁面，非具體案名，詳見問題 25 |
| 9. url 指向列表頁而非個別公告頁 | ⚠️ | HURC（國家住宅及都市更新中心）問題 17 延續（約 63% 連結為列表頁 fallback，本次因 sandbox 無法連線重新統計精確比例）；其餘來源今日推播項目 url 正常 |
| 10. date 欄位為空、導致日期窗被跳過放行 | ❌ | **台北自來水處 103/103（100%）**、**土地銀行出租不動產 44/44（100%）** date 欄位皆空；政府採購網舊查詢殘留 231/300（77%，多為問題 2 修復前的 Claude fallback 舊資料，會隨新資料寫入自然淘汰，非持續性風險） |

## 二、異常與處理

> **環境限制說明**：本次 sandbox 對外連線（`web.pcc.gov.tw`、`gpwd.mnd.gov.tw`、`www.landbank.com.tw` 等）仍被 agent proxy 以 403 阻擋，`WebFetch` 對 `landbank.com.tw` 同樣回報 `EGRESS_BLOCKED`，無法即時執行 `dry_run_all_regions.py` 對外抓取驗證。改採：① 直接讀取 `state.json`／`sent_log.json` 的完整歷史紀錄逐筆核對真實內容（而非僅看聚合數字）；② 用 GitHub API 取回 run #120 完整 job log 逐行核對；③ 通讀 `scraper.py` 相關函式原始碼，交叉比對假說與實際資料。問題編號延續 09-23 報告。

### 問題 3（**狀態升級：09-21 的真實推播案例應重新歸類為已發生的誤報，而非理論風險**）

- **現象**：09-21 報告當時僅核對「地上權設定」標題是否命中 `GLOBAL_FILTER["blacklist"]`（結果：未命中，判定「非漏網」）。本次重新以**業務目標本身**（"雙北地區的公有不動產「租賃／標租」案件"）覆核：「地上權設定」是《國有財產法》下另一種處分方式，機關將土地的長期使用權（通常 20～70 年）讓與承租人，由承租人在地上自建並持有建物所有權，法律性質與「單純出租供他人使用、期滿即收回」的租賃／標租**完全不同**，如同「停車場」「自動販賣機」一樣不屬於目標業務範圍。09-21 該筆推播（`【地上權設定】北區分署 115年第5批 國有非公用不動產 公告:115年09月21日 開標:115年11月09日`）因此應歸類為**真實誤報**，已推送給使用者，無法回溯撤回。
- **定位**：`scraper.py:339`（`FNP_PAGES` 第 2 項 `{"cat": "地上權設定", ...}`）、`scraper.py:769`（`"skip_global_filter": True`，使該來源完全繞過 `GLOBAL_FILTER["blacklist"]`，即使補了黑名單關鍵字也擋不住）。
- **建議修法**（比 09-22 報告單純「補黑名單」更根本，因為 `skip_global_filter=True` 會讓黑名單完全失效）：直接不抓地上權設定分類頁：
  ```diff
   FNP_PAGES = [
       {"cat": "招標公告",   "url": "https://esvc.fnp.gov.tw/rtMsg?svcId=5eafac8df8c649ba9cf62a591e44223c"},
  -    {"cat": "地上權設定", "url": "https://esvc.fnp.gov.tw/tenderSetSuperficies?svcId=f6c87d65ebc84e988b4d245777d83a81"},
  -    {"cat": "停車場",     "url": "https://esvc.fnp.gov.tw/parkingMsg?svcId=a211eb824d6841d598fc784fb44ba962"},
       {"cat": "創業租賃",   "url": "https://esvc.fnp.gov.tw/crtMsg?svcId=ecc2694bdf6144e298053cb2a9f2460f"},
   ]
  ```
  （停車場一併移除的理由同 09-22 報告問題 3 原文，本次未變）。
- **是否已修**：未修。**建議提升優先度**：這不再是「尚未發生的風險」，而是「已發生 1 次的真實誤報」。

### 問題 22（延續，本次補上具體的雙北案件被誤殺範例）：政府採購網編碼猜測失敗，導致亂碼且會讓地區篩選失效

- **現象**：`state.json['政府採購網']` 中 34/300（11%，集中在 09-22、09-23 兩天新抓的項目，比例與昨日 34/53=64% 一致，代表今日無新增亂碼，因為尚無新執行）筆標題呈現亂碼。本次額外解碼比對其中一筆：
  ```
  {'id': 'CA1150902R', 'title': 'з¬¬2ж¬Ўе…¬й–ӢжЁҷз§ҹиҮәеҢ—еёӮдёӯжӯЈеҚҖдёӯиҸҜи·Ҝ2ж®ө409е··5еј„4иҷҹзӯү18жҲ¶еёӮжңүдёҚеӢ•з”ў',
   'date': '115/09/23', 'url': '...pk=70023704...'}
  ```
  對照 `sent_log.json` 中 09-15 由台北市財政局來源正確推播過的同一案「**第2次公開標租臺北市中正區中華路2段409巷5弄4號等18戶市有不動產**」，可確認這筆亂碼案件其實是**臺北市中正區**的真實雙北案件。由於編碼錯誤，標題中的「臺北市」變成無法比對的亂碼字元，若 `agency` 欄位（`tds[1]`，機關名稱）同樣受編碼影響（同一頁面、同一次解碼，理論上會一起亂碼），則 `passes_filters()` 的地區篩選（`regions` 比對 `title+agency`）會判定此案「不含台北/新北」而**誤殺**。本次案例因已由台北市財政局來源提前 8 天推播過，未造成使用者實際漏收，但證明了問題 22 描述的風險是真實機制、不是假設。
- **定位**：`scraper.py:420`（`r.encoding = r.apparent_encoding or "utf-8"`）。
- **建議修法**（未變更）：
  ```diff
       r = requests.get(URL, headers=HTTP_HEADERS, timeout=30, verify=False)
       r.raise_for_status()
  -    r.encoding = r.apparent_encoding or "utf-8"
  +    r.encoding = "utf-8"
  ```
- **是否已修**：未修。**建議提升優先度**：已找到「編碼亂碼確實會導致雙北案件被誤殺」的具體實例，不再只是理論推演。

### 問題 24（**新增**）：`find_new_items()` 的 url 補齊索引只處理單一方向，教育部學產基金於 09-23（`aa7f25f` 修復生效後）仍重複推播同一案

- **現象**：09-23 報告（問題 23）預期 `aa7f25f`（09-22 02:31 UTC 提交）已修好「Claude fallback 遺失 url 導致誤判為新案」的問題，並預期 09-24 執行後都發局/政治作戰局的 `new` 會恢復正常。但直接核對 `sent_log.json`，**同一案件在該修復生效後仍重複推播**：
  ```
  2026-09-22 01:30 教育部學產基金：['115年度臺北捷運永春站國有學產房地公開標租']
  2026-09-23 01:30 教育部學產基金：['115年度臺北捷運永春站國有學產房地公開標租']
  ```
  且目前 `state.json['教育部學產基金']` 中，這筆案件確實**同時存在 2 筆紀錄**（皆為 `date: '115-09-14'`，一筆 `url` 空、一筆 `url` 有值）：
  ```
  {'id': '', 'title': '115年度臺北捷運永春站國有學產房地公開標租', 'date': '115-09-14', 'url': ''}
  {'id': '', 'title': '115年度臺北捷運永春站國有學產房地公開標租', 'date': '115-09-14', 'url': 'https://depart.moe.edu.tw/News_Content.aspx?...'}
  ```
  根因：`find_new_items()`（`scraper.py:1008-1047`）的 `empty_url_index` 只索引「舊紀錄 url 空」的情況，用來辨識「今天補齊 url」（空→有）的方向；但**沒有處理反方向**——若某天因網路狀況改走 Claude fallback、抓不到 `href`，新項目的 `url` 是空的，而舊紀錄已經有 url，這種「有→空」的退化版本不會被目前的索引攔截，會被當成全新案件寫入並推播。09-22 該案第一次由正常 parser 抓到（有 url）寫入 state；09-23 疑似再次觸發 Claude fallback（或站方回應不含 `<a href>`），抓到同案但 url 空，因此又推播一次。
- **定位**：`scraper.py:1008-1047`（`find_new_items()`，`empty_url_index` 僅單向索引）。
- **建議修法**：新增反方向索引，「新項目沒有 url、但舊紀錄同 title+date 已有 url」時視為重複、不推播也不覆蓋原紀錄：
  ```diff
   def find_new_items(name: str, items: list[dict], state: dict) -> list[dict]:
       existing = state.get(name, [])
       seen = {_entry_key(e) for e in existing}

       empty_url_index: dict[str, str] = {}
  +    filled_url_titles: set[str] = set()
       for e in existing:
           d = e if isinstance(e, dict) else {"title": e, "date": "", "url": ""}
           if not d.get("url"):
               empty_url_index[_title_date_key(d)] = _entry_key(d)
  +        else:
  +            filled_url_titles.add(_title_date_key(d))

       new: list[dict] = []
       backfill_old_keys: set[str] = set()
  +    degraded_keys: set[str] = set()
       for i in items:
           k = item_key(i)
           if k in seen:
               continue
           td = _title_date_key(i)
           if i.get("url") and td in empty_url_index:
               backfill_old_keys.add(empty_url_index[td])
               continue
  +        if not i.get("url") and td in filled_url_titles:
  +            degraded_keys.add(k)
  +            continue
           new.append(i)

       merged: dict[str, dict] = {}
       for e in existing:
           k = _entry_key(e)
           if k in backfill_old_keys:
               continue
           merged[k] = {"title": e, "date": "", "url": ""} if isinstance(e, str) else e
       for i in items:
           k = item_key(i)
  +        if k in degraded_keys:
  +            continue
           merged[k] = {"id": i.get("id", ""), "title": i.get("title", ""), "date": i.get("date", ""), "url": i.get("url", "")}
       state[name] = list(merged.values())[-300:]
       return new
  ```
- **是否已修**：未修。依規則本次僅提出 diff，未套用；也**未修改 `state.json`**（目前已存在的重複紀錄依規則保留）。
- **待決重點**：09-23 報告對問題 23 的「預期明日自動恢復」過於樂觀，建議 09-24 執行完成後**務必**重新核對都發局/政治作戰局的 `new` 是否也出現同一種反方向重複（目前兩者 `url` 空值比例仍有 `35/65`、`11/31`，並非 0，結構性風險尚未清零）。

### 問題 25（**新增**）：國防部政治作戰局（cnid=695）混入「社會住宅」個人申請導覽頁，非租賃/標租招標案

- **現象**：`state.json['國防部政治作戰局']` 抽查近期紀錄，除了 cnid=609（眷村土地標租彙總 PDF，屬正確業務範圍）外，cnid=695 子頁面收錄的多為**社會住宅招租公告**（給個人承租戶申請入住抽籤，非公有不動產出租招標）與純導覽頁：
  ```
  114/10/21  臺北市文山區「自慢藏」社會住宅招租公告及申請須知
  114/10/21  中央社宅申辦應備文件
  114/10/21  中央社宅招租線上申請操作懶人包
  114/10/21  新北市三重區「富貴好室」社會住宅招租公告及申請須知
  114/10/21  高雄市三民區「美都明仁」社會住宅招租公告及申請須知（非雙北，region 篩選可擋，但性質同樣不對）
  ```
  「中央社宅申辦應備文件」「中央社宅招租線上申請操作懶人包」是純粹的操作說明/導覽頁，並非具體案名，符合 QA 檢查項目 8 描述的情況；而「社會住宅招租公告」即使標題含雙北地名、命中白名單「招租」，其業務性質是**提供給一般民眾承租居住**的社宅抽籤申請，與任務定義的「公有不動產出租標租案」（機關將產權出租給廠商/法人經營使用）並非同一件事。這批項目目前因日期普遍是 114 年（2025 年，距今超過 10 天），被 `is_within_date_window()` 擋下未推播，**屬僥倖**：若站方之後更新同一批公告的日期戳記（常見於「最後修改」型網站），或改版後日期欄位被清空（依「無法解析日期時放行」規則），就可能被誤推。
- **定位**：`scraper.py:577-613`（`parse_gpwd()`，同時抓 cnid=609 與 cnid=695 兩個性質不同的子頁面）。
- **建議修法（兩種方案擇一，皆需人工決定業務範圍）**：
  - 方案 A（偏保守）：`GLOBAL_FILTER["blacklist"]` 加入 `"社會住宅招租"`／`"申辦"`／`"操作懶人包"`／`"申請須知"` 等詞（但需注意台北市都發局也有合法的「標租…社會住宅內商業設施」案件，不能整詞擋「社會住宅」，需用更精確的詞組如「社會住宅招租公告及申請須知」）。
  - 方案 B（偏乾淨）：`parse_gpwd()` 的 `for cnid in ("609", "695")` 移除 `"695"`，只保留 cnid=609（眷村土地標租彙總）。
- **是否已修**：未修，兩方案皆待人工拍板。

### 問題 26（**新增**）：土地銀行出租不動產來源疑似抓到網站共用公告區塊，非「出租不動產」專屬列表

- **現象**：`state.json['土地銀行出租不動產']` 44 筆記錄，**title 為空字串的紀錄佔多數**（`agency` 固定為 `"土地銀行"` 但實際 `title` 內容抽查如下，並非空白而是被截斷顯示，逐筆核對後皆為銀行一般公告，非出租招標案）：
  ```
  近期發現有不法人士偽冒本行「土銀too幸福(landhappy)」行動應用程式及活動網頁，為保障您的個資安全，…
  一卡通票證公司將於115年8月22日(星期六)01:00起05：00進行系統維護作業…
  本行修訂「保管箱出租契約」，自115年11月1日起實施。
  土銀行史展
  本行「智能客服」將於115/9/8（二）17時至20時進行系統維護作業…
  中華電信將於115年9月22日(星期二)21:30起至115年9月23日(星期三)01：30進行系統維護作業…
  ```
  **44 筆中沒有 1 筆是真正的不動產招租公告**，且「保管箱出租契約」這種個人保管箱租賃服務因標題含「出租」二字，剛好命中 `GLOBAL_FILTER["whitelist"]`（見 `scraper.py:805` 該來源自訂白名單同樣含「出租」），是可預期會通過篩選的誤報類型範例，只是至今日期欄位 100% 空白（問題 10）尚未觸發推播。`date` 欄位 100% 空白與這些內容一致：這些是銀行「最新消息」跑馬燈公告，本來就不會有「公告日期」欄位，佐證 parser 抓到的 CSS 選擇器對應的是網站共用的「最新消息」區塊，而非 `/Bulletin/RentRealty` 頁面裡真正的招租公告表格。
- **定位**：`scraper.py:616-642`（`parse_landbank()`）。sandbox 環境對 `www.landbank.com.tw` 的連線與 `WebFetch` 皆被 proxy 擋下（`EGRESS_BLOCKED`），無法直接開瀏覽器核對正確的表格 CSS class/id。
- **建議修法**：待有網路權限環境，人工開瀏覽器檢視 `https://www.landbank.com.tw/Bulletin/RentRealty` 原始碼，確認「出租招租公告」表格的正確 selector（目前程式碼用泛用的 `table tbody tr`／`article`，極可能連帶抓到頁面共用的公告跑馬燈區塊）。在確認前，此來源 **7 天 notify 總數為 0、且 44 筆歷史紀錄 0 筆命中真正業務範圍**，建議見三、優化建議「方向 B」。
- **是否已修**：未修，待網路權限環境重新確認頁面結構。

### 問題 27（**新增，同日追加，已由使用者實測確認並解決**）：`ANXING_URL` 長期指向錯誤路徑（缺 `/api/import/tenders`），且被問題 4 完全遮蔽、從未被發現

- **現象**：問題 4 修法（`.strip()`）套用並 push 後，使用者用獨立測試腳本直接對 production 的 `ANXING_URL`／`ANXING_KEY` 值送出同樣的 POST 請求，第一次測試回傳 **HTTP 405**（Next.js 頁面回應，非 API JSON），代表當時 `ANXING_URL` 指向的是網站首頁網域本身，缺少實際 API 路徑。經在 `anxing-web` 專案原始碼搜尋 `x-import-key` header 比對，確認正確路徑為 `src/app/api/import/tenders/route.ts` → **`/api/import/tenders`**。改用 `https://justwork.estia.com.tw/api/import/tenders` 重測後，回傳 **HTTP 200**：`{"received":1,"saved":1,"skipped":0,...}`，確認匯入邏輯本身正確、`IMPORT_KEY` 也正確。
  - **關鍵推論**：這個路徑錯誤的問題其實從一開始就存在，但因為問題 4（header 值含換行字元）會讓 Python `requests` 在**組 header 階段**就直接丟出例外（`Invalid leading whitespace...`），程式碼根本沒機會真的把 HTTP 請求送出去，所以 production log 過去 6 次真實重現全部都是同一種錯誤訊息，`ANXING_URL` 本身對不對從未被驗證過、也不可能被驗證到——**兩個獨立的 bug 疊在一起，第一個完全遮蔽了第二個**，直到這次修好第一個才讓第二個浮現。
- **定位**：GitHub Secret `ANXING_URL`（不在程式碼中，是 repo Settings → Secrets and variables → Actions 裡的值），程式碼本身（`scraper.py:1190`、`1212`）無需改動，因為 `push_to_anxing()` 本來就假設 `ANXING_URL` 是完整的 API 端點網址。
- **是否已修**：**使用者已在本機驗證正確值並自行更新 GitHub Secret `ANXING_URL` 為 `https://justwork.estia.com.tw/api/import/tenders`**（本次對話中完成，非透過程式碼 commit）。測試資料（標題含「ERP連線測試，可忽略」）已請使用者於安幸後台自行清除。**待 09-24（或下次有推播的執行日）daily.yml 真實跑一次，確認 production log 出現 `✅ ERP 匯入：...` 而非任何警告，才算完整驗證閉環。**

### 問題 1、5、6、7、9、11、13、15、17（延續，本次重新核對程式碼，狀態未變，不重複貼出全文）

- 問題 1：國營台鐵地區篩選失效（`scraper.py:197` 仍用「臺北營業分處」子字串判斷）；今日趨勢 fetched 5→2 為正常波動，未見新外洩
- 問題 5：`SOURCES` 逐來源 `whitelist`/`blacklist`/`regions`（如 `scraper.py:775`、`797`、`805`）仍是死碼，`passes_filters()`（`scraper.py:820`）只讀 `GLOBAL_FILTER`
- 問題 6：國有財產署 `state.json` 重複紀錄，依規則本次未清理
- 問題 7：`log_checker.py:44-47` 讀取 job log 未強制 `r.encoding="utf-8"`；本次重新核對 `daily_report.json`，**自 08-25 至 09-23 共 30 筆歷史紀錄，`sources` 全數為空字典、`total_new`/`total_pushed` 全數為 0**，代表 `parse_log()`（`log_checker.py:53-86`）的兩條 regex 因編碼錯誤自 08-25 有紀錄以來從未真正配對成功過，`daily_report.json` 這個監測工具本身長期完全失效（僅 `errors` 欄位因抓到純 ASCII 的 `WARNING`/`ERROR` 關鍵字還能撈到內容，但內容也是亂碼）
- 問題 9：`GLOBAL_FILTER["regions"]`（`scraper.py:76`）仍只有「台北／臺北／新北／瑠公管理處」，未涵蓋林口/板橋等雙北行政區名
- 問題 11：`qa_report.py` 的 `zero_streak` 仍用 `--days` 限制的視窗而非全歷史計算
- 問題 13：`get()`（`scraper.py:99-106`）對非 2xx 回應仍不記 log
- 問題 15：`state.json` 中 3 個無對應 `SOURCES` 設定的舊來源鍵值，優先度低
- 問題 17：HURC 列表頁 fallback 問題延續未修

## 三、優化建議

### 建議 1（**依據問題 3 升級**，方向 A）：立即從 `FNP_PAGES` 移除「地上權設定」與「停車場」兩個分類頁

- **依據**：09-21 已發生 1 次真實誤報（地上權設定案推播給使用者），且該來源 `skip_global_filter=True` 讓任何補黑名單的方案都無效，唯一可靠做法是不抓這兩個分類的頁面。
- **做法**：見問題 3 的 diff，`FNP_PAGES` 只保留「招標公告」「創業租賃」兩類。
- **預期效果**：徹底消除此來源推播非租賃/標租案件的可能性（目前 4 類中已有 2 類經證實或極可能不屬於業務範圍）。

### 建議 2（**依據問題 26**，方向 B）：土地銀行出租不動產來源 7 天 notify=0、44 筆歷史紀錄 0 筆命中業務範圍，建議「移除」而非僅調整篩選

- **依據**：與其他 7 天 notify=0 的來源不同（台北市財政局、台北市都發局、國家住宅及都市更新中心等所抓到的內容本身皆為合法標租/標售案，只是案件發生頻率低——這些**應予保留**），土地銀行的問題是**抓到的內容本身就不是招租公告**（系統維護通知、防詐騙警示、保管箱契約修訂等），是 parser 選錯區塊的品質問題，不是「業務量低」。調整篩選條件（加黑名單）治標不治本，因為真正的招租表格可能根本沒被抓到。
- **做法**：優先請有網路權限的人工環境核對 `/Bulletin/RentRealty` 頁面正確結構（見問題 26）；若短期內無法排查，建議**先移除此來源**（避免每天浪費 1 次 HTTP 請求且 0 產出，同時避免「保管箱出租契約」這類已知會通過白名單的誤報標題有朝一日被推播），待修好 selector 後再重新啟用。
- **預期效果**：消除 1 個已知 0 產出且有誤報風險的來源；若日後選對表格，可能找回真正的雙北不動產招租公告。

### 建議 3（延續，依據問題 9，未變更）：擴充 `GLOBAL_FILTER["regions"]` 涵蓋雙北行政區名

- 內容同前版，`state.json['國家住宅及都市更新中心']` 中「林口世大運選手村社會住宅店鋪招租」2 筆真實漏網案例延續存在。

### 建議 4（**新增**，方向 B 延伸）：`state.json` 每來源 300 筆去重上限，對政府採購網這類新的高流量來源可能不足

- **依據**：政府採購網修復後單日 `fetched` 已達 53 筆，且 `DATE_WINDOW_DAYS=10` 代表同一筆公告會在未來 9 天的查詢結果中重複出現；若持續維持 50+ 筆/天的抓取量，300 筆上限約只能涵蓋 5～6 天，一旦超過上限、最早寫入的紀錄被 `[-300:]` 裁切掉，該筆公告若之後又出現在查詢結果中，會被誤判為「新案」而重複推播（與問題 24 的根因類型相同，但觸發機制不同：一個是 url 有無造成 key 不同，這個是筆數超過上限造成舊 key 被物理性移除）。
- **做法**：待觀察政府採購網未來 1-2 週的穩定 fetched 量後，評估是否需要將高流量來源的 per-source 上限從 300 提高（例如提高到 1000，或依 fetched 量動態調整），需要人工權衡 `state.json` 檔案大小與 GitHub commit 頻率。
- **預期效果**：避免未來因 state 容量不足造成的重複推播，本次列為前瞻性建議，非急迫。

方向 C（新增來源）：既有來源（A/B 方向）尚有 3 個新發現的具體問題待處理（問題 3 升級、25、26），暫緩評估新來源。

## 四、待決事項

1.【**已解決（同日追加）**】問題 4（ANXING ERP header 未 `.strip()`）：使用者已核准，修法已套用並 push（commit `c1e4402`），並經獨立測試腳本實測驗證有效。剩餘待決：是否仍要重設 GitHub Secret `ANXING_KEY` 本身清除換行字元（非必要，`.strip()` 已可防禦，屬治本 vs 治標的選擇）。
2.【**已解決（同日追加，問題 27）**】`ANXING_URL` 缺少 `/api/import/tenders` 路徑：使用者已在本次對話中自行改好 GitHub Secret `ANXING_URL`，並實測 `HTTP 200` 成功。剩餘待決：測試資料是否已於安幸後台清除（使用者已被提醒但未回報確認）；**待 09-24（或下次有推播的執行日）daily.yml 真實執行一次，確認 production log 出現 `✅ ERP 匯入：...` 而非任何警告，才算完整驗證閉環**——這是本輪待決事項中優先度最高、最快能確認的一項。
3.【**狀態升級，已有 1 次真實誤報，優先度應提升為最高**】問題 3（`FNP_PAGES` 含「地上權設定」「停車場」+ `skip_global_filter=True`）：是否核准直接移除這兩個分類頁面（建議 1）？
4.【**新增**】問題 24（`find_new_items()` url 反方向補齊未處理，教育部學產基金修復後仍重複推播）：是否核准 diff？並請於 09-24 執行完成後協助確認都發局/政治作戰局是否也出現同型態重複（見問題 24 待決重點）
5.【**新增**】問題 25（國防部政治作戰局 cnid=695 混入社會住宅個人申請導覽頁）：方案 A（補黑名單）或方案 B（移除 cnid=695）？需業務面判斷是否要保留社宅類資訊
6.【**新增**】問題 26（土地銀行來源疑似抓錯區塊，0 筆命中業務範圍）：是否核准「先移除來源，待修好再重新啟用」（建議 2）？或維持現狀待人工於有網路環境核對？
7.【延續，優先度提升，已找到雙北案件因亂碼被誤殺的具體佐證】問題 22（政府採購網 `r.encoding` 猜測失敗）：是否核准固定為 `"utf-8"`？
8.【延續】問題 1（國營台鐵地區篩選失效）：是否核准 `parse_tra()` 地點判定修法？
9.【延續，長期未拍板】問題 5（`SOURCES` 逐來源篩選死碼）：是否需要「逐來源覆蓋」能力，或直接刪除？
10.【延續，低優先度】問題 6（國有財產署 state.json 重複紀錄）
11.【延續，已用實際樣本驗證修法有效】問題 7（`log_checker.py` 編碼錯誤，`daily_report.json` 自 08-25 起 30 天全數失效）：是否核准 `r.encoding="utf-8"` 一行修法？
12.【延續】問題 9（雙北行政區名未涵蓋於 regions）：是否核准擴充清單？
13.【延續】問題 11（`qa_report.py` 連續天數計算被顯示視窗封頂）：是否核准修正 diff？
14.【延續】問題 13（`get()` 對非 2xx 回應不記 log，影響 11 處呼叫點的可觀測性）
15.【延續，累計 2 次真實重現】問題 14（跨來源重複推播，Google Alerts 內部去重）：是否核准 diff？
16.【延續，低優先度】問題 15（3 個無對應 SOURCES 的舊 state 鍵值）
17.【延續】問題 17（HURC 列表頁 fallback）
18.【**新增，前瞻性，暫不急迫**】建議 4（政府採購網高流量對 state.json 300 筆上限的潛在風險）：是否同意先觀察 1-2 週再決定是否調整上限？
