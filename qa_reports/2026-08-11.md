# 標案爬蟲監測報告 2026-08-11

## 一、狀況總覽

整體狀態：❌ 有異常（Anthropic API 組織停用已連續 **15 天**（07-28～08-11），3 個核心來源持續斷線；今日新增 1 項來源資料品質問題（土地銀行出租不動產疑似抓錯頁面區塊，31 天內快取 0% 為不動產案件）；既有已知漏洞（國營台鐵地區篩選、URC 列表頁 url、log 訊息不精確、Google Alerts 新聞網域混入）均未修復）
最近執行：2026-08-11 01:35（推播 1 筆 / 新增 11 筆 / 抓取 66 筆）

| 檢查項目 | 結果 | 說明 |
|---|---|---|
| 1. 今日已執行 | ✅ | `sent_log.json` 最新 key 為 `2026-08-11 01:35`，即今天 |
| 2. 最近一次 GitHub Actions 結論 | ✅ | run `31449495613`，2026-08-11T01:35:21Z～01:36:01Z，`workflow_dispatch` 觸發，`conclusion=success`；近 7 次執行（08-05～08-11）全部 `success` |
| 3. line_pushed 與 total_notify 一致性 | ✅ | `_summary`：`total_fetched=66`、`total_new=11`、`total_notify=1`、`line_pushed=true`，一致；job log 顯示「✅ LINE broadcast 成功（2 則）」 |
| 4. 是否所有來源 fetched=0 | ✅ | 否，本次共抓到 66 筆，12/13 來源有抓到資料 |
| 5. 個別來源連續 3 天以上 fetched=0 | ❌ | 教育部學產基金、台北市都發局、國防部政治作戰局 連續 **15 天**（07-28～08-11）`fetched=0`；今日 job log（run `31449495613`，job `93650714590`）錯誤訊息與前 14 天逐字相同：`invalid_request_error — This organization has been disabled.`（政府採購網「標租」子分類同樣走 Claude fallback 失敗，但「出租」子分類為原生 parser 不受影響，今日仍抓到 1 筆）。詳見問題 1 |
| 6. 來源抓取量暴跌/暴增 | ✅ | 政府採購網今日 `fetched=1`，較 7 天均值（2 筆）略降但未達 50% 門檻；Google Alerts 今日 `fetched=10`（昨日 0），屬 RSS 內容量日常波動，非抓取失敗（無錯誤 log），與 08-04～08-10 觀察到的正常區間（0～12）一致，未列異常 |
| 7. 應擋卻漏網的案件 | ✅ | 近 8 天（08-04～08-11）共 5 筆推播：國產署北區分署「臺北星鑽」都更房地標租案（08-04，正確）、國營台鐵新竹縣新豐鄉案（08-07，屬已知地區篩選漏洞，非新事件，見問題 2）、URC 臺北市信義區土地租賃案（08-07，正確）、Google Alerts 木柵市場攤位標租案（08-09，正確）、Google Alerts 懷愛館公用房地租賃案（08-11 新推播，正確）。逐一核對標題，無停車場／自動販賣機／工程／採購類漏網 |
| 8. 標題為分類名稱／導覽文而非案名 | ⚠️ | 延續：URC 快取（45 筆）中「會員中心」「委員名單」「加入會員」3 筆為導覽文而非案名（`_JUNK_RE` 未攔截，因不含關鍵字，但也因此不含關鍵字所以未通過白名單、未推播，屬快取污染而非推播風險）；國有財產署「地上權設定」批次標題延續前次報告。今日無新增此類推播 |
| 9. url 指向列表頁而非個別公告頁 | ❌ | URC `state.json` 全部 45 筆快取核對：23 筆（51.1%）為列表頁 `https://www.hurc.org.tw/hurc/procurement`、21 筆（46.7%）url 為空，僅 1 筆（2.2%）為個別公告頁，比例與 08-10（51.1%／46.7%）完全一致，漏洞未修復 |
| 10. date 欄位為空導致日期窗被跳過放行的項目 | ⚠️ | `state.json` 全站快取共 757 筆，457 筆（60.4%）`date` 為空，與 08-10（61.1%，746 筆）比例接近，延續性架構問題 |

## 二、異常與處理

### 問題 1：Anthropic API 組織已停用，3 個來源連續 15 天斷線（延續自 07-28，優先度最高）

- **現象**：教育部學產基金、台北市都發局、國防部政治作戰局 連續 **15 天**（07-28～08-11）`fetched=0`。今日 job log（run `31449495613`，job `93650714590`）：
  ```
  01:35:43 WARNING   [政府採購網] Claude API 錯誤：invalid_request_error — This organization has been disabled.
  01:35:46 WARNING   [教育部學產基金] Claude API 錯誤：invalid_request_error — This organization has been disabled.
  01:35:49 WARNING   [台北市都發局] Claude API 錯誤：invalid_request_error — This organization has been disabled.
  01:35:51 WARNING   [國防部政治作戰局] Claude API 錯誤：invalid_request_error — This organization has been disabled.
  ```
- **業務影響加重佐證（新發現）**：核對 `state.json` 中台北市都發局既有快取（15 天前 API 仍可用時抓到的 31 筆），內容**確為真實雙北標租案**，例如「公告標租本市南港區南港機廠社會住宅1區內4戶商業服務設施及其對應持分土地」「公告標租本市信義區六張犁社會住宅1、2區內7戶商業設施」等，完全符合業務目標（雙北公有不動產租賃）。這代表此來源並非低價值來源，而是「斷線 15 天期間若有新公告，會被完全靜默漏接、無任何 log 或告警」，風險等級高於單純的技術債。
- **定位**：`scraper.py:675-698` `parse_with_claude_fallback()`。程式碼本身連續 15 天無變更，根源為 Anthropic API 金鑰/帳務問題，須人工在 Anthropic Console 處理，非程式碼可修復範圍。
- **建議修法（非程式碼問題，需人工處理）**：
  1. 登入 Anthropic Console 確認組織停用原因並恢復，或更換新的可用 API Key，更新 GitHub repo secret `ANTHROPIC_API_KEY`。
  2. 金鑰恢復後執行 `dry_run_all_regions.py --debug --source 台北市都發局`（優先，因已證實高價值）、`教育部學產基金`、`國防部政治作戰局` 驗證原生 selector 是否仍有效，並人工檢視斷線期間（07-28～恢復日）該三來源官網是否有錯過的公告。
- **是否已修**：未修，已連續 15 天。

### 問題 2：土地銀行出租不動產疑似抓到錯誤頁面區塊，31 天內快取 0% 為不動產案件（新發現）

- **現象**：核對 `state.json` 中土地銀行出租不動產快取（共 31 筆，涵蓋近 30 天累積抓取，`fetched_sum=58、new_sum=16、notify_sum=0`），**全部 31 筆標題均與不動產租賃無關**，實際為該行 ATM／網銀系統維護公告、行銷抽獎活動等一般客服公告，例如：
  ```
  本行對外服務及自動化服務設備06/14(日)凌晨02:30~03:30及夜間22:30~23:30期間停止服務公告。
  「臺灣土地銀行八十週年行慶行銷專案『網路投保e起抽』」抽獎活動
  本行「智能客服」將於115/7/21（二）17時至20時進行系統維護作業，屆時將暫停服務
  ```
  這些標題不含 `GLOBAL_FILTER` 白名單關鍵字（出租/標租/租賃/招租…），因此目前**不會**誤推播（`notify_sum=0` 符合預期），但代表此來源自建置以來實質上從未真正抓到「出租不動產」欄目內容，`fetched` 數字是假象。
- **定位**：`scraper.py:592-617`（`parse_landbank()`）。CSS 選擇器 `table tbody tr, table tr, .bulletin-item, .list-item, article` 範圍過廣，推測抓到的是該行網站共用版型（header/footer）內的全站服務公告跑馬燈／清單元件，而非 `/Bulletin/RentRealty` 頁面真正的租賃公告表格；也可能是該頁真正內容為 JS 動態渲染，靜態 HTML 抓不到表格，Claude fallback 退而抓取頁面上其他可見文字（服務公告區塊）。
- **驗證限制（本次無法完成）**：本 session 的網路出口對 `www.landbank.com.tw` 直接被 egress proxy 阻擋（`EGRESS_BLOCKED`），`dry_run_all_regions.py --debug --source 土地銀行出租不動產` 與 WebFetch 皆無法連線驗證真實 HTML 結構，因此**未進行任何程式碼修改**（依修改規則：未經 dry_run 驗證通過不可視為完成）。
- **建議修法（待人工/CI 環境驗證後套用，暫不套用）**：
  1. 在可連外的環境（如 GitHub Actions runner 或人工瀏覽器）開啟 `https://www.landbank.com.tw/Bulletin/RentRealty`，確認租賃公告表格的實際 class/id。
  2. 將選擇器收斂為該表格專屬容器（範例，實際 class 需現場確認）：
     ```python
     # scraper.py，parse_landbank() 第 604 行，範例修法，class 名稱待人工確認後替換
     - for row in soup.select("table tbody tr, table tr, .bulletin-item, .list-item, article"):
     + for row in soup.select("#RentRealtyList table tbody tr, .rent-realty-table tr"):  # TODO: 確認實際 class/id
     ```
  3. 額外防呆：無論選擇器是否修正，建議加入來源專屬排除規則，避免服務性公告污染快取：
     ```python
     # scraper.py，parse_landbank() 第 609 行後新增
     if re.search(r"系統維護|服務暫停|停止服務|抽獎|活動|ATM|網路銀行|App", title):
         continue
     ```
- **是否已修**：未修（需人工/CI 環境驗證頁面結構後才可套用，見待決事項）。

### 問題 3～6：延續前次報告，本次核對現況與 08-10 一致，無新事證

- **問題 3（國營台鐵地區篩選漏洞）**：`scraper.py:194-196` 核對與前次報告完全相同，`NON_TAIPEI_HINTS` diff 仍未套用。此漏洞已造成 2 次實際外洩（07-28、08-07）。
- **問題 4（log 訊息「無新增案件」不精確）**：`scraper.py:1197` 未修改，今日因 `total_notify=1>0` 未觸發該分支，無新事證。
- **問題 5（URC url 指向列表頁 / 空 url）**：現象與修法同 `qa_reports/2026-08-09.md`、`2026-08-10.md` 問題 4，今日核對比例一致，未修復。
- **問題 6（Google Alerts 混入新聞媒體網域）**：`state.json` 191 筆快取中 23 筆（12.0%）含新聞媒體網域（`news.yahoo.com`、`udn.com`、`setn.com` 等），較 08-10 的 20 筆略增（快取持續累積），比例與前次報告一致，未修復。

## 三、優化建議

### 建議 1：台北自來水處持續 100% 抓到非租賃案件，建議移除或改抓正確分類頁（延續 08-10 建議，數據更新）

- **依據**：`state.json` 快取已累積至 55 筆（08-10 為 54 筆），今日新增第 55、56 筆分別為「115年雨衣雨鞋財物採購」「115年安全鞋財物採購(含後續擴充)」，逐筆核對**仍是 100% 為工程/財物/勞務採購**，無一筆不動產出租/招租案。近 31 天 `fetched_sum=51、notify_sum=0`，是唯一「fetched 量不低但推播從未發生」且非因 API 故障的來源。
- **做法**：至 `water.gov.taipei` 網站確認是否存在獨立的「非公用不動產出租」或「財產管理」公告分類（`sms=` 參數可能不同）；若確認該處近期未以此系統公告不動產出租案，建議直接移除此來源。
- **預期效果**：避免每日浪費 1 次 HTTP 請求與白名單比對在注定不會產生結果的來源上；若找到正確分類頁，則新增一個目前完全缺漏的雙北公有不動產供給管道。

### 建議 2：優先復原台北市都發局來源（見問題 1 佐證），其標租公告與業務目標高度吻合

- **依據**：見問題 1 新發現——`state.json` 中台北市都發局既有 31 筆快取，經逐筆核對，多數為「公告標租本市OO區社會住宅內OO戶商業設施」類，100% 為雙北公有不動產租賃公告，是 13 個來源中內容與業務目標**吻合度最高**的來源之一，卻因 Anthropic API 停用已連續 15 天完全斷線、0 筆抓取。
- **做法**：問題 1 修復（Anthropic API Key 復原）後，優先驗證此來源，並人工回溯官網公告頁確認 07-28～恢復日期間是否有錯過的公告（因為斷線期間沒有任何 log 記錄實際錯過了什麼）。
- **預期效果**：復原後預期可穩定產出雙北社會住宅商業設施標租案，是目前潛在漏接風險最高、恢復後效益最大的來源。

## 四、待決事項

1. **【最優先，已連續 15 天，業務影響有新事證】Anthropic API 組織已停用**（問題 1）：需人工登入 Anthropic Console 確認並恢復，或更換新的可用 API Key，並更新 GitHub repo secret `ANTHROPIC_API_KEY`。台北市都發局快取證實此來源公告與業務目標高度吻合，斷線期間可能有真實公告被完全漏接。
2. **（新）問題 2（土地銀行出租不動產疑似抓錯頁面）**：本 session 網路出口對 `landbank.com.tw` 被 egress proxy 阻擋，無法驗證真實 HTML 結構。請人工或於可連外環境執行 `dry_run_all_regions.py --debug --source 土地銀行出租不動產`，確認正確的表格選擇器後再套用報告中提出的 diff。
3. **【已造成 2 次實際外洩，diff 連續多份報告未拍板】問題 3（國營台鐵地區篩選）**：`NON_TAIPEI_HINTS` diff 是否核准套用，請儘速決定。
4. 問題 4（log 訊息文字不精確）、問題 5（URC url）、問題 6（Google Alerts 新聞網域）的修法 diff 是否核准套用。
5. 建議 1（台北自來水處疑似抓錯類別頁面）：是否核准安排人工至 `water.gov.taipei` 網站確認是否有獨立不動產出租公告分類，或直接移除此來源。
6. 國有財產署「地上權設定」類別是否納入業務範圍，需人工判斷（延續前次報告的待決事項）。
