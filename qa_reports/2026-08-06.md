# 標案爬蟲監測報告 2026-08-06

## 一、狀況總覽

整體狀態：❌ 有異常（Anthropic API 組織停用已連續 **10 天**，3 個核心來源持續斷線；問題 2～8 延續前幾日報告，均未修法，本次逐項重新核對現況數字）
最近執行：2026-08-06 01:30（推播 0 筆 / 新增 4 筆 / 抓取 59 筆）

| 檢查項目 | 結果 | 說明 |
|---|---|---|
| 1. 今日已執行 | ✅ | `sent_log.json` 最新 key 為 `2026-08-06 01:30`，即今天 |
| 2. 最近一次 GitHub Actions 結論 | ✅ | run `31062915999`，2026-08-06T01:30:15Z，`workflow_dispatch` 觸發，conclusion=`success`；近 5 次執行（08-02～08-06）全部 `success`，無失敗 |
| 3. line_pushed 與 total_notify 一致性 | ✅ | 今日 `line_pushed=false`、`total_notify=0`，一致；重新掃描近 7 天（07-31～08-06）逐日核對，0 筆矛盾（08-03、08-04 為 `notify=1`/`line_pushed=true`，其餘 5 天皆為 0/false，全部對應正確） |
| 4. 是否所有來源 fetched=0 | ✅ | 否，本次共抓到 59 筆 |
| 5. 個別來源連續 3 天以上 fetched=0 | ❌ | 教育部學產基金、台北市都發局、國防部政治作戰局 連續 **10 天**（07-28～08-06）`fetched=0`（詳見問題 1） |
| 6. 來源抓取量暴跌/暴增 | ⚠️ | 國營台鐵今日（08-06）`fetched=0`，已是連續第 **2 天**（08-05 起，前 5 天皆穩定 1 筆），尚未達 3 天門檻但降幅達 100%，已列入下次觀察重點；其餘來源大致穩定（財政局 13、瑠公管理處 15、郵局 8→10）；Google Alerts 本週波動大（8/1/5/1/12/1 筆）屬 RSS 來源自然波動 |
| 7. 應擋卻漏網的案件 | ✅ | 今日新增 4 筆（郵局 1、土地銀行 2、Google Alerts 1）逐一核對：郵局新增為台南市佳里區郵局案件，因標題/機關不含雙北關鍵字已被 `passes_filters()` 正確擋下（`notify=0`，非漏網）；土地銀行 2 筆為站內公用公告（見問題 7）；Google Alerts 1 筆為合法農田水利標租公告。本次未發現應擋卻漏網的案件 |
| 8. 標題為分類名稱／導覽文而非案名 | ⚠️ | 國有財產署推播標題延續為 parser 合成字串（來源結構限制，非程式錯誤，延續前幾日報告） |
| 9. url 指向列表頁而非個別公告頁 | ⚠️ | `state.json` 快取中仍有大量 url 為空的舊紀錄（延續，見二） |
| 10. date 欄位為空導致日期窗被跳過放行 | ⚠️ | `state.json` 快取中仍有大量 `date` 為空的舊紀錄（延續，見二） |

---

## 二、異常與處理

### 問題 1：Anthropic API 組織已停用，3 個來源連續 10 天斷線（延續自 07-28，優先度最高）

- **現象**：教育部學產基金、台北市都發局、國防部政治作戰局 3 個來源自 07-28 起連續 **10 天** `fetched=0`（逐日核對 `sent_log.json` 07-28～08-06 確認）。07-27 之前這 3 個來源原生 parser 穩定產出 2/30/11 筆，07-28 同一天全部歸零，且 `daily_report.json` 同日起連續出現：
  ```
  [教育部學產基金] Claude API 錯誤：invalid_request_error — This organization has been disabled.
  [台北市都發局] Claude API 錯誤：invalid_request_error — This organization has been disabled.
  [國防部政治作戰局] Claude API 錯誤：invalid_request_error — This organization has been disabled.
  ```
  （政府採購網同日也曾出現同一錯誤，但其原生 PCC 頁面解析不依賴 Claude，抓取量當天反而由 2 筆升至 4 筆，08-03 起錯誤已不再出現——僅此來源已自行恢復。）
- **定位**：`scraper.py:675-698` `parse_with_claude_fallback()`。這 3 個 parser 的原生 `table tbody tr` selector 目前持續回傳 0 筆，觸發 Claude fallback，而 Claude API 本身因組織停用持續失敗，故 100% 變成 0 筆。`git log --oneline -- scraper.py` 確認最後一次程式碼異動是 07-24，07-27→07-28 之間無程式碼變更，排除「改碼導致失效」，問題根源在 API 金鑰／帳務，而非程式或網路。
- **建議修法（非程式碼問題，需人工處理）**：
  1. 登入 [Anthropic Console](https://console.anthropic.com) 確認組織停用原因（帳務/合規）並恢復，或更換為新的可用 API Key，更新 GitHub repo secret `ANTHROPIC_API_KEY`。
  2. 金鑰恢復後，執行 `dry_run_all_regions.py --debug --source 教育部學產基金`（依序 3 個來源）驗證原生 selector 是否仍然有效。
  3. 中期治本：強化這 3 個來源的原生 selector 容錯，降低對 Claude fallback 的依賴，避免「Claude 一斷、來源全滅」的單點故障重演。
- **是否已修**：未修，已連續 10 天，累計影響約 43 筆潛在案件（依 07-27 前每日平均 2+30+11=43 筆估算 × 10 天，實際遺漏數無法回溯確認）。

### 問題 2：國營台鐵地區篩選漏洞（延續，尚未修法）

- **現象**：`parse_tra()` 只依「業管單位」欄位是否含「臺北營業分處」篩選，但轄區橫跨全台，`passes_filters()` 用 `text = title + agency`（`scraper.py:800`），`agency` 恆含「臺北」二字，region 篩選對此來源形同虛設。今日重新核對 `state.json`，「臺北營業分處」名下仍有 4 筆非雙北標的：新竹縣竹東鎮、桃園市楊梅區、新竹市香山區、新竹縣新豐鄉，其中新竹縣新豐鄉一案已於 07-28 實際推播給使用者。
- **定位**：`scraper.py` `parse_tra()` 第 194-196 行；`passes_filters()` 第 796-814 行。
- **建議修法**：
  ```python
  # scraper.py，parse_tra() 內，「只保留臺北營業分處的標案」之後加一段
  NON_TAIPEI_HINTS = ["新竹市", "新竹縣", "桃園", "苗栗", "台中", "臺中", "彰化",
                       "南投", "雲林", "嘉義", "台南", "臺南", "高雄", "屏東",
                       "宜蘭", "花蓮", "台東", "臺東", "基隆"]
  items = [i for i in items if "臺北營業分處" in i.get("agency", "") + i.get("title", "")]
  items = [i for i in items if not any(h in i["title"] for h in NON_TAIPEI_HINTS)]  # 新增
  log.info(f"  [國營台鐵] {len(items)} 筆（臺北營業分處）")
  ```
- **是否已修**：未修（完整 diff 已於 07-31～08-05 報告連續提出，尚待人工核准套用）。

### 問題 3：Google Alerts 混入房產新聞報導，非官方公告（延續）

- **現象**：31 天 sent_log 掃描到至少 2 筆推播為新聞媒體報導而非官方標案公告（07-26、08-03，詳見 08-04 報告），本週（08-05、08-06）無新增此類案例。
- **定位**：`parse_google_alerts()`（`scraper.py:627-663`）未對來源網域做任何過濾。
- **建議修法**：完整 diff 見 `qa_reports/2026-08-04.md`（`NEWS_DOMAIN_BLACKLIST` 過濾常見新聞媒體網域）。
- **是否已修**：未修。

### 問題 4：`_is_junk()` 防呆函式定義後從未被呼叫（死代碼，延續）

- **現象**：`scraper.py:113-114` 定義了 `_is_junk(item)`，但 `main()`（第 1142-1157 行）從未呼叫它。今日再次以 `grep -n "_is_junk("` 全檔掃描確認僅 1 處（定義本身）。
- **建議修法**：`notify_items` 篩選條件加入 `and not _is_junk(i)`（完整 diff 見 08-04 報告）。
- **是否已修**：未修。

### 問題 5：`SOURCES` 內各來源自訂 `whitelist`/`blacklist`/`regions` 為死代碼（延續）

- **現象**：`SOURCES`（`scraper.py:703-793`）多個來源定義了自訂 `whitelist`/`blacklist`/`regions`，但 `passes_filters(item)` 簽名只吃 `item`，完全沒有讀取這些欄位。今日再次核對 `main()` 呼叫處（`scraper.py:1151`）`passes_filters(i)` 未傳入 `src`，確認現況未變。
- **建議修法**：讓 `passes_filters` 接受來源層級覆寫（完整 diff 見 08-04 報告）。需先決定「來源 `regions`/`whitelist` 為空列表」的語意（見待決事項）。
- **是否已修**：未修。

### 問題 6：國有財產署「停車場」類別繞過黑名單（`skip_global_filter=True`，延續）

- **現象**：`scraper.py:744-745` 該來源整體設為 `skip_global_filter=True`，理由是標題為組合字串已在 parser 篩北區分署，但這也連帶讓黑名單（含「停車場」等關鍵字）失效。目前該類別持續 `fetched=0`，尚未實際誤推，風險為理論性。
- **建議修法**：暫不變更（風險未實現，且此設計有其理由——parser 已在來源層做地區篩選）；若日後該類別開始有資料，應改為僅 `skip_global_filter` region 判斷，保留 blacklist 判斷。
- **是否已修**：不適用（觀察中）。

### 問題 7：土地銀行出租不動產抓到站內公用公告，非租賃清單（延續，今日新增具體案例）

- **現象**：`parse_landbank()`（`scraper.py:592-618`）selector 為 `"table tbody tr, table tr, .bulletin-item, .list-item, article"`，範圍過寬，命中站內共用公告區塊而非 `/Bulletin/RentRealty` 實際租賃清單容器。今日新增 2 筆 `new` 項目，人工核對標題皆為與租賃無關的站內公告：
  - 「本行『網路銀行』之信用卡及基金交易...將於8/9（日）凌晨1時至5時暫停服務公告」
  - 「內政部辦理之政策性貸款加碼補貼於115年7月31日截止...」
  這 2 筆已正確被 `GLOBAL_FILTER` whitelist 擋下未推播，但持續污染 `state.json`（近 7 天 `new=7` 筆，`notify=0`）。
- **定位**：`scraper.py:604`。
- **建議修法**：
  ```python
  # scraper.py:604，收窄 selector，優先鎖定 RentRealty 清單容器
  for row in soup.select("#RentRealty table tbody tr, .rent-list tbody tr, table tbody tr"):
      ...
  ```
  （確切容器 class/id 需連上 `https://www.landbank.com.tw/Bulletin/RentRealty` 實際頁面核對，本沙盒環境對外連線持續被 proxy 回傳 403，需在有網路權限環境執行。）
- **是否已修**：未修。

### 問題 8：台北自來水處抓到一般採購公告頁，非「出租/招租」專屬頁面（延續）

- **現象**：本次核對近 7 天 `fetched` 穩定 1~3 筆、`new=2`、`notify=0`，內容型態與前次診斷一致（抓到一般公告頁而非專屬租賃頁面）。
- **建議修法**：詳見 08-04 報告。
- **是否已修**：未修。

---

## 三、優化建議

### 建議 1（延續最高優先）：解決問題 1 後，補強 3 個 Claude-only 來源的原生 parser 容錯

- 依據：07-27 之前這 3 個來源原生 parser 穩定產出 2/30/11 筆，證明平時不需要 Claude 也能運作；Claude 中斷後已連續 10 天全滅，代表目前架構「Claude 一斷、來源全滅」是單點故障。
- 做法：問題 1 解決後，重新確認原生 selector 是否仍有效並強化容錯。
- 預期效果：未來 Claude API 若再中斷，不會讓 3 個來源同時斷線超過 1 天。

### 建議 2（修正前次判斷）：農業部瑠公管理處「零波動」經核對可能為真實刊登頻率低，非 parser 卡死，建議改用「距上次刊登天數」監測取代單純連續天數

- 依據：`sent_log.json` 完整 31 天歷史中，瑠公管理處 `fetched` 每天精準為 **15**，`new` 每天為 **0**，無一例外。但今日進一步核對 `state.json` 中這 15 筆的實際日期分布為 **2022-12-29 ～ 2026-07-04**（涵蓋 3 年半的歷史公告與開標結果，非同一批固定文字），且最新一批「115年度第5批」公告日期為 07-04，早於本監測起始日 07-07。這代表 parser 抓到的是真實的分頁清單（非快取卡死的固定頁面），只是該機關最近一次刊登（07-04）剛好落在監測窗口開始之前，此後未再刊登新案，屬於低頻刊登機關的正常現象，而非程式錯誤。
- 做法：撤回前次報告「疑似 parser 卡在快取頁」的疑慮；改為在 `qa_report.py` 中加入「距上次刊登天數」指標（用 state.json 中最新一筆的 `date` 與今天相減），若超過 60 天無新公告才真正示警，避免用「連續 N 天 fetched 不變」誤判低頻但正常運作的來源。
- 預期效果：減少誤判為異常的巡檢雜訊，讓真正的 parser 故障（如問題 1）更容易被注意到。

### 建議 3（新發現）：本週（近 7 天）整站僅推播 2 筆，13 個來源中 12 個 notify=0，但抽查未發現因篩選過嚴造成的漏網案件

- 依據：`sent_log.json` 近 7 天彙總，僅 Google Alerts 有 2 筆 `notify`，其餘 12 個來源合計抓取約 344 筆、7 筆標記為 `new`，全部 `notify=0`。逐筆核對今日新增的 4 筆 `new` 項目（郵局台南案件、土地銀行 2 筆站內公告、Google Alerts 1 筆合法公告），前 3 筆被 `GLOBAL_FILTER` 正確擋下（地區篩選或本非租賃內容），並非誤殺；`--non-taipei` 反向核對因本沙盒環境對外連線被 proxy 403 阻擋，本次無法在有網路環境執行完整交叉比對。
- 做法：本週篩選機制運作方向正確（擋掉的確實是非目標案件），暫不建議放寬 whitelist/regions；但問題 7（土地銀行 selector 過寬）造成大量非租賃噪音持續污染 `state.json` 與 `new` 計數，仍應優先修正，以免長期累積影響 dedup 效能與後續人工判讀。
- 預期效果：確認目前低推播量的根本原因是「近期真的沒有新公告 + 問題 1 斷線」，而非篩選過嚴，避免誤放寬篩選條件反而引入雜訊。

### 方向 C：新增來源

本次持續略過。問題 1（Anthropic API，已達 10 天未解，影響範圍最大）與問題 2～8 均未修復，在既有問題解決並觀察至少一週成效之前，新增未驗證來源的邊際效益低於修復既有問題（與 08-04、08-05 報告判斷一致）。

---

## 四、待決事項

1. **【最優先，已連續 10 天】Anthropic API 組織已停用**（問題 1）：需人工登入 Anthropic Console 確認並恢復，或更換新的可用 API Key，並更新 GitHub repo secret `ANTHROPIC_API_KEY`。
2. 問題 2～8 的修法 diff（詳見 `qa_reports/2026-08-04.md`、`2026-08-05.md` 與本報告）是否核准套用——已連續多日提出但尚未有人拍板。
3. 問題 7（土地銀行 selector 收窄）、建議 2（瑠公管理處指標調整）需要有網路存取權限的環境（GitHub Actions runner 或本機）連上實際網站確認目前 HTML 結構，本沙盒環境對外連線（含 WebFetch）持續被 proxy 回傳 403 阻擋，無法即時比對。
4. 問題 5（`SOURCES` 死代碼欄位）處理方向：(a) 直接移除從未生效的欄位，或 (b) 讓 `passes_filters` 真正套用來源層級覆寫（需先決定空列表的語意）。
5. 國有財產署「地上權設定」類別是否納入業務範圍，需人工判斷。
6. 國營台鐵今日起連續 2 天 `fetched=0`（08-05、08-06），尚未達 3 天示警門檻，建議明日持續觀察；若第 3 天仍為 0，應比照問題 1/5 處理流程定位原因。
