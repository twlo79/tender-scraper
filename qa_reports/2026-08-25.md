# 標案爬蟲監測報告 2026-08-25

## 一、狀況總覽

整體狀態：⚠️ 有注意事項（今日執行本身正常、無新增外洩推播；但「國有財產署」「政府採購網」兩來源今日首次 `fetched=0`，原因待查；另有 8 項已知精準度缺口延續未修，含 1 項已造成過真實外洩、`log-check.yml` 權限缺口已進入第 3 天）
最近執行：2026-08-25 01:30（推播 0 筆 / 新增 2 筆 / 抓取 95 筆）

| 檢查項目 | 結果 | 說明 |
|---|---|---|
| 1. 今日已執行 | ✅ | `sent_log.json` 最新 key 為 `2026-08-25 01:30`，即今天 |
| 2. 最近一次 GitHub Actions 結論 | ✅ | `daily.yml` run `32797792860`，2026-08-25T01:30:10Z，`conclusion=success`；近 7 次 run（08-19～08-25）全數 success |
| 3. line_pushed 與 total_notify 一致性 | ✅ | `_summary`：`total_fetched=95`、`total_new=2`、`total_notify=0`、`line_pushed=false`，一致；近 7 天逐日核對（08-19～08-25）皆一致，無「有案未推」或「無案卻推」 |
| 4. 是否所有來源 fetched=0 | ✅ | 否，13 來源中 11 個抓到資料，僅「國有財產署」「政府採購網」為 0（合計 95 筆） |
| 5. 個別來源連續 3 天以上 fetched=0 | ✅ | 「國有財產署」「政府採購網」皆為今日首次歸零（前 6 天分別穩定在 2 筆、2～4 筆），尚未達 3 天門檻，但列入下方問題持續觀察 |
| 6. 來源抓取量暴跌/暴增 | ⚠️ | 「國有財產署」今日 2→0（跌 100%）、「政府採購網」今日 2→0（跌 100%），為近 7 天唯二單日暴跌案例；Google Alerts 08/21→08/22 由 5 筆降至 1 筆屬固有信件量波動（與前次報告判斷一致，非爬取異常），其餘來源近 7 天皆平穩 |
| 7. 應擋卻漏網的案件 | ⚠️ | 今日無新推播，故無新增外洩；但近 7 天內仍有 08-24 已推播的「廣告標租」誤報 1 筆未撤回（見問題 1，屬延續）。另用 `passes_filters()` 對 `state.json` 全庫 802 筆重放，確認目前仍有 **6 筆停車類、5 筆廣告標租類、1 筆決標結果類**尚未被黑名單擋下，數字與 08-24 報告完全一致（未惡化亦未改善） |
| 8. 標題為分類名稱／導覽文而非案名 | ⚠️ | `state.json`「國家住宅及都市更新中心」仍有「會員中心」「委員名單」「加入會員」3 筆導覽文快取（與前次持平），比對 `sent_log.json` 從未被實際推播，僅為快取雜訊 |
| 9. url 指向列表頁而非個別公告頁 | ❌ | 全庫 802 筆中 463 筆（57.7%）url 為空，與前次 57.9% 持平；政府採購網 95.3%（286/300）、台北市都發局 100%（33/33）、國防部政治作戰局 100%（11/11）、教育部學產基金 100%（4/4）、台北自來水處 74.1%（43/58） |
| 10. date 欄位為空 | ❌ | 全庫 802 筆中 463 筆（57.7%）date 為空，與前次 57.8% 持平；台北自來水處 100%（58/58）、土地銀行 100%（37/37）、政府採購網 95.3%（286/300） |

## 二、異常與處理

### 問題 1（新發現）：「國有財產署」「政府採購網」今日 fetched=0，且 `get()` 無法從 Actions log 判斷根因

- **現象**：`sent_log.json` 顯示兩來源今日皆為 `fetched=0`，為近 7 天首見（國有財產署前 6 天穩定 2 筆／天，政府採購網前 6 天 2～4 筆／天）。調閱今日 `daily.yml` run `32797792860` 的完整 log：兩來源皆**沒有**出現 `GET 失敗`（連線例外）警告，`parse_fnp()` 的 4 個子類別（招標公告／地上權設定／停車場／創業租賃）與 `_pcc_fetch_keyword()` 的 2 個關鍵字（出租／標租）皆是「請求似乎成功但解析出 0 筆」，而非「連線例外」。
  這代表根因可能是：(a) 目標網站 HTML 結構改版、CSS selector 失效，(b) 網站回傳非預期內容（維護頁／防爬蟲頁）但仍是 HTTP 200，(c) 純粹當天剛好兩來源同時無公告（機率低，兩來源業務性質不同卻同日歸零較不尋常）。
  **本 QA 執行環境對 `esvc.fnp.gov.tw`、`web.pcc.gov.tw`、甚至 `google.com` 出口一律回傳 `EGRESS_BLOCKED`**（已用 WebFetch 與直接 `requests` 雙重驗證），故本次**無法在此環境即時開啟頁面核對 HTML 結構是否改版**，與 08-24 報告記錄的「政府採購網」根因調查遇到相同環境限制。
- **定位**：`scraper.py:99-106`（`get()`，未記錄 HTTP 狀態碼）；`scraper.py:340-388`（`parse_fnp()`）；`scraper.py:390`（`_pcc_fetch_keyword()`）。
- **建議修法**（先加強診斷能力，讓下次歸零時能直接從 Actions log 判斷根因，不需依賴有網路權限的環境）：
  ```diff
   def get(url, **kwargs) -> requests.Response | None:
       try:
           r = requests.get(url, headers=HTTP_HEADERS, timeout=20, **kwargs)
           r.encoding = r.apparent_encoding or "utf-8"
  +        if r.status_code != 200:
  +            log.warning(f"  非 200 回應 {url}：HTTP {r.status_code}")
           return r
       except Exception as e:
           log.warning(f"GET 失敗 {url}：{e}")
           return None
  ```
  並在 `parse_fnp()` 迴圈內、`soup.select("a.message-flex")` 結果為空時補一行診斷 log（例如記錄 `len(r.text)` 或是否含有已知的 selector class 字串），`_pcc_fetch_keyword()` 同理。
- **是否已修**：未修（預設不自動改動程式碼；此為診斷用最小 diff，供人工套用後於明天執行結果驗證）。若明日兩來源仍為 0 且達 3 天門檻，應提升為 ❌ 並優先處理。

### 問題 2（延續，08-24 新發現，連續第 2 天未修）：GLOBAL_FILTER 黑名單缺「廣告標租」類非不動產案件

- **現象**：08-24 已有 1 筆國營台鐵「廣告標租」案件（車站牆面廣告版位）真實推播至 LINE。今日用 `passes_filters()` 重放 `state.json` 全庫，同型案件仍有 **5 筆**（皆「臺北車站外東側機房牆面」或「臺北站B1層剪票口外牆面」廣告版位），數字與昨日持平，尚未再擴大但也未修復。
- **定位**：`scraper.py:69-75`（`GLOBAL_FILTER["blacklist"]`）。
- **建議修法**：
  ```diff
       "blacklist": [
           "開標結果",
           "自動販賣機", "場地短期出租", "機械類標租", "共享雨傘",
  +        "廣告標租", "牆面廣告", "廣告版位",
           "新建工程", "統包工程", "物業管理", "專案管理", "保險",
           "清洗作業", "鑄鐵直管", "延性鑄鐵", "塗裝", "管線",
           "財物採購", "勞務採購",
       ],
  ```
- **是否已修**：未修。

### 問題 3（延續，08-20 曾造成真實外洩，連續第 6 天未修）：GLOBAL_FILTER 黑名單缺「停車場」類關鍵字

- **現象**：今日重放 `state.json` 全庫 802 筆，仍有 **6 筆**含「停車」會通過現有篩選：國營台鐵 2 筆（臺北車站東西區地下暨西側地上停車場經營遴選案、新北市鶯歌區中正段停車場）、台北市財政局 3 筆（內湖區單一停車位、板橋區臨時路外停車場、板橋區臨時平面停車場）、郵局房地產出租 1 筆（臺北市松山區店面及4個停車位）。數字與 08-24 報告完全一致。
- **定位**：`scraper.py:69-75`。
- **建議修法**：
  ```diff
       "blacklist": [
           "開標結果",
  -        "自動販賣機", "場地短期出租", "機械類標租", "共享雨傘",
  +        "自動販賣機", "場地短期出租", "機械類標租", "共享雨傘",
  +        "停車場", "停車位", "停車格", "機械式停車",
           "新建工程", "統包工程", "物業管理", "專案管理", "保險",
           "清洗作業", "鑄鐵直管", "延性鑄鐵", "塗裝", "管線",
           "財物採購", "勞務採購",
       ],
  ```
- **是否已修**：未修。**已連續 6 份報告（08-20～08-25）標記為最高優先。**

### 問題 4（延續，08-18 曾造成真實外洩，連續第 6 天未修）：國營台鐵「臺北營業分處」地區篩選誤判

- **現象**：`scraper.py:194` 的 `parse_tra()` 僅以 `"臺北營業分處" in agency+title` 篩選，未核對案件實際座落縣市，轄區橫跨新竹、花蓮等地。因 `state.json` 未保存 `agency` 欄位，本次無法直接重放核對外縣市筆數是否變動；根因程式碼本身未變。
- **定位**：`scraper.py:193-195`。
- **建議修法**：
  ```diff
       a = item.select_one("div.btn-box a[href]")
       href = urljoin(BASE, a["href"]) if a else URL
       items.append({"title": title, "date": dt, "url": href, "agency": unit})
  -    # 只保留臺北營業分處的標案
  -    items = [i for i in items if "臺北營業分處" in i.get("agency", "") + i.get("title", "")]
  +    # 只保留臺北營業分處管轄「且」案件座落雙北的標案
  +    NON_TAIPEI_COUNTIES = ["新竹縣", "新竹市", "苗栗", "宜蘭", "花蓮", "台中", "臺中", "台東", "臺東"]
  +    items = [
  +        i for i in items
  +        if "臺北營業分處" in i.get("agency", "") + i.get("title", "")
  +        and not any(c in i.get("title", "") for c in NON_TAIPEI_COUNTIES)
  +    ]
       log.info(f"  [國營台鐵] {len(items)} 筆（臺北營業分處）")
       return items
  ```
- **是否已修**：未修。

### 問題 5（延續，連續第 6 天未修）：GLOBAL_FILTER 黑名單未涵蓋「決標結果」

- **現象**：重放確認 Google Alerts 快取仍有 1 筆（「公告『新北市萬里區設置社區式長照機構』公開標租之決標結果一案」）會通過篩選，與前次持平。
- **建議修法**：
  ```diff
       "blacklist": [
  -        "開標結果",
  +        "開標結果", "決標結果",
           ...
       ],
  ```
- **是否已修**：未修。

### 問題 6（延續，連續第 3 天未修）：`log-check.yml` 缺 `actions: read` 權限，`daily_report.json` 已失效超過 30 天

- **現象**：`daily_report.json` 最新條目仍停在 2026-08-24，`sources={}`、`total_new=0`、`total_pushed=0`，但同日 `sent_log.json` 實際為 `total_new=3, total_notify=1`，明顯不符。調閱 `log-check.yml` 今日執行紀錄：截至報告產生時間（03:35 UTC，cron 為 UTC 03:07），今日尚未觸發（比對近 7 天實際觸發時間皆落在 UTC 03:52～04:01，與 cron 時間有 45～55 分鐘排程延遲屬歷史常態，非異常）；最近一次成功執行仍是 08-24T04:01:24Z run `32688473728`（`conclusion=success` 但寫入空值），根因未變：`permissions:` 區塊仍缺 `actions: read`。
- **定位**：`.github/workflows/log-check.yml`；次要定位 `log_checker.py:23-29`（`get_latest_log()` 缺 HTTP 狀態碼檢查）。
- **建議修法**：
  ```diff
   permissions:
     contents: write
  +  actions: read
  ```
  ```diff
       r = requests.get(
           f"https://api.github.com/repos/{GITHUB_REPO}/actions/workflows/daily.yml/runs?per_page=2",
           headers=GH, timeout=20,
       )
  +    if r.status_code != 200:
  +        print(f"❌ 無法取得 workflow runs：HTTP {r.status_code} {r.text[:200]}")
  +        return ""
       runs = r.json().get("workflow_runs", [])
  ```
- **是否已修**：未修。套用後需人工手動 `workflow_dispatch` 觸發一次 `log-check.yml` 驗證。

### 問題 7（延續）：`SOURCES` 中各來源自訂的 `whitelist`/`blacklist`/`regions` 從未生效

- **現象**：`passes_filters(item)`（`scraper.py:796`）僅讀取 `GLOBAL_FILTER`，呼叫處 `scraper.py:1151` 也只傳入 `i`，`SOURCES` 內逐來源設定（如 Google Alerts 設定的 `"regions": ["台北","臺北","新北","gov.tw"]`，見 `scraper.py:726`）仍是死碼，狀態未變。
- **建議修法**：
  ```diff
  -def passes_filters(item: dict) -> bool:
  +def passes_filters(item: dict, src: dict | None = None) -> bool:
       title  = item.get("title", "")
       agency = item.get("agency", "")
       text   = title + agency
  +    src = src or {}

  -    regions = GLOBAL_FILTER.get("regions", [])
  +    regions = GLOBAL_FILTER.get("regions", []) + src.get("regions", [])
       if regions and not any(k in text for k in regions):
           return False

  -    whitelist = GLOBAL_FILTER.get("whitelist", [])
  +    whitelist = GLOBAL_FILTER.get("whitelist", []) + src.get("whitelist", [])
       if whitelist and not any(k in title for k in whitelist):
           return False

       blacklist = GLOBAL_FILTER.get("blacklist", [])
       if blacklist and any(k in title for k in blacklist):
           return False

       return True
  ```
  並將 `scraper.py:1151` 由 `passes_filters(i)` 改為 `passes_filters(i, src)`。
- **是否已修**：未修。

### 問題 8（延續）：url/date 欄位大量空白，「政府採購網」疑似 parser 失效

- **現象**：見狀況總覽第 9、10 項，全庫 57.7%／57.7% 欄位空白，與前次持平。「政府採購網」url/date 空白率仍達 95.3%（286/300），且今日確認該來源 `_pcc_fetch_keyword()` 已連續多日抓不到目標表格（見問題 1），高度符合「目標表格 selector 已失效、長期落入 Claude fallback 從整頁雜訊文字亂抓標題」的特徵。因本環境網路出口封鎖 `web.pcc.gov.tw` 且無法即時開啟頁面核對，此仍為根因推斷而非已確認結論。
- **是否已修**：未修。

### 問題 9（延續）：土地銀行出租不動產／台北自來水處 date 欄位結構性空白

- **現象**：近 7 天（08/19～08/25）土地銀行 `fetched_sum=14, notify_sum=0`（date 100% 空白）；台北自來水處 `fetched_sum=22, notify_sum=0`（date 100% 空白）。日期窗機制對這兩來源完全失效，狀態未變。
- **是否已修**：未修。

## 三、優化建議

### 建議 1（方向 A）：問題 2～5、7 的 diff 已連續多天待拍板，建議一次套用降低外洩風險

- **依據**：`passes_filters()` 對 `state.json` 全庫 802 筆重放，確認目前仍有 12 筆會通過現有篩選但屬非目標案件（廣告標租 5、停車類 6、決標結果 1），且 08-24 已有 1 筆廣告標租真實外洩。這些 diff 彼此不衝突，可一次套用。
- **做法**：套用「二、異常與處理」問題 2、3、4、5、7 的 diff。
- **預期效果**：排除已確認的 4 類誤報型態（廣告版位、停車、決標結果、地區誤判），並讓 `SOURCES` 逐來源篩選設定真正生效。

### 建議 2（方向 A）：Google Alerts 今日新增項目顯示「地區篩選可能誤殺無地名標題的雙北案件」，建議擴大比對欄位

- **依據**：今日 Google Alerts 新增 1 筆「115年第4批農田水利事業作業基金非事業用不動產土地公開標租案」，標題未含「台北」「臺北」「新北」等地名，因而被 `GLOBAL_FILTER["regions"]` 擋下（notify=0）。此類案件是否確實位於雙北，因本環境對 `iatyu.nat.gov.tw` 出口被封鎖，**本次無法即時開啟原始網頁核實**，故列為待人工確認的疑似漏網案例，而非確定結論。
- **做法**：建議人工開啟該公告網址（`https://www.iatyu.nat.gov.tw/news/Articles?a=10153&id=29542`）核實案件座落地點；若確為雙北案件，建議評估針對此類「無地名但屬公有不動產標租」的標題，改用更寬鬆的地區判斷（例如僅在標題完全未提及任何縣市地名時，改看發布機關轄區是否為雙北），避免因公告單位慣用行政區以外用語（如流域、水利會轄區名）而誤殺。
- **預期效果**：減少因標題未寫地名而被地區篩選誤殺的雙北案件；需先由人工核實方向是否成立，避免反而放寬到大量外縣市案件。

### 建議 3（方向 B）：11/13 來源近 7 天 notify_sum=0，需區分「無新案」與「結構性失效」分別處理

- **依據**：近 7 天各來源 notify_sum：國營台鐵 3、Google Alerts 1，其餘 11 個來源（台北自來水處、農業部瑠公管理處、郵局房地產出租、台北市財政局、國家住宅及都市更新中心、國有財產署、政府採購網、教育部學產基金、台北市都發局、國防部政治作戰局、土地銀行）皆為 0。其中台北市財政局（fetched_sum=73）、台北市都發局（210）、國防部政治作戰局（77）抽查快取標題確實屬雙北不動產標租範疇，判斷為「近期剛好無新案」；但台北自來水處與土地銀行 date 100% 空白（問題 9）、政府採購網疑似 parser 失效（問題 8），屬「結構性失效」，即使有新案也可能因日期窗機制失效而漏推或誤判。
- **做法**：問題 8、9 建議儘速安排人工在有網路權限環境開啟原始頁面，核對 HTML 結構並修正 date/url 解析；若短期內無法修復，土地銀行／自來水處兩來源建議先二選一：移除或改抓正確分類頁。
- **預期效果**：把「無新案」與「來源已經失效但看起來像沒新案」區分開，避免長期靜默漏抓真正的雙北租賃案件。

### 建議 4（方向 C，新增來源候選，暫列次要優先）

因問題 2～9 尚有多項未拍板的精準度缺口，以下候選建議待前述缺口修復後再評估：

1. **台灣電力公司資產管理（台電不動產標租）**：`https://www.taipower.com.tw` 內「不動產暨設備標售標租」專區，公告以靜態 HTML table 呈現（编号／名稱／地點／公告日期／截止日期欄位），實作難度低（與現有台鐵/財政局 table parser 模式相同），涵蓋雙北變電所閒置用地與員工宿舍等標租案。
2. **台北市地政局市有非公用不動產**：與現有「台北市財政局」互補，公告頁多為靜態 table，實作難度低至中，但需注意與財政局來源可能重疊，須做跨來源去重。

## 四、待決事項

1. **【連續 6 份報告未拍板，08-20 已造成真實外洩】問題 3（GLOBAL_FILTER 黑名單缺「停車場」類關鍵字）**：diff 是否核准套用？最高優先。
2. **【連續 6 份報告未拍板，08-18 曾造成真實外洩】問題 4（國營台鐵地區篩選誤判）**：diff 是否核准套用？
3. **【連續第 2 天，08-24 已造成真實外洩】問題 2（GLOBAL_FILTER 黑名單缺「廣告標租」類）**：diff 是否核准套用？
4. **【連續多天未拍板】問題 5（GLOBAL_FILTER 黑名單缺「決標結果」）**：diff 是否核准套用？
5. **【連續第 3 天】問題 6（`log-check.yml` 缺少 `actions: read` 權限，`daily_report.json` 已失效 30 天以上）**：diff 是否核准套用？套用後需人工手動觸發 `log-check.yml` 驗證。
6. **【延續未拍板】問題 7（`SOURCES` 逐來源篩選設定從未生效）**：diff 是否核准套用？
7. **【新增，本次無法在此環境驗證】問題 1：「國有財產署」「政府採購網」今日首次 fetched=0**：是否核准套用診斷用 `get()` 狀態碼記錄 diff？並請安排人工在有網路權限環境開啟 `esvc.fnp.gov.tw`、`web.pcc.gov.tw` 核對 HTML 結構是否改版。若明日仍為 0（達 3 天門檻）應提升為 ❌ 優先處理。
8. **【延續】問題 8：「政府採購網」`_pcc_fetch_keyword()` 目標表格 selector 疑似失效**：是否安排人工在有網路權限環境核對？
9. **【延續】問題 9（url/date 欄位大量空白）**：是否安排人工重新核對各站 HTML 結構並修正 parser？
10. **【延續】土地銀行/自來水處來源是否移除**，或安排人工確認正確的不動產出租分類頁？請明確二選一。
11. **【延續】國有財產署「地上權設定」類別是否納入業務範圍**，需人工判斷。
12. **【延續】「攤位標租」類案件（公有零售市場攤位）是否納入業務範圍**，仍待人工確認（目前 `state.json` 中 Google Alerts 快取有 11 筆同類案件會通過篩選）。
13. **【延續】本 QA routine 執行環境的網路出口政策是否調整**：本次已再度確認 `esvc.fnp.gov.tw`、`web.pcc.gov.tw`、`iatyu.nat.gov.tw`、甚至 `google.com` 皆為 `EGRESS_BLOCKED`，導致「階段二」除錯完全無法做即時 HTML 驗證，只能依賴 GitHub Actions log 與程式碼靜態分析。
14. **【新增】方向 C 提出的 2 個新來源候選（台電不動產標租、台北市地政局市有非公用不動產）是否核准評估／排入實作？**
15. **【新增】建議 2 提出的 Google Alerts「115年第4批農田水利事業作業基金非事業用不動產土地公開標租案」是否為雙北案件**：請人工開啟原始網址核實，判斷地區篩選是否誤殺。
