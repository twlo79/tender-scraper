# 標案爬蟲監測報告 2026-08-01

## 一、狀況總覽

整體狀態：❌ 有異常（延續前 4 日未解決的問題，另新增 1 項資料品質 bug）
最近執行：2026-08-01 01:30（UTC，GitHub Actions run [30678046768](https://github.com/twlo79/tender-scraper/actions/runs/30678046768) success）（推播 0 筆 / 新增 9 筆 / 抓取 62 筆）

| 檢查項目 | 結果 | 說明 |
|---|---|---|
| 1. 今日已執行 | ✅ | `sent_log.json` 有 `2026-08-01 01:30` 記錄，對應 Actions run 於 `2026-08-01T01:30:15Z` 觸發（workflow_dispatch），`conclusion: success` |
| 2. 最近一次 Actions run 結論 | ✅ | success（近 30 次 daily.yml run，07-03～08-01，全數 success，非 Make.com 未觸發問題） |
| 3. line_pushed 與 total_notify 一致性 | ✅ | 今日 `_summary.total_notify=0`、`line_pushed=false`，一致（log 明確寫「無新增案件，略過 LINE 推播」）。以 Actions job log 逐日交叉比對 07-26～08-01（而非 `daily_report.json`，理由見異常 8），`total_notify>0` 時皆有「✅ LINE broadcast 成功」，全數一致 |
| 4. 所有來源 fetched=0 | ✅ 否 | 13 個來源中 10 個抓到資料（合計 62 筆），僅 3 個為 0 |
| 5. 個別來源連續 3 天以上 fetched=0 | ❌ | 教育部學產基金／台北市都發局／國防部政治作戰局：**連續第 5 天** fetched=0（07-28～08-01）。根因見問題 1，仍未解決 |
| 6. 來源 fetched 暴跌/暴增 | ✅ | 其餘 10 個來源 7 日內波動皆在合理範圍（農業部瑠公穩定 15 筆、財政局 13-14 筆、郵局恆 8 筆），無 >50% 驟降或 >3 倍暴增 |
| 7. 應被黑名單擋掉卻漏網 | ⚠️ | 近 7 天無新推播觸發新漏網事件，但 07-28 已推播的台鐵新竹縣案件（問題 2，非雙北）根因仍未修復，隨時可能再次發生 |
| 8. 標題為分類/導覽文而非真正案名 | ⚠️ | `state.json` 中「國家住宅及都市更新中心」快取仍可見「會員中心」等導覽文字被當成標案標題擷取；07-30 推播的國有財產署標題為批次公告「北區分署115年第13批」而非個別案名（見異常 6） |
| 9. url 指向列表頁而非個別公告頁 | ⚠️ | 多來源 `state.json` 快取中 url 為空字串（比列表頁更差，是完全無連結）：政府採購網 300 筆中 290 筆、台北自來水處 64 筆全空、國家住宅及都市更新中心約半數。根因為 Claude fallback 只吃純文字（見異常 7） |
| 10. date 欄位為空放行 | ⚠️ | 政府採購網 300 筆快取中 290 筆 `date` 為空、台北自來水處全數為空，皆因走 Claude fallback 路徑遺失日期資訊（同異常 7）。屬既有設計下的已知副作用，非今日新增風險，但影響「新鮮度」判斷的可信度 |

## 二、異常與處理

### 問題 1（延續，連續第 5 天）：Anthropic API 組織仍處於停用狀態，3 個來源完全斷線

- **現象**：與 07-28～07-31 完全相同的錯誤，今日（08-01）再次出現：
  ```
  01:30:xx WARNING [政府採購網] Claude API 錯誤：invalid_request_error — This organization has been disabled.
  01:30:xx WARNING [教育部學產基金] Claude API 錯誤：invalid_request_error — This organization has been disabled.
  01:30:xx WARNING [台北市都發局] Claude API 錯誤：invalid_request_error — This organization has been disabled.
  01:30:53 WARNING [國防部政治作戰局] Claude API 錯誤：invalid_request_error — This organization has been disabled.
  ```
  （來源：run [30678046768](https://github.com/twlo79/tender-scraper/actions/runs/30678046768) job id 91309207926）。教育部學產基金／台北市都發局／國防部政治作戰局三個來源的原生 parser（`parse_moe_xuechan`／`parse_taipei_udd`／`parse_gpwd`）在事件發生前（07-27 及更早）本就長期只靠 Claude fallback 才能出數字（真實 table parser 平時就回傳 0 筆，此為長期潛在問題，見異常 7 的關聯分析），因此 Claude 組織停用等於這 3 個來源**完全斷線**，而非降載。已累計 **5 天**（07-28、07-29、07-30、07-31、08-01）。
- **定位**：`ANTHROPIC_API_KEY` 對應的 Anthropic 組織被停用（帳務/合規問題），與程式碼無關。
- **建議修法**：非程式碼問題，需人工處理：
  1. 登入 [Anthropic Console](https://console.anthropic.com) 確認組織停用原因並恢復，或更換為新的可用 API Key。
  2. 恢復或更換後，更新 GitHub repo secret `ANTHROPIC_API_KEY`。
  3. 中期建議：這 3 個來源應補上不依賴 Claude 的原生 parser（詳見優化建議 2），否則往後只要 Claude API 有任何中斷就會重演本次事故。
- **是否已修**：未修。**本次環境無法連線 `depart.moe.edu.tw`／`udd.gov.taipei`／`gpwd.mnd.gov.tw`（WebFetch 與本機皆回傳 403），無法獨立驗證這 3 個網站目前的即時 HTML 結構是否已改版**，僅能確認 Claude fallback 本身失效為目前可確定的根因。**維持最優先待決事項，已連續 5 天無人工處理，建議立即處理。**

### 問題 2（延續，仍未修復）：國營台鐵地區篩選漏洞——分處名稱含地名導致非雙北案件外洩

- **現象**：`parse_tra()` 僅以「臺北營業分處」這個管理單位名稱篩選項目，但該單位轄區橫跨全台。07-28 已實際推播一筆非雙北案件：「【臺北營業分處】【土地出租】【新竹縣新豐鄉明新段111、113地號部分土地】【115.07.28~115.08」——因標題含「臺北營業分處」五字而通過 `GLOBAL_FILTER["regions"]` 的關鍵字比對，但實際出租標的在新竹縣，非台北/新北。今日程式碼比對確認**修法仍未套用**（`grep -n "臺北營業分處"` 僅出現於 `scraper.py:195` 的原始篩選邏輯，無後續地名二次篩選）。
- **定位**：`scraper.py:194-196`。
- **建議修法**：
```diff
--- a/scraper.py
+++ b/scraper.py
@@ -193,6 +193,9 @@ def parse_tra() -> list[dict]:
     # 只保留臺北營業分處的標案
     items = [i for i in items if "臺北營業分處" in i.get("agency", "") + i.get("title", "")]
+    # 臺北營業分處轄區橫跨全台，僅靠單位名無法排除外縣市案件，需再依標題地名二次篩選
+    NON_TAIPEI_HINTS = ["新竹", "桃園", "苗栗", "台中", "臺中", "彰化", "宜蘭", "花蓮", "台東", "臺東"]
+    items = [i for i in items if not any(h in i["title"] for h in NON_TAIPEI_HINTS)]
     log.info(f"  [國營台鐵] {len(items)} 筆（臺北營業分處）")
     return items
```
- **是否已修**：未修。套用後請執行 `python dry_run_all_regions.py --debug --source 國營台鐵` 驗證新竹案件被排除、雙北案件（如樹林車站旅運販賣空間）仍保留。

### 問題 3（延續，仍未修復）：Google Alerts 混入房產媒體新聞報導，非實際招標公告

- **現象**：07-26 推播「租期最長20年！國有房地標租續租新制上路台北星鑽20戶7月底打頭陣 - 樂屋網房產新聞」，是媒體對政策的報導文章，不是具體招標/標租公告，不應計入「案件」。
- **重要提醒**：`passes_filters()` 完全不讀取 `SOURCES` 內 Google Alerts 自訂的 `whitelist`/`blacklist`，只讀取模組層級的 `GLOBAL_FILTER`（見問題 7）。任何修法都必須寫進 `GLOBAL_FILTER["blacklist"]` 才會生效。
- **定位**：`scraper.py:69-78`（`GLOBAL_FILTER["blacklist"]`）。
- **建議修法**：
```diff
--- a/scraper.py
+++ b/scraper.py
@@ -69,6 +69,7 @@ GLOBAL_FILTER = {
     "blacklist": [
         "開標結果",
+        "樂屋網", "591", "好房網", "蘋果地產", "房產新聞", "新制上路",
         "自動販賣機", "場地短期出租", "機械類標租", "共享雨傘",
         "新建工程", "統包工程", "物業管理", "專案管理", "保險",
         "清洗作業", "鑄鐵直管", "延性鑄鐵", "塗裝", "管線",
         "財物採購", "勞務採購",
     ],
```
- **是否已修**：未修。套用後請執行 `python dry_run_all_regions.py --debug --source "Google Alerts"` 驗證同類新聞報導被排除，真正的標租公告仍正常通過。

### 問題 4（延續，仍未修復）：`_is_junk()` 導覽文防呆函式從未被呼叫

- **現象**：`scraper.py:113` 定義 `_is_junk(item)`，今日以 `grep -n "_is_junk("` 確認全檔僅該定義一行出現，仍無任何呼叫點。與此對應的實例：`state.json`「國家住宅及都市更新中心」快取中可見標題為「會員中心」（純導覽文字，非標案）的項目被當成正常 item 存入。
- **定位**：`parse_with_claude_fallback()`（`scraper.py:696` 附近）直接回傳 Claude 結果，未套用過濾。
- **建議修法**：
```diff
--- a/scraper.py
+++ b/scraper.py
@@ -693,7 +693,8 @@ def parse_with_claude_fallback(text: str, name: str, base: str) -> list[dict]:
             u = item.get("url", "")
             if u and not u.startswith("http"):
                 item["url"] = urljoin(base, u)
-        return items if isinstance(items, list) else []
+        items = items if isinstance(items, list) else []
+        return [i for i in items if not _is_junk(i)]
     except Exception as e:
         log.warning(f"  [{name}] Claude fallback 失敗：{e}")
         return []
```
- **是否已修**：未修。待 Anthropic API 恢復（問題 1）後，用 `dry_run_all_regions.py --debug --source 教育部學產基金` 等驗證。

### 問題 5（延續，仍未修復，本次環境仍無法連網驗證）：郵局房地產出租／國家住宅及都市更新中心因缺 `agency` 欄位被地區篩選誤殺

- **現象**：`parse_post()`／`parse_hurc()` 產出的 item dict 沒有 `"agency"` 欄位。這兩來源 31 天彙總（今日重新統計）：郵局房地產出租 fetch=292／new=5／notify=0，國家住宅及都市更新中心 fetch=325／new=1／notify=0。根因是全國性機構公告標題只寫「內湖」「南港區」「林口」等行政區名，未含「台北/新北」字樣，被 `GLOBAL_FILTER["regions"]` 誤殺。
- **定位**：`scraper.py` 中 `passes_filters()` 的地區篩選邏輯（`scraper.py:796-804`），僅比對 `title+agency` 是否含「台北/臺北/新北/瑠公管理處」，未涵蓋雙北各行政區名稱。
- **建議修法**：
```diff
--- a/scraper.py
+++ b/scraper.py
@@ -60,6 +60,13 @@
+TAIPEI_NEWTAIPEI_DISTRICTS = [
+    "松山區","信義區","大安區","中山區","中正區","大同區","萬華區","文山區","南港區","內湖區","士林區","北投區",
+    "板橋區","新莊區","中和區","永和區","土城區","樹林區","三峽區","鶯歌區","三重區","蘆洲區","五股區","泰山區",
+    "林口區","淡水區","三芝區","石門區","八里區","金山區","萬里區","汐止區","瑞芳區","貢寮區","平溪區","雙溪區",
+    "深坑區","石碇區","坪林區","烏來區",
+]
+
 def passes_filters(item: dict) -> bool:
     """套用 GLOBAL_FILTER 的白名單、黑名單、地區篩選（所有來源共用）。"""
     title  = item.get("title", "")
     agency = item.get("agency", "")
     text   = title + agency

     regions = GLOBAL_FILTER.get("regions", [])
-    if regions and not any(k in text for k in regions):
+    if regions and not any(k in text for k in regions) and not any(d in title for d in TAIPEI_NEWTAIPEI_DISTRICTS):
         return False
```
- **是否已修**：未修。

### 問題 6（延續，仍未修復，本次環境仍無法連網驗證）：土地銀行出租不動產 parser 選擇器過於寬泛

- **現象**：`state.json`「土地銀行出租不動產」快取（31 天彙總 fetch=62／new=14／notify=0，轉換率 0%）幾乎全是銀行系統維護公告、行銷活動廣告（如晶片金融卡跨國提款服務公告），與租賃/招租公告無關。
- **定位**：`parse_landbank()` 的 CSS selector `table tbody tr, table tr, .bulletin-item, .list-item, article` 範圍過廣，疑似命中全站公用公告區塊而非 `/Bulletin/RentRealty` 頁面裡的租賃清單表格。
- **建議修法**（治標，根本解仍需實際 HTML 比對）：
```diff
     for row in soup.select("table tbody tr, table tr, .bulletin-item, .list-item, article"):
+        row_text = row.get_text()
+        if any(kw in row_text for kw in ["系統維護", "網路銀行", "客服", "資訊線路", "抽獎", "行動Pay"]):
+            continue
```
- **是否已修**：未修。本次環境對 `www.landbank.com.tw` 的連線仍回傳 403（proxy 層），無法即時驗證 HTML 結構。

### 問題 7（延續，仍未修復）：`SOURCES` 內個別來源自訂的 whitelist／blacklist／regions 欄位是死代碼

- **現象**：`passes_filters(item: dict)` 簽名今日確認仍只有 `item` 一個參數（`scraper.py:796`），呼叫處（`scraper.py:1151`）也仍是 `passes_filters(i)`。`SOURCES` 中 7 個來源自訂的 `whitelist`/`blacklist`/`regions`（郵局房地產出租、政府採購網、教育部學產基金、國防部政治作戰局、土地銀行出租不動產、國有財產署、Google Alerts）全部從未被讀取，僅有模組層級 `GLOBAL_FILTER` 真正生效。
- **重要性**：任何人依照過去報告的建議去修改某來源專屬的 `blacklist`/`regions`，改了也不會生效——07-28 報告本身就踩過這個坑（見問題 3 說明）。
- **建議修法**：二擇一：(a) 移除這些未使用的欄位並在註解說明篩選規則統一由 `GLOBAL_FILTER` 控制；(b) 修改 `passes_filters(item, source=None)`，真正合併套用來源層級覆寫（需注意：多數來源目前 `regions`/`whitelist` 為空列表 `[]`，若直接照字面「空列表=不限」語意套用，會讓這些來源失去 `GLOBAL_FILTER` 的地區限制，屬於危險的行為變更，**不建議未經人工確認就直接套用**）。
- **是否已修**：未修。方向 (a)/(b) 選擇列入待決事項。

### 問題 8（新發現）：`log_checker.py` 抓取 Actions log 未指定編碼，`daily_report.json` 長期資料失真

- **現象**：`daily_report.json` 對照 GitHub Actions 實際 job log 逐日核對，發現**系統性不一致**：
  - 07-28 實際 log：合計新增 10 筆、推播 2 筆（國營台鐵 2 筆），LINE broadcast 成功。但 `daily_report.json["2026-07-28"]` 卻記錄 `total_new=0`、`total_pushed=0`、`sources={}`、`line="無推播（0 筆）"`。
  - 07-30 實際 log：合計新增 6 筆、推播 2 筆，同樣被記成 `total_pushed=0`、`line="無推播（0 筆）"`。
  - 30 天內每一天的 `sources` 欄位皆為空字典 `{}`，`total_new`/`total_pushed` 幾乎恆為 0，只有 `errors`（WARNING/ERROR 純英文部分）勉強可讀，但其中的中文機關名稱全部呈現亂碼（如「政府採購網」被存成 `"æ¿åºæ¡è³¼ç¶²"`）。
  - 用 Python 重放 `log_checker.parse_log()`，對照真實 log 原文（含中文）測試，regex 本身完全正確、可正常解析出 `sources`/`total_new`/`total_pushed`/`line="成功"`；但把同一段文字先以 UTF-8 編碼、再以 Latin-1（ISO-8859-1）解碼後重放，會產生與 `daily_report.json` 裡幾乎一致的亂碼字串，重現了 `sources={}`／`total_pushed=0`／`line` 判斷失敗的現象。
- **定位**：`log_checker.py` 的 `get_latest_log()`（第 46-50 行）：
  ```python
  r = requests.get(
      f"https://api.github.com/repos/{GITHUB_REPO}/actions/jobs/{jobs[0]['id']}/logs",
      headers=GH, timeout=30, allow_redirects=True,
  )
  return r.text
  ```
  未設定 `r.encoding`。GitHub Actions log 端點回應 `Content-Type` 未明確帶 UTF-8 charset 時，`requests` 依 HTTP 舊規範預設以 ISO-8859-1 解碼 `.text`，導致所有中文（UTF-8 多位元組）被錯誤拆解成亂碼，讓依賴中文字面比對的 regex（`抓取：`、`合計新增：`、`推播：`、`LINE broadcast 成功`）全數失配。
- **影響**：`daily_report.json` 是專為「人工或 Claude Code 自動 QA 閱讀」設計的資料源（`.github/workflows/daily.yml` 的 job summary 註解也是此用途），但實際上：
  - 無法用它判斷任何一天是否真的有推播（`total_pushed` 恆為 0，即使實際上有推播），本報告 A3 項目因此改用 Actions log 直接交叉驗證，而非依賴 `daily_report.json`。
  - `errors` 欄位雖然還能看到英文錯誤訊息，但機關名稱亂碼會讓快速掃描報告的人難以第一時間判斷是哪個來源出問題。
- **建議修法**：
```diff
--- a/log_checker.py
+++ b/log_checker.py
@@ -44,10 +44,11 @@ def get_latest_log() -> str:
     r = requests.get(
         f"https://api.github.com/repos/{GITHUB_REPO}/actions/jobs/{jobs[0]['id']}/logs",
         headers=GH, timeout=30, allow_redirects=True,
     )
-    return r.text
+    r.encoding = "utf-8"
+    return r.text
```
- **是否已修**：未修。套用後建議手動重跑一次 `python log_checker.py`（或觸發 `log-check.yml` workflow_dispatch），確認新產生的 `daily_report.json` 條目 `sources` 非空、中文正常顯示、`total_pushed` 與 `sent_log.json` 的 `_summary.total_notify` 相符，再視需要回補（或直接放棄）過去 30 天已損毀的歷史條目。

## 三、優化建議

### 方向 A：篩選精準度

**建議 1（延續，仍是最高槓桿）：先修問題 7（死代碼）與問題 2/3/5 的篩選規則，而非新增來源**

- **依據**：31 天彙總（今日重新統計，`sent_log.json`）：
  | 來源 | fetch | new | notify | 轉換率 |
  |---|---:|---:|---:|---:|
  | 台北市都發局 | 750 | 0 | 0 | 0%（且 31 天內從未出現過「新增」，見建議 3） |
  | 農業部 瑠公管理處 | 465 | 1 | 1 | 100%（樣本太小） |
  | 台北市財政局 | 391 | 1 | 1 | 100%（樣本太小） |
  | 國家住宅及都市更新中心 | 325 | 1 | 0 | 0% |
  | 郵局房地產出租 | 292 | 5 | 0 | 0% |
  | 國防部政治作戰局 | 273 | 1 | 1 | 100%（樣本太小） |
  | 土地銀行出租不動產 | 62 | 14 | 0 | 0% |
  | 教育部學產基金 | 59 | 1 | 0 | 0% |

  國家住宅及都市更新中心、郵局房地產出租、土地銀行出租不動產這 3 個「fetch 高但 notify=0」的來源，經抽查並非「白名單過嚴」而是各自對應到問題 5（缺 agency 欄位被地區誤殺）與問題 6（selector 誤命中站內公用公告）的具體 bug，屬於**可修復的假性零轉換**，不建議直接下架。
- **做法**：依序套用問題 5、6、7(a 或 b) 的 diff。
- **預期效果**：郵局／國家住宅及都市更新中心至少可追回過去已確認的雙北租賃案件；土地銀行可排除銀行站內公用公告噪音，讓真正的租賃公告（若存在）能被看見。

**建議 2（新發現）：國營台鐵地區篩選有實際漏網案例，土地銀行/都發局的地區篩選亦需比照檢查「機關名含地名但標的物不在該地」的模式**

- **依據**：07-28 已實際推播 1 筆新竹縣案件（問題 2）。這類「行政單位名稱剛好含地名關鍵字，但管轄範圍跨縣市」的模式，除台鐵外，其他跨區機關（如國防部眷村、土地銀行全國分行）理論上也可能出現同類假陽性，只是目前尚未實際觀察到具體案例。
- **做法**：套用問題 2 的 diff 作為第一個修復範例；後續可考慮把「排除清單」（NON_TAIPEI_HINTS）提升為 `GLOBAL_FILTER` 共用設定，供所有跨區來源共用，而非只修台鐵一處。
- **預期效果**：降低非雙北案件誤推的機率，減少對使用者的信任耗損（收到不相關地區的推播容易讓人忽略整個服務）。

### 方向 B：來源健康度

**建議 3（新發現）：台北市都發局 31 天內抓了 750 筆卻從未出現過「新增」，需查證是否抓到過期存檔頁**

- **依據**：`sent_log.json` 31 天趨勢顯示台北市都發局在 07-27 之前每天穩定抓到約 28-30 筆（07-26 為 30 筆），但每一天都是「✅ 無新增」，31 天累積 `new=0`。相較之下，其他長期低轉換來源（郵局、國家住宅都更）至少偶爾出現 `new>0`。都發局「筆數固定不變、內容完全零更新」的模式，比較符合「parser 抓到的是固定不變的存檔/歷史清單頁，而非每日更新的最新公告頁」，而非單純「這段期間真的沒有新公告」。
- **做法**：待問題 1（Claude API）恢復、都發局原生 parser 可重新驗證後，人工核對 `https://udd.gov.taipei/events/psxwq1j` 這個頁面本身是否為「最新公告」還是「累積歸檔清單」，若是後者需找到正確的「最新公告」分頁網址。
- **預期效果**：若確認是網址問題，修正後可望讓都發局從「完全零轉換」變成有效產出，是目前唯一一個「fetch 破百但 new=0」的極端案例，優先度應提高。

**建議 4（延續）：土地銀行出租不動產若短期無法修 selector，可考慮暫時停用推播、僅保留 fetch 供人工週期性檢查**

- **依據**：31 天 fetch=62／new=14／notify=0，且抽查顯示新增的 14 筆全部是銀行公用公告而非租賃公告，代表目前的 selector 對「找到真正租賃公告」完全無效。
- **做法**：問題 6 的 diff 是治標方案；若優化建議 3 個月內仍未看到任何真實租賃公告通過，建議重新評估是否值得維護此來源，或改用土地銀行官網的 RSS/JSON API（若有提供）取代目前的 HTML 硬爬。
- **預期效果**：避免持續耗費爬取資源在噪音來源上。

### 方向 C（新來源）：本次略過

既有問題 1～8 均尚未修復，且問題 1（Claude API）已達 5 天未解、影響範圍最大（3 個來源完全斷線），問題 8（`daily_report.json` 資料源本身失真）也才剛發現。在既有問題修復並觀察至少一週成效之前，新增未驗證來源的邊際效益低於修復既有問題，故本次不提出新來源候選。

## 四、待決事項

1. **【最優先，已連續 5 天，建議立即處理】Anthropic API 組織已停用**（問題 1）：需人工登入 Anthropic Console 確認並恢復，並視需要更新 GitHub repo secret `ANTHROPIC_API_KEY`。這是本次唯一持續惡化（天數增加）且無法透過程式碼修復的項目。
2. 問題 2～6、8 的修法 diff 是否核准套用（均為建議，尚未套用）。問題 6（土地銀行）因本次環境仍無法連線 `landbank.com.tw`，套用前建議先在有網路存取權限的環境確認實際 HTML 結構。
3. 問題 7（`SOURCES` 死代碼欄位）處理方向：(a) 移除欄位 或 (b) 讓 `passes_filters` 真正套用來源層級覆寫。若選 (b)，需先決定「來源 `regions`/`whitelist` 為空列表」的語意（沿用 `GLOBAL_FILTER` 或視為明確不限），避免造成篩選規則意外放寬。
4. 前期報告遺留：國有財產署「地上權設定」類別是否納入業務範圍（地上權為長期用益物權，與「租賃/標租」性質相近但非同一法律關係），需人工判斷。
5. 前期報告遺留：「太陽光電發電系統標租」類案件是否應納入黑名單排除，需業主確認業務範圍認定。
6. 建議 3（台北市都發局 31 天 new=0）是否值得優先人工查證頁面網址是否為過期存檔頁——這會決定都發局復原後能否真正產生新推播，而不只是「抓到資料但等於沒抓到」。
7. **環境限制說明**：本次執行環境對 `esvc.fnp.gov.tw`、`landbank.com.tw`、`udd.gov.taipei`、`depart.moe.edu.tw`、`gpwd.mnd.gov.tw` 等網域的直接連線／WebFetch 均被拒（proxy 403 或站方 403），故問題 1、6 的假設未能以 `dry_run_all_regions.py` 或 WebFetch 即時驗證即時 HTML 結構，僅基於 `state.json`、`sent_log.json`、`daily_report.json`、GitHub Actions 實際 job log 與程式碼靜態比對得出結論。需在有網路存取權限的環境（如 GitHub Actions runner）重新確認。
