# 標案爬蟲監測報告 2026-09-04

## 一、狀況總覽

整體狀態：❌ 有異常（執行層面、抓取層面本次皆正常，今日**沒有**新的真實誤推播；但問題 1〔國營台鐵地區篩選對該來源完全失效〕自 09-03 首度發現後仍未修，程式碼自 08-28 commit `b504d6c` 後持續未變動——本次以 32 天累計數字重新驗證，此來源 `notify_sum=9` 完全等於 `new_sum=9`，即該來源「每一筆新抓到的項目 100% 被推播」，地區篩選在此來源仍是完全無效的死碼，隨時可能再次外洩非雙北案件，故列為 ❌；另有 5 項延續自 07-29～09-03、連續多日未拍板的已知缺口，同樣未修；本次新增 1 項輕微觀察：政府採購網今日首度單日 `fetched=0`，經比對 Actions job log 判斷為連線成功後查無符合結果，非連線失敗，暫列 ⚠️ 觀察）

最近執行：2026-09-04 01:30（推播 1 筆 / 新增 10 筆 / 抓取 99 筆）

| 檢查項目 | 結果 | 說明 |
|---|---|---|
| 1. 今日已執行 | ✅ | `sent_log.json` 最新 key 為 `2026-09-04 01:30`，即今天 |
| 2. 最近一次 GitHub Actions 結論 | ✅ | `daily.yml` run `33826002559`，2026-09-04T01:30:07Z，`conclusion=success`；近 10 次 run（08-27～09-04）全數 success，無失敗紀錄 |
| 3. line_pushed 與 total_notify 一致性 | ✅ | `_summary`：`total_fetched=99`、`total_new=10`、`total_notify=1`、`line_pushed=true`，一致；job log 亦顯示「✅ LINE broadcast 成功（2 則）」 |
| 4. 是否所有來源 fetched=0 | ✅ | 否，13 來源中 12 個今日 >0，僅政府採購網為 0（見第 6 項） |
| 5. 個別來源連續 3 天以上 fetched=0 | ✅ | 無。政府採購網僅今日單日 0，前 6 天皆為 2 |
| 6. 來源抓取量暴跌/暴增 | ⚠️ | **政府採購網**：09-03 的 2 筆 → 09-04 的 0 筆（-100%），連續 6 天固定 2 筆後首次歸零；查 job log 該步驟耗時 4~7 秒且無 `連線失敗` WARNING（對照本環境對外連線一律 403，若為連線失敗會有明確 WARNING），研判為查詢成功但當日確實無符合關鍵字+日期窗的標案，非程式錯誤，建議觀察是否連續多日維持 0。**Google Alerts**：近 7 天 fetched 為 0/1/4/1/6/1/9，今日 9 筆為 7 天平均（約 2.2）的 4.1 倍，但此來源為關鍵字聯播內容，日常波動本就大（history 中已出現過 0→6 的類似跳動），非新異常 |
| 7. 應擋卻漏網的案件 | ❌ | **問題 1 仍未修**：本次以 32 天累計數字重新量化——國營台鐵 `fetched_sum=69`、`new_sum=9`、`notify_sum=9`，new 與 notify **完全相等**，代表這個來源目前「有新項目就必推播」，地區篩選（`GLOBAL_FILTER["regions"]`）對它 100% 無過濾效果，純屬巧合尚未於今日觸發新的非雙北外洩，但缺陷本身未變。今日本身無新推播誤報（TRA 今日 `new=0`），故本次無新增外洩案例，但既有結構性風險維持 ❌ |
| 8. 標題為分類名稱／導覽文而非案名 | ⚠️ | 今日推播內容非此類；延續問題 4（國有財產署 `skip_global_filter=True`），09-01 樣本仍是目前唯一已知案例，狀態未變 |
| 9. url 指向列表頁而非個別公告頁 | ❌ | 重新抽查 `state.json`：政府採購網 300 筆中 284 筆（94.7%）`url` 空白、台北市都發局 34/34（100%）空白、國防部政治作戰局 11/11（100%）空白、教育部學產基金 4/4（100%）空白。與 09-02／09-03 報告數字一致，根因不變（舊 dedup schema 升級前留存的字串項目），非目前 parser 持續產生 |
| 10. date 欄位為空、導致日期窗被跳過放行 | ❌ | 台北自來水處 59 筆快取 100% `date` 空白且 0/59 含租賃白名單詞；土地銀行出租不動產 39 筆快取 100% `date` 空白。狀態與歷次報告一致，未變 |

## 二、異常與處理

### 問題 1（延續，自 2026-09-03 首度發現起第 2 天未修，唯一結構性、曾造成真實外洩的問題）：國營台鐵地區篩選對此來源完全失效

- **現象**：`scraper.py:194-195` 的來源內部篩選 `items = [i for i in items if "臺北營業分處" in i.get("agency", "") + i.get("title", "")]` 只保留「臺北營業分處」轄下案件；但該分處轄區橫跨雙北以外縣市（鐵路沿線行政區劃分，非地理雙北）。後續 `passes_filters()`（`scraper.py:796-804`）的地區檢查 `any(k in text for k in regions)` 因每筆項目的 `agency`/`title` 都固定含「臺北」二字而恆定通過，等同地區篩選對此來源完全失效。
- **量化證據（本次更新）**：32 天累計 `fetched_sum=69`、`new_sum=9`、`notify_sum=9`——new 與 notify 相等，即目前只要抓到新項目必推播，此來源實質上「零過濾」。已知曾造成 2 次真實外洩（09-01 基隆市安樂區、09-03 桃園市桃園區），今日 `new=0` 未再新增外洩，但缺陷本身未修，隨時可能於下一筆非雙北新案件出現時再次外洩。
- **定位**：`scraper.py:194-196`（`parse_tra()` 的分處篩選邏輯）+ `scraper.py:796-804`（`passes_filters()` 地區檢查對 `agency` 欄位的通用假設，在此來源不成立）。
- **建議修法**（與 09-03 報告相同，scoped 在 `parse_tra()` 內，不影響其他來源既有邏輯）：
  ```diff
       a = item.select_one("div.btn-box a[href]")
       href = urljoin(BASE, a["href"]) if a else URL
       items.append({"title": title, "date": dt, "url": href, "agency": unit})
       # 只保留臺北營業分處的標案
       items = [i for i in items if "臺北營業分處" in i.get("agency", "") + i.get("title", "")]
  +    # 「臺北營業分處」轄區橫跨雙北以外縣市（已實際發生：09-01 基隆市安樂區、
  +    # 09-03 桃園市桃園區皆因 agency/title 固定含「臺北營業分處」字樣而誤判通過
  +    # GLOBAL_FILTER 地區篩選）。改為檢查標題「地點」括號（第 3 個【】區塊）
  +    # 是否真的落在雙北，而非依賴分處名稱。
  +    def _tra_is_shuangbei(title: str) -> bool:
  +        parts = re.findall(r"【(.*?)】", title)
  +        loc = parts[2] if len(parts) > 2 else title
  +        return any(k in loc for k in ("台北", "臺北", "新北"))
  +    items = [i for i in items if _tra_is_shuangbei(i["title"])]
       log.info(f"  [國營台鐵] {len(items)} 筆（臺北營業分處）")
       return items
  ```
  （`re` 已在檔案頂部 `import re`，無需額外 import。）
- **驗證阻礙**：本次執行環境對 `www.railway.gov.tw` 仍為 `EGRESS_BLOCKED`（`dry_run_all_regions.py --debug --source 國營台鐵`／`--non-taipei` 皆已嘗試，proxy 回 403 Forbidden），無法即時重跑驗證此 diff 套用後的筆數變化。
- **是否已修**：未修（依規則預設不自動改程式碼；此為業務衝擊最高的項目，建議優先核准）。

### 問題 2（延續，自 2026-07-29 起已第 37 天未修）：政府採購網 parser 抓到與雙北不動產完全無關的全國性標案

- 狀態與 09-03 報告一致：32 天累計 `fetched_sum=65`、`notify_sum=0`，`state.json` 300 筆快取上限中仍為大量全國性無關標案。今日 `fetched=0`（見狀況總覽第 6 項），為連續 6 天固定 2 筆後首次歸零，研判是查詢當日無符合結果而非連線失敗，不影響此問題本身的判定。程式碼自 08-28 後未變動。
- **定位**：`scraper.py:390-455`（`_pcc_fetch_keyword()`）
- **建議修法**：同 09-03 報告，於回傳前加一道標題含關鍵字的防禦性檢查（`items = [i for i in items if kw in i.get("title","")]`）。
- **驗證阻礙**：`web.pcc.gov.tw` 本次仍為 `EGRESS_BLOCKED`。
- **是否已修**：未修，第 37 天。

### 問題 3（延續，自 2026-07-29 起已第 37 天未修）：土地銀行出租不動產 parser 選擇器過寬，抓到全站系統維護公告

- 狀態不變：32 天累計 `fetched_sum=60`、`new_sum=14`、`notify_sum=0`。
- **定位**：`scraper.py:592-617`（`parse_landbank()`）
- **建議修法**：同 09-03 報告，收斂選擇器或加標題關鍵字防禦（需標題含「出租」或「不動產」）。
- **驗證阻礙**：`www.landbank.com.tw` 本次仍為 `EGRESS_BLOCKED`。
- **是否已修**：未修，第 37 天。

### 問題 4（延續，長期未拍板）：`GLOBAL_FILTER` 黑名單缺「停車場」「決標結果」「廣告標租」；國有財產署 `skip_global_filter=True` 讓分類彙總列（非個別案名）漏網

- 狀態不變：`scraper.py:69-76` 黑名單仍未加入「停車場」「決標結果」「廣告標租」；`scraper.py:744-745` 國有財產署仍為 `skip_global_filter=True`，`FNP_PAGES`（`scraper.py:330`）仍含「停車場」分類頁（`esvc.fnp.gov.tw/parkingMsg`）。
- **建議修法**：同 09-03 報告的兩組 diff（黑名單新增關鍵字；移除 FNP「停車場」頁並拿掉 `skip_global_filter`）。
- **是否已修**：未修。

### 問題 5（延續，長期未拍板）：安幸 ERP 匯入 `push_to_anxing()` header 值未 `.strip()`，本次執行再次真實觸發

- **現象**：本次 job log（`33826002559` / job `100878700875`）：`⚠ ERP 匯入異常（不影響 LINE 推播）：Invalid leading whitespace, reserved character(s), or return character(s) in header value: '***\n'`。今日 `notify=1`，再度真實觸發，累計自 09-01 起凡有推播必失敗（3/3 次）。
- **定位**：`scraper.py:1131-1132`（`url = os.getenv("ANXING_URL", "")` / `key = os.getenv("ANXING_KEY", "")`）
- **建議修法**：
  ```diff
       url = os.getenv("ANXING_URL", "")
       key = os.getenv("ANXING_KEY", "")
  +    url = url.strip()
  +    key = key.strip()
  ```
  仍需人工至 GitHub repo Settings → Secrets 重新貼上不含換行的 `ANXING_KEY`／`ANXING_URL`，程式碼端 `.strip()` 僅為防禦性保底。
- **是否已修**：未修。此整合上線以來凡有推播必失敗（3/3）。

### 問題 6（延續，長期未拍板）：`SOURCES` 中各來源自訂 `whitelist`/`blacklist`/`regions` 從未生效（死碼）

- 狀態不變：`passes_filters(i)` 呼叫處（`scraper.py:1188`）仍只傳入單一參數，各來源自訂設定從未被讀取。
- **建議修法**：同 09-03 報告，`passes_filters(item, source_cfg=None)` + 呼叫處改傳入 `src`。
- **是否已修**：未修。

## 三、優化建議

### 建議 1（方向 A）：優先套用問題 1 的區域判定修法——國營台鐵目前對新項目零過濾

- **依據**：32 天累計，國營台鐵 `new_sum=9` 與 `notify_sum=9` 完全相等，代表地區篩選對此來源沒有攔下任何一筆，是目前唯一「新項目 100% 直接推播」的來源，也是本月內唯二造成真實 LINE 誤推播的問題（09-01 基隆市、09-03 桃園市）。
- **做法**：問題 1（地區判定）+ 問題 4（黑名單補「停車場」「廣告標租」，涵蓋 08-20 鶯歌區停車場標租、08-24 臺北車站廣告標租兩種既往類別誤報）合併套用；套用後需以 `dry_run_all_regions.py --debug --source 國營台鐵` 比對修法前後筆數（目前環境 `EGRESS_BLOCKED`，需於有網路權限環境驗證）。
- **預期效果**：國營台鐵不再是「有新項目必推」的來源，後續基隆、桃園等鐵路沿線非雙北案件會在地區層被正確擋下。

### 建議 2（方向 A，延續）：問題 2、3、4、6 的既有 diff 儘速一次套用

- **依據**：與前次報告相同——政府採購網（32 天 `fetched_sum=65`／`notify_sum=0`）、土地銀行（`fetched_sum=60`／`notify_sum=0`）快取內容分別為全國無關標案／全站銀行公告，長期佔用 300 筆 dedup 上限（政府採購網已滿 300）；問題 4 的 FNP 分類彙總列已有 1 筆真實漏網推播（09-01）。四組 diff 互不衝突，可一次套用並用 `dry_run_all_regions.py --debug` 做全來源前後對照。
- **預期效果**：政府採購網／土地銀行快取回歸雙北租賃相關內容；國有財產署推播回歸個別案名。

### 建議 3（方向 B，延續）：台北自來水處 32 天 0 筆通過、快取內容 0/59 曾含租賃詞——建議調整抓取頁面而非直接判定為壞來源

- **依據**：本次重新抽查 `state.json`「台北自來水處」現有 59 筆快取，仍是**沒有任何一筆**標題含白名單關鍵字，`date` 欄位 59/59（100%）空白。32 天累計 `fetched_sum=98`、`new_sum=5`、`notify_sum=0`。目前抓取的 `News.aspx?n=D2818696FF5048B8` 疑似是該處全類別公告看板，而非「標租／招租」專屬分類頁。
- **做法**：同前次報告，建議在有網路權限環境確認 water.gov.taipei 是否存在「不動產標租」或「土地招租」獨立分類頁；若確認該處近年並無不動產租賃業務，則考慮從來源清單移除。
- **預期效果**：若改抓對的分類頁，此來源終於可能產出真正雙北租賃案件；若確認無相關業務，移除可省下每次執行的頁面請求。

### 建議 4（方向 B）：台北市財政局／都發局／政治作戰局／瑠公管理處維持現狀，不建議移除

- **依據**：32 天累計 `notify_sum` 皆為 0（都發局 `fetched_sum=750`、財政局 348、政治作戰局 275、瑠公管理處 480），但快取內容經歷次報告抽查確認為真實雙北不動產標租相關案件（如「南港機廠社會住宅商業設施」「115年度臺北永吉段都更案店鋪、住宅國有學產不動產第2次公開標租」），只是官方公告本身低頻更新。與台北自來水處、土地銀行性質不同，不應一概而論為壞來源。
- **做法**：維持現狀，不移除、不調整篩選條件。

### 建議 5（方向 C）：本次因網路環境限制，仍暫不提出新來源候選

- 本次執行環境對外部網域（`www.railway.gov.tw`、`web.pcc.gov.tw`、`www.landbank.com.tw`、`www.water.gov.taipei`、`esvc.fnp.gov.tw` 等）一律 `EGRESS_BLOCKED`（已用 `dry_run_all_regions.py --debug`／`--non-taipei` 雙重驗證，proxy 回 403），且方向 A 的問題 1 有明確、量化的高優先事項需先處理，故延續前幾次報告的判斷，暫緩新來源調查。建議留待下次有網路權限的環境，或由人工提供候選清單。

## 四、待決事項

1. 【延續，自 09-03 首度發現起第 2 天，唯一造成過真實外洩的結構性問題】問題 1（國營台鐵地區篩選失效，32 天累計 new=notify=9，等同零過濾）：是否核准套用 `parse_tra()` 的地點判定 diff？並授權在有網路權限環境用 `dry_run_all_regions.py --debug --source 國營台鐵` 驗證？**建議優先於下列所有事項處理。**
2. 【長期未拍板，自 07-29 起第 37 天】問題 2（政府採購網 parser 抓到全國性無關標案）：是否核准防禦性 diff？
3. 【長期未拍板，自 07-29 起第 37 天】問題 3（土地銀行 parser 選擇器過寬）：是否核准防禦性 diff？
4. 【長期未拍板】問題 4（黑名單缺「停車場」「決標結果」「廣告標租」；國有財產署 `skip_global_filter=True`）：diff 是否核准套用？
5. 【長期未拍板】問題 5（安幸 ERP header 值未 `.strip()`，累計 3/3 次推播必失敗）：是否請人工至 GitHub repo Secrets 重新設定 `ANXING_KEY`／`ANXING_URL`？程式碼端 `.strip()` 防禦修法是否核准？
6. 【長期未拍板】問題 6（`SOURCES` 逐來源篩選設定從未生效）：diff 是否核准套用？
7. 【延續】建議 3（台北自來水處 32 天 0 筆通過、快取內容 0/59 曾含租賃詞）：是否授權人工核對該處網站是否有專屬「標租／招租」分類頁可替換目前的全類別公告頁？若確認無相關業務，是否核准移除此來源？
8. 方向 C（新來源候選）：是否於下次有網路權限時再行補上，或由人工提供候選清單？
9. 【新增，輕微觀察】政府採購網今日單日 `fetched=0`（連續 6 天固定 2 筆後首次歸零，研判為查無結果而非連線失敗）：是否需要連續觀察 3 天以上再決定是否列為正式異常？目前僅供留意，非需立即拍板。
