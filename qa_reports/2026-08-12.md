# 標案爬蟲監測報告 2026-08-12

## 一、狀況總覽

整體狀態：⚠️ 有注意事項（**重大好消息**：連續 15 天的 Anthropic API 組織停用問題已於今日恢復，3 個核心來源重新上線；但恢復後新發現「台北市都發局」快取 url 全部為空，加上既有的 URC 列表頁 url／全站 date 空值等資料品質問題仍未修復）
最近執行：2026-08-12 02:09（推播 1 筆 / 新增 5 筆 / 抓取 97 筆）

| 檢查項目 | 結果 | 說明 |
|---|---|---|
| 1. 今日已執行 | ✅ | `sent_log.json` 最新 key 為 `2026-08-12 02:09`，即今天 |
| 2. 最近一次 GitHub Actions 結論 | ✅ | run `31555985476`（run #77），2026-08-12T02:09:43Z～02:11:00Z，`workflow_dispatch` 觸發，`conclusion=success`；近 7 次執行（08-06～08-12）全部 `success` |
| 3. line_pushed 與 total_notify 一致性 | ✅ | `_summary`：`total_fetched=97`、`total_new=5`、`total_notify=1`、`line_pushed=true`，一致；job log 顯示「✅ LINE broadcast 成功（2 則）」 |
| 4. 是否所有來源 fetched=0 | ✅ | 否，本次共抓到 97 筆，13/13 來源皆有抓到資料（近 15 天以來首次全部來源同時有資料） |
| 5. 個別來源連續 3 天以上 fetched=0 | ✅（今日解決，延續 15 天的異常今日恢復） | 教育部學產基金、台北市都發局、國防部政治作戰局 此前連續 **15 天**（07-28～08-11）`fetched=0`，原因為 Anthropic API 組織停用（`invalid_request_error — This organization has been disabled.`）。今日 job log（run `31555985476`，job `93988397355`）**完整核對，不再出現任何 Claude API 錯誤訊息**，三來源分別抓到 3／30／11 筆，教育部學產基金並成功推播 1 筆新案。詳見「二、異常與處理」問題 1（恢復記錄） |
| 6. 來源抓取量暴跌/暴增 | ✅ | 教育部學產基金（0→3）、台北市都發局（0→30）、國防部政治作戰局（0→11）三者暴增皆為問題 1 恢復所致，非異常；其餘 10 個來源今日數字與 7 天均值差距皆在合理範圍內（無單一來源超過 ±50%/3倍 且無合理解釋） |
| 7. 應擋卻漏網的案件 | ✅ | 全部 31 天 `sent_log.json` 歷史記錄逐筆核對，**無任何停車場／自動販賣機／工程／採購類標題被推播**。但發現 `GLOBAL_FILTER["blacklist"]` 目前**不含任何「停車」相關關鍵字**，`state.json` 快取中已有 12+ 筆雙北停車場/停車位標題（例：「公開標租臺北市內湖區康寧段四小段452-2地號市有土地作單一停車位使用案」）同時符合白名單「標租」與雙北地區篩選，目前未推播僅因這些項目在 dedup 追蹤期間未被判定為「新增」，屬僥倖未爆發的潛在風險，非規則本身擋下。詳見「三、優化建議」建議 1 |
| 8. 標題為分類名稱／導覽文而非案名 | ⚠️ | 延續：URC 快取（45 筆）中「會員中心」1 筆仍為導覽文而非案名，因不含白名單關鍵字未推播，屬快取污染而非推播風險，與 08-11 報告一致 |
| 9. url 指向列表頁而非個別公告頁 | ⚠️（延續 + 新發現） | URC `state.json` 45 筆快取核對：23 筆（51.1%）為列表頁 `https://www.hurc.org.tw/hurc/procurement`、21 筆（46.7%）url 為空，僅 1 筆（2.2%）為個別公告頁，比例與 08-11 完全一致，漏洞未修復。**新發現**：台北市都發局今日恢復後檢查其快取最新 5 筆，url **全部為空字串**（非列表頁 fallback），與該來源 parser 設計（`href = URL` 應 fallback 到列表頁而非空字串）不符，推測今日走的是 Claude fallback 路徑、Claude 回傳 JSON 未含 url 欄位。詳見問題 2 |
| 10. date 欄位為空導致日期窗被跳過放行的項目 | ⚠️ | `state.json` 全站快取共 762 筆，458 筆（60.1%）`date` 為空，與 08-11（60.4%，757 筆）比例接近，延續性架構問題；今日實際推播的 1 筆（教育部學產基金）date 欄位正常，未受影響 |

## 二、異常與處理

### 問題 1：Anthropic API 組織停用問題，連續 15 天後於今日恢復（重大更新）

- **現象**：教育部學產基金、台北市都發局、國防部政治作戰局 三來源自 07-28 起連續 15 天 `fetched=0`，錯誤為 `invalid_request_error — This organization has been disabled.`。**今日（08-12）job log（run `31555985476`）完整檢視，全程無任何「Claude API 錯誤」或「Claude fallback 失敗」訊息**，三來源分別抓到 3／30／11 筆，其中教育部學產基金抓到「115年度臺北永吉段都更案店鋪、住宅國有學產不動產第2次公開標租」並成功推播（唯一今日推播案件），內容與業務目標高度吻合。
- **定位**：`scraper.py:675-698` `parse_with_claude_fallback()`，根源為 Anthropic 帳務問題，非程式碼問題。今日恢復推測為人工或自動流程已在 Anthropic Console 端處理（本 session 未變更任何程式碼或密鑰）。
- **建議修法**：無需修改程式碼。建議人工確認 Anthropic Console 帳務狀態是否穩定（避免再次被停用），並回溯 07-28～08-11 斷線期間台北市都發局／教育部學產基金／國防部政治作戰局官網是否有中間錯過的公告（因斷線期間無任何 log 記錄實際錯過內容，雖然機率不高但無法排除）。
- **是否已修**：✅ 已恢復（非本 session 修改，屬外部帳務問題自行解決）。建議持續觀察未來數日是否穩定。

### 問題 2：台北市都發局今日快取 url 全部為空（新發現）

- **現象**：`state.json` 中台北市都發局最新 5 筆快取（今日抓取後新增），`url` 欄位**全部為空字串**：
  ```
  公告標租本市南港區經貿社會住宅內1戶商業設施及其對應持分土地(已決標) | 114/06/16 | (url 空)
  公告標租臺北市健康社會住宅內1戶商業設施及其對應持分土地。(已決標) | 112/07/27 | (url 空)
  公告標租本局管有「萬芳社區中心BCD國宅」社區內3戶商業服務設施及其持分土地。(已決標-2標均已標租) | 114/04/25 | (url 空)
  公告標租本市北投區洲美社會住宅內1戶支援型商業設施及其對應持分土地，詳如公告事項。 | 115/06/29 | (url 空)
  公告標租本市新奇岩社會住宅內4戶商業設施及其對應持分土地，詳如公告事項。(已截標) | 110/08/27 | (url 空)
  ```
  這些標題內容正確、日期格式正常，唯獨 url 缺失。若使用者點擊推播訊息中的連結會失敗（雖然這批目前因日期過舊未觸發推播，但未來新公告若走同一路徑會有同樣問題）。
- **定位**：`scraper.py:515-554` `parse_taipei_udd()`。主要 parser（table 解析）在找不到 `<a>` 連結時會 fallback 為 `href = URL`（列表頁網址，非空字串）——這代表這批資料**並非**走主要 parser，而是走 552-553 行的 `parse_with_claude_fallback()`（表示今日主要 table selector 未命中，可能網站結構已變動）。`scraper.py:675-698` 的 `parse_with_claude_fallback()` 中 `u = item.get("url", "")`，若 Claude 回傳 JSON 未包含 `url` 欄位，就會留空，未 fallback 到列表頁網址。
- **建議修法**（未套用，待驗證）：
  ```python
  # scraper.py，parse_with_claude_fallback() 第 693-695 行附近
  for item in items:
      u = item.get("url", "")
      if u and not u.startswith("http"):
          item["url"] = urljoin(base, u)
+     elif not u:
+         item["url"] = base  # Claude 未回傳 url 時 fallback 到來源列表頁，避免推播訊息連結空白
  ```
  另建議檢查 `udd.gov.taipei/events/psxwq1j` 現在的實際 HTML 結構，確認 `table tbody tr, table tr` selector 是否仍命中（今日很可能因為只斷線 15 天回來，網站本身結構如常，但仍建議用 `dry_run_all_regions.py --debug --source 台北市都發局` 驗證，本 session 因 sandbox 網路出口限制無法連線該網域，見待決事項）。
- **是否已修**：未修（依修改規則，未經 dry_run 驗證通過的 parser 改動不套用）。

### 問題 3～6：既有已知問題，今日核對現況與 08-11 一致，無新事證

- **問題 3（URC url 指向列表頁 / 空 url）**：現象與修法同 `qa_reports/2026-08-09.md`～`2026-08-11.md` 問題，今日核對比例一致（51.1%／46.7%），未修復。
- **問題 4（Google Alerts 混入新聞媒體網域）**：`state.json` 193 筆快取中 24 筆（12.4%）含新聞媒體網域，與 08-11 的 12.0% 比例一致，未修復。
- **問題 5（國營台鐵地區篩選漏洞）**：`scraper.py:194-196` 核對與前次報告完全相同，`NON_TAIPEI_HINTS` diff 仍未套用，已造成 2 次實際外洩（07-28、08-07）。
- **問題 6（台北自來水處 / 土地銀行出租不動產抓取內容與租賃無關）**：核對 `state.json` 最新快取，台北自來水處近 8 筆全為工程/財物/勞務採購（如「115年安全鞋財物採購」），土地銀行出租不動產近 8 筆全為 ATM／網銀維護、行銷抽獎公告，與 08-11 報告發現完全一致，未修復。

## 三、優化建議

### 建議 1：GLOBAL_FILTER 黑名單未涵蓋「停車場」類關鍵字，屬潛在誤推播風險（新發現，附數據）

- **依據**：全文檢索 `state.json` 全來源快取，共找到 **12 筆以上**雙北地區的停車場/停車位相關標題同時符合現行白名單詞（多數含「標租」），例如：
  ```
  台北市財政局：公開標租臺北市內湖區康寧段四小段452-2地號市有土地作單一停車位使用案
  台北市財政局：公開標租新北市板橋區江子翠段第二崁小段189-5地號臺北市有土地作臨時路外停車場使用案
  政府採購網：本校停車場出租經營管理
  政府採購網：115年停車場標租經營管理案
  ```
  這些項目目前恰好未在 `sent_log.json` 全 31 天記錄中被推播（純屬未被判定為「新增」的僥倖，非規則主動擋下），而業務目標明確排除「停車場」。現行 `GLOBAL_FILTER["blacklist"]` 完全沒有涵蓋此類詞。
- **做法**：於 `scraper.py:69-75` `GLOBAL_FILTER["blacklist"]` 新增停車相關關鍵字：
  ```python
  # scraper.py 第 69-75 行
  "blacklist": [
      "開標結果",
      "自動販賣機", "場地短期出租", "機械類標租", "共享雨傘",
      "新建工程", "統包工程", "物業管理", "專案管理", "保險",
      "清洗作業", "鑄鐵直管", "延性鑄鐵", "塗裝", "管線",
      "財物採購", "勞務採購",
+     "停車場", "停車位", "停車格", "地下停車場委託經營",
  ],
  ```
- **預期效果**：徹底堵住目前僅靠運氣未爆發的停車場誤推播風險，符合「停車場不是目標」的既定業務規則。

### 建議 2：教育部學產基金 / 台北市都發局恢復上線，建議近日安排人工回溯斷線期間公告（延續 08-11 建議，補充今日驗證結果）

- **依據**：問題 1 已確認今日恢復，且教育部學產基金今日立即抓到並成功推播 1 筆與業務目標高度吻合的案件（「115年度臺北永吉段都更案店鋪、住宅國有學產不動產第2次公開標租」），驗證此來源價值真實存在。台北市都發局快取的社會住宅商業設施標租公告同樣 100% 符合業務目標（延續 08-11 報告核對結果）。
- **做法**：既然 API 已恢復且執行穩定，建議儘快（本週內）人工瀏覽 `udd.gov.taipei/events/psxwq1j`、`depart.moe.edu.tw`（教育部學產基金）、`gpwd.mnd.gov.tw`（國防部政治作戰局）三個官網，確認 07-28～08-11 斷線期間是否有已過期、爬蟲從未抓到的公告，避免真實漏接的雙北標租案件。
- **預期效果**：確認 15 天斷線期間的實際業務影響範圍是否為零，並可視需要對外補通知已流失時效的案件。

## 四、待決事項

1. **（新）建議 1 是否核准套用**：`GLOBAL_FILTER["blacklist"]` 新增「停車場／停車位／停車格」等關鍵字，堵住目前僅靠運氣未觸發的誤推播風險。
2. **（新）問題 2（台北市都發局 url 為空）**：本 session 網路出口對 `udd.gov.taipei` 被 egress proxy 阻擋，無法即時 dry-run 驗證。請人工或於可連外環境執行 `dry_run_all_regions.py --debug --source 台北市都發局`，確認是否走 Claude fallback、以及報告中提出的 diff（url 為空時 fallback 到列表頁）是否可套用。
3. 建議 2：是否安排人工回溯 07-28～08-11 斷線期間三個來源官網是否有錯過的公告。
4. 【已造成 2 次實際外洩，多份報告未拍板】問題 5（國營台鐵地區篩選）：`NON_TAIPEI_HINTS` diff 是否核准套用，請儘速決定。
5. 問題 3（URC url）、問題 4（Google Alerts 新聞網域）的既有修法 diff（見 `qa_reports/2026-08-09.md`、`2026-08-11.md`）是否核准套用。
6. 問題 6（台北自來水處、土地銀行出租不動產抓取內容與租賃無關）：是否核准移除這兩個來源，或安排人工確認正確的公告分類頁。
7. 持續觀察 Anthropic API 帳務是否穩定，避免再次被停用導致三來源二度斷線。
