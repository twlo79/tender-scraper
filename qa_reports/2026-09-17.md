# 標案爬蟲監測報告 2026-09-17

## 一、狀況總覽

整體狀態：❌ 有異常（今日執行與 GitHub Actions 本身正常；但問題 1〔國營台鐵地區篩選失效〕今日造成**第 3 次真實外洩**——LINE 已推播 1 筆非雙北案件〔基隆市安樂區西定段326之2地號〕；問題 2～9 全數延續、天數持續累加、皆未修）

最近執行：2026-09-17 01:30（推播 4 筆 / 新增 5 筆 / 抓取 95 筆）

| 檢查項目 | 結果 | 說明 |
|---|---|---|
| 1. 今日已執行 | ✅ | `sent_log.json` 最新 key 為 `2026-09-17 01:30`；GitHub Actions run `35170789697` created_at 2026-09-17T01:30:04Z，與排程時間相符，Make.com 有觸發 |
| 2. 最近一次 GitHub Actions 結論 | ✅ | run `35170789697` `conclusion=success`，job `scrape` 9 個 step 全部 success；近 5 次 run（09-13～09-17，`34730679964`～`35170789697`）全數 success |
| 3. line_pushed 與 total_notify 一致性 | ✅ | `_summary`：`total_fetched=95`、`total_new=5`、`total_notify=4`、`line_pushed=true`，一致，無漏推或空推 |
| 4. 是否所有來源 fetched=0 | ✅ | 否，13 來源中 11 個 >0；國有財產署、政府採購網今日為 0（延續，見問題 2、6） |
| 5. 個別來源連續 3 天以上 fetched=0 | ❌ | 政府採購網：`sent_log.json` 顯示自 09-04 起連續第 **14 天** fetched=0（延續，見問題 2）。國有財產署自 09-16 起連續第 2 天 fetched=0，未達 3 天門檻，列入觀察 |
| 6. 來源抓取量暴跌/暴增 | ⚠️ | 國營台鐵今日 5 筆，較近 7 天均值（約 2.3）暴增逾 2 倍，屬今日新公告量真實增加（非異常，逐筆核對見下）；土地銀行出租不動產 09-16、09-17 連續兩天 f2/n0，打破先前「隔日交替 2/0」型態，屬正向變化非異常；其餘 10 個來源今日數字與 7 天趨勢一致 |
| 7. 應擋卻漏網的案件 | ❌ | **今日推播 4 筆中有 1 筆為誤推**：國營台鐵「【基隆市安樂區西定段326之2地號部分土地(土地供設置電動汽車公共充電樁)】」——基隆市非雙北範圍，因 `parse_tra()`（`scraper.py:197`）僅用「臺北營業分處」轄區判斷、未判斷實際地點，此為問題 1 累計第 3 次真實外洩（前 2 次為 09-01、09-03）。其餘 3 筆（新北市平溪區、臺北市中正區、臺北市北投區）皆為合法雙北案件，url／date 正確 |
| 8. 標題為分類名稱／導覽文而非案名 | ✅ | 今日新增 5 筆標題皆為具體案名，非導覽文。延續性風險仍在：`state.json['國家住宅及都市更新中心']` 快取中除既有「會員中心」外，另核對出「委員名單」「加入會員」共 3 筆非公告導覽文字（因未過白名單所以未曾外洩，但佔用 dedup 300 筆名額，見問題 9 備註） |
| 9. url 指向列表頁而非個別公告頁 | ❌ | 延續（問題 8）：`政府採購網` 300 筆快取中 284 筆（94.7%）url 為空字串；`台北自來水處` 70 筆中 43 筆（61%）url 為空；`國家住宅及都市更新中心` 60 筆中 21 筆（35%）url 為空；`國防部政治作戰局` 11 筆全數（100%）url 為空（結構性，該來源為跨縣市彙總公告無個別頁面）。今日新增項目本身 url 皆正常，問題出在既有快取 |
| 10. date 欄位為空、導致日期窗被跳過放行 | ⚠️ | 今日新增 5 筆 date 欄位均非空。延續性觀察：`台北自來水處` 70 筆快取中 **全數 70 筆（100%）** date 為空（`parse_taipei_water()` 的 `td_map.get("公告日期")` 系列恆抓不到值）；`政府採購網` 300 筆中 284 筆 date 為空；因這兩來源項目多會被黑名單擋下，目前未觀察到因此誤放行的實際案例，但屬潛在風險 |

## 二、異常與處理

以下問題 1～9 編號與 09-16 報告一致，僅更新天數、數字與問題 1 的新事證。

### 問題 1（延續，自 09-03 起第 15 天未修，**今日新增第 3 次真實外洩，優先度最高**）：國營台鐵地區篩選對此來源完全失效

- **現象**：`passes_filters()`（`scraper.py:808-826`）用 `title + agency` 一起比對地區關鍵字，而 `parse_tra()`（`scraper.py:167-197`）僅用「臺北營業分處」是否出現在 `agency+title` 來篩選項目，其轄區橫跨全台（含基隆、桃園等），導致地區篩選對此來源形同虛設。**今日 01:30 執行實際推播**：「【基隆市安樂區西定段326之2地號部分土地(土地供設置電動汽車公共充電樁)】」（`pk=6556`），基隆市不屬雙北，此為累計第 3 次真實外洩（前 2 次：09-01 基隆市安樂區西定段、09-03 桃園市桃園區桃園段武陵小段——同一地段本次疑似為續案）。
- **定位**：`scraper.py:197`（`parse_tra()` 篩選臺北營業分處那一行）。
- **建議修法**（與前次相同，尚未套用）：
  ```diff
       items = [i for i in items if "臺北營業分處" in i.get("agency", "") + i.get("title", "")]
  +    def _tra_is_shuangbei(title: str) -> bool:
  +        parts = re.findall(r"【(.*?)】", title)
  +        loc = parts[2] if len(parts) > 2 else title
  +        return any(k in loc for k in ("台北", "臺北", "新北"))
  +    items = [i for i in items if _tra_is_shuangbei(i["title"])]
       log.info(f"  [國營台鐵] {len(items)} 筆（臺北營業分處）")
       return items
  ```
- **驗證阻礙**：本次監測環境（sandbox）對外部標案來源網域出站連線全域被 proxy 阻擋（今日再次確認：`https://www.railway.gov.tw` 回傳 `ProxyError ... 403 Forbidden`），無法即時重跑 `dry_run_all_regions.py --debug --source 國營台鐵` 驗證；production（GitHub Actions runner）不受此限制。
- **是否已修**：未修。**已連續 3 次真實外洩，建議列為下次修改最高優先**。

### 問題 2（延續，自 09-04 起第 14 天未修）：政府採購網連續 14 天 fetched=0

- **現象**：`sent_log.json` 顯示 09-04～09-17 共 **14 天連續 0 筆**。成因與定位與前次相同，本次因 sandbox 環境出站連線被阻擋，無法重新確認是否為網站改版或查詢詞問題。
- **定位**：`scraper.py:397-462`（`_pcc_fetch_keyword()`），`target_table` 選取失敗時無 log。
- **建議修法**：同 09-16 報告（人工瀏覽器核對 + 補 log diff，未變更）。
- **是否已修**：未修。

### 問題 3（延續，長期未拍板）：`GLOBAL_FILTER` 黑名單缺「停車場」；國有財產署 `skip_global_filter=True` 讓停車場類公告可繞過所有篩選

- **現象**：今日重新核對 `scraper.py:69-75` 黑名單、`scraper.py:337-342` `FNP_PAGES`、`scraper.py:757` `skip_global_filter=True`，三處皆無變化。
- **建議修法**：同 09-16 報告（未變更）。
- **是否已修**：未修。

### 問題 4（延續，長期未拍板；今日 notify=4，ERP 呼叫應已觸發，需人工確認 production log 是否再次噴錯）：安幸 ERP 匯入 header 值含換行字元

- **現象**：`push_to_anxing()`（`scraper.py:1147-1148`）直接用 `os.getenv()` 當 HTTP header 值，未 `.strip()`。今日 `total_notify=4 > 0`，不同於 09-16 的 0 筆提前返回情境，理論上會觸發 ERP 呼叫；因 `daily_report.json` 尚未產生 09-17 資料（log-check.yml 排定約台灣時間 16:30 執行，本報告產出時間 11:35 尚未到），本次無法確認今日是否再次重現此錯誤，建議待 log-check.yml 執行後人工複查。
- **定位**：`scraper.py:1147-1148`。
- **建議修法**：同 09-16 報告（`.strip()` diff，未套用）。
- **是否已修**：未修。

### 問題 5（延續，長期未拍板）：`SOURCES` 中各來源自訂 `whitelist`/`blacklist`/`regions` 從未生效（死碼）

- **現象**：今日重新核對 `passes_filters()`（`scraper.py:808-826`）簽名未變，仍只吃模組層級 `GLOBAL_FILTER`。
- **建議修法**：同 09-16 報告（未變更）。
- **是否已修**：未修。

### 問題 6（延續，自 09-05 起第 14 天）：國有財產署「115年第17批」重複紀錄仍原樣保留在 `state.json`

- **現象**：`state.json['國有財產署']` 現存 8 筆，重複紀錄（x2）仍在，因該來源今日 fetched=0（延續中，見上）未有新資料觸發變化。依規則本次不修改 `state.json`。
- **是否已修**：未修。

### 問題 7（延續，自 09-09 起第 9 天）：`log_checker.py` 讀取 GitHub Actions log 未指定編碼，`daily_report.json` 長期無法正確解析

- **現象**：`daily_report.json` 最新一筆仍為 2026-09-16（`sources={}`、`total_pushed=0`，與該日實際 LINE 推播 0 筆一致，但過去多日已證實此欄位在有推播時會失真，如 09-15 記錄 `total_pushed=0` 但實際推播 3 筆）；09-17 尚未產生（log-check.yml 排程於台灣時間約 16:30，本報告產出時 11:35 尚未執行，非異常延遲，僅為時間點問題）。
- **定位**：`log_checker.py:46-50`（`get_latest_log()` 的 `requests.get()` 未設定 `r.encoding`）。
- **建議修法**：同 09-16 報告（`r.encoding = "utf-8"`，未套用）。
- **是否已修**：未修。

### 問題 8（延續，自 09-10 起第 8 天）：多個來源 `url` 欄位大範圍空白

- **現象**：今日重新統計（見一、狀況總覽第 9 項）：政府採購網 94.7%、台北自來水處 61%、國家住宅及都市更新中心 35%、國防部政治作戰局 100% url 為空，比例與前日相近。
- **驗證阻礙**：sandbox 環境對相關來源網域出站連線與 WebFetch 皆被阻擋，無法取得目前實際 HTML 結構確認根因。
- **建議修法**：同 09-16 報告（未變更）。
- **是否已修**：未修。

### 問題 9（延續，自 09-16 重新核實起第 2 天）：雙北案件標題只寫行政區名（未寫「台北/新北」）被地區篩選誤殺

- **現象**：`GLOBAL_FILTER["regions"]`（`scraper.py:76`）仍僅比對「台北／臺北／新北／瑠公管理處」，未涵蓋雙北各行政區名稱。今日無新案例可驗證（今日新增 5 筆皆有明確市名），但程式碼未變更，缺口仍成立。
- **定位**：`scraper.py:76`。
- **建議修法**：同 09-16 報告（regions 清單擴充 diff，未套用）。
- **是否已修**：未修。

### 備註（非結構性觀察）

- **`_is_junk()` 導覽文防呆函式從未被呼叫**：今日重新核對 `state.json['國家住宅及都市更新中心']`，除既有「會員中心」外，另發現「委員名單」「加入會員」共 3 筆非公告導覽文字佔用 dedup 名額。建議在 `main()` 迴圈內 `items = src["fn"]()` 之後加一行 `items = [i for i in items if not _is_junk(i)]`。
- **土地銀行出租不動產 timeout 型態變化**：09-16、09-17 連續兩天 `fetched=2`，打破先前「隔日交替 2/0」規律，可能是巧合或網站回應變快，非急迫問題，`get()` 的 `timeout=20` 寫死問題仍在（同 09-16 報告 diff）。
- **本次 QA 環境限制**：sandbox 對所有外部標案來源網域出站連線皆被 proxy 阻擋為 403 Forbidden，`WebFetch` 對外部網域一律回傳 `EGRESS_BLOCKED`，屬環境級限制而非個別來源異常；production（GitHub Actions runner）不受影響，今日 run `35170789697` 執行結果不受影響。

## 三、優化建議

### 建議 1（方向 A，優先度提升）：問題 1（國營台鐵）已連續第 3 次造成真實外洩，建議優先於問題 9 單獨排入下次修改

- **依據**：09-01、09-03、**09-17（今日）** 三次真實推播了非雙北案件（基隆市 x2、桃園市 x1），發生頻率約每 8～14 天一次，且每次都是透過 LINE OA broadcast 直接送達使用者，屬已對外造成影響的既成問題，優先度高於同方向但尚未造成外洩的問題 9（漏推）。
- **做法**：套用問題 1 的 `parse_tra()` diff；待有網路權限環境時用 `dry_run_all_regions.py --debug --source 國營台鐵` 驗證抓到筆數與地點判斷正確，並回溯檢查 `state.json['國營台鐵']` 現有快取中還有多少筆非雙北案件已被誤放行（快取本身不修改，但用於估計影響範圍）。
- **預期效果**：國營台鐵不再誤推非雙北案件，消除目前唯一已知會持續造成真實外洩的缺口。

### 建議 2（方向 B，延續 09-15/09-16）：國防部政治作戰局應歸類為「結構性低價值來源」，建議人工拍板是否移除

- **依據**：近 7 天 `notify` 總數為 0（09-11～09-17：fetched 皆為 11，notify 皆為 0），累計 fetched=352+（延續統計），url 100% 空白，快取標題皆為跨縣市年度彙總公告格式（如「公告110年臺北市等12縣市92宗不動產標租」），與同樣零推播但屬「暫無新公告、篩選邏輯正確」的台北市都發局（近 7 天 fetched 穩定 30）性質不同——後者是「沒有新案件」，前者是「有案件但程式結構性無法從彙總標題辨識雙北」。
- **做法**：人工評估是否移除此來源以節省每次執行約 8 秒的抓取時間；若保留，應改為定期人工開啟彙總 PDF 抽查，爬蟲無法自動化此步驟。
- **預期效果**：避免誤判「零推播」來源全部都需要放寬篩選，降低未來為此類來源擴大白名單而引入新誤報的風險。

## 四、待決事項

1. 【延續，自 09-03 起第 15 天，**今日新增第 3 次真實外洩**，優先度最高】問題 1（國營台鐵地區篩選失效）：是否核准 `parse_tra()` 的地點判定 diff？並授權在有網路權限環境驗證？
2. 【延續，自 09-04 起第 14 天】問題 2（政府採購網連續 14 天 fetched=0）：是否請人工用瀏覽器手動核對同組查詢參數，確認是網站改版還是查詢詞過窄？是否核准新增診斷 log 的 diff？
3. 【延續，長期未拍板】問題 3（`GLOBAL_FILTER` 缺「停車場」黑名單 + 國有財產署 `FNP_PAGES` 含停車場分類 + `skip_global_filter`）：三個 diff 是否核准套用？
4. 【延續，長期未拍板；今日 notify=4，建議優先確認】問題 4（安幸 ERP header 值未 `.strip()`）：待今日 log-check.yml 執行後，請人工確認 production log 是否再次出現 header 錯誤；`.strip()` 防禦修法是否核准？是否需重新設定 GitHub Secrets 中的 `ANXING_KEY`／`ANXING_URL`？
5. 【延續，長期未拍板】問題 5（`SOURCES` 逐來源篩選設定從未生效）：產品面是否還需要「逐來源覆蓋」能力？需要則授權重新設計 `passes_filters()`，不需要則授權刪除死碼欄位。
6. 【延續，自 09-05 起】問題 6：國有財產署「115年第17批」在 `state.json` 中的重複紀錄是否需要人工手動清理？
7. 【延續，自 09-09 起】問題 7（`log_checker.py` 編碼錯誤導致 `daily_report.json` 長期無法正確解析、推播統計失真）：是否核准套用 `r.encoding = "utf-8"` 一行修法？
8. 【延續，自 09-10 起】問題 8（多個來源 url 欄位大範圍空白）：是否授權在有網路權限環境進一步除錯？
9. 【延續，自 09-16 起】問題 9（雙北行政區名未涵蓋於 `GLOBAL_FILTER["regions"]`）：是否核准擴充 regions 清單的 diff？
10. 【延續，09-15/09-16 建議 2】國防部政治作戰局（近 7 天 fetched=352+、notify=0，全數為跨縣市彙總公告）是否評估移除？或改為定期人工抽查彙總 PDF？
