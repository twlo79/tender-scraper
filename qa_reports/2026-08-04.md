# 標案爬蟲監測報告 2026-08-04

## 一、狀況總覽

整體狀態：❌ 有異常（Anthropic API 組織停用已連續 **8 天**，3 個核心來源持續斷線；另有多項已知篩選/內容精準度漏洞延續未修，本次新增 2 項具體量化證據）
最近執行：2026-08-04 01:30（推播 1 筆 / 新增 3 筆 / 抓取 58 筆）

| 檢查項目 | 結果 | 說明 |
|---|---|---|
| 1. 今日已執行 | ✅ | `sent_log.json` 最新 key 為 `2026-08-04 01:30`，即今天 |
| 2. 最近一次 GitHub Actions 結論 | ✅ | run `30868932105`（#69），2026-08-04T01:30:04Z，`workflow_dispatch` 觸發，conclusion=`success`；近 7 天（07-29～08-04）全部 `success` |
| 3. line_pushed 與 total_notify 一致性 | ✅ | `line_pushed=true`、`total_notify=1`，一致；重新掃描全部 31 天 sent_log，0 筆不一致 |
| 4. 是否所有來源 fetched=0 | ✅ | 否，本次共抓到 58 筆，非全站失敗 |
| 5. 個別來源連續 3 天以上 fetched=0 | ❌ | 教育部學產基金、台北市都發局、國防部政治作戰局 連續 **8 天**（07-28～08-04）`fetched=0`；今日 job log 三次明確出現 `Claude API 錯誤：invalid_request_error — This organization has been disabled.` |
| 6. 來源抓取量暴跌/暴增 | ✅ | 其餘 10 個來源近 7 天數字穩定（台北市財政局 13-14、瑠公管理處 15、郵局 8、國有財產署 2-3 恆定）。Google Alerts 07-31 出現單日 8 筆（7 天均值約 2.7 筆，約 3 倍），但屬 RSS 來源自然波動，非抓取異常，列為 ⚠️ 觀察即可 |
| 7. 應擋卻漏網的案件 | ⚠️ | 本次 7 天窗口內 4 筆推播無停車場/工程/採購類漏網；但問題 2（國營台鐵地區篩選漏洞）本質仍在，`state.json` 快取中目前仍有 4 筆「臺北營業分處」名下實為新竹/桃園的土地案（見問題 2），隨時可能再度推播 |
| 8. 標題為分類名稱／導覽文而非案名 | ⚠️ | 本次 7 天推播標題本身皆為具體案名，非導覽文；但 `_is_junk()` 防呆函式定義後仍從未被 `main()` 呼叫（死代碼，問題 4 延續），且 Google Alerts 過去曾出現「⋯文件一覽表」類列表頁標題 |
| 9. url 指向列表頁而非個別公告頁 | ⚠️ | `state.json` 重新掃描：台北市都發局 31 筆 `url` 全空、國防部政治作戰局 11 筆全空、教育部學產基金 3 筆全空、政府採購網 300 筆中 289 筆空、國家住宅及都市更新中心 44 筆中 21 筆空、**土地銀行出租不動產 25 筆全空**。這些來源一旦有新案要推播，使用者拿到的會是空連結或列表頁 |
| 10. date 欄位為空導致日期窗被跳過放行 | ⚠️ | `state.json` 重新掃描：**台北自來水處 54 筆全空**、政府採購網 300 筆中 289 筆空、土地銀行 25 筆全空、國防部政治作戰局/教育部學產基金/台北市都發局多筆為空。屬 `is_within_date_window()` 既有設計（解析失敗即放行），非新增風險，但範圍比先前報告記錄的更大（見下方修正說明） |

**修正先前報告的一處錯誤歸因**：08-01、08-02 報告曾將「台北自來水處 date/url 全空」歸因於「走 Claude fallback 路徑遺失欄位」。本次重新核對 `parse_taipei_water()`（`scraper.py:128-162`）程式碼，確認**此函式完全不呼叫 Claude fallback**，是純規則式（`data-title` 屬性比對）解析器。真正原因是原生解析器在目前抓取的頁面上找不到 `data-title="公告日期"` 等欄位（詳見問題 8，本次新發現）。

---

## 二、異常與處理

### 問題 1：Anthropic API 組織已停用，3 個來源連續 8 天斷線（延續自 07-28）

- **現象**：教育部學產基金、台北市都發局、國防部政治作戰局 3 個來源自 07-28 起連續 8 天 `fetched=0`。今日（08-04）job log：
  ```
  01:30:44 WARNING   [教育部學產基金] Claude API 錯誤：invalid_request_error — This organization has been disabled.
  01:30:48 WARNING   [台北市都發局] Claude API 錯誤：invalid_request_error — This organization has been disabled.
  01:30:50 WARNING   [國防部政治作戰局] Claude API 錯誤：invalid_request_error — This organization has been disabled.
  ```
- **定位**：交叉比對 07-27（run `30229945501`，正常抓到 2/30/11 筆，無任何 WARNING）與 07-28（run `30320441872`，起始出現 0/0/0 及 API 錯誤）兩次 GitHub Actions job log，確認斷點精準落在 07-28。`scraper.py` 對這 3 個 parser 最後一次程式碼異動是 07-20（`git log --oneline -- scraper.py`），07-27→07-28 之間無程式碼變更，排除「改碼導致失效」。這 3 個 parser 的原生 `table tbody tr` selector 目前持續回傳 0 筆（見問題 8 同類分析），只要抓不到就會 fallback 到 Claude，而 Claude 端本身已停用，故 100% 變成 0 筆，無論原生 selector 當下狀態如何。
- **建議修法（非程式碼問題，需人工處理）**：
  1. 登入 [Anthropic Console](https://console.anthropic.com) 確認組織停用原因（帳務/合規）並恢復，或更換為新的可用 API Key。
  2. 更新 GitHub repo secret `ANTHROPIC_API_KEY`。
  3. 中期治本：待 API 恢復後，用有網路權限的環境（GitHub Actions runner 或本機，本沙盒環境對外連線全被 proxy 403 擋下）比對這 3 站目前真實 HTML 結構，強化原生 selector 容錯，避免「Claude 一斷、來源全滅」的單點故障重演。
- **是否已修**：未修。已累計 **8 天**，優先度維持最高。

### 問題 2：國營台鐵地區篩選漏洞（延續，尚未修法）

- **現象**：`parse_tra()` 只依「業管單位」欄位是否含「臺北營業分處」篩選，但轄區橫跨全台。`passes_filters()` 的 region 判斷用 `text = title + agency`（`scraper.py:800`），`agency` 恆含「臺北」二字，region 篩選對此來源形同虛設。今日重新掃描 `state.json`，「臺北營業分處」名下仍有 4 筆非雙北標的仍在快取中：新竹縣竹東鎮、桃園市楊梅區、新竹市香山區、新竹縣新豐鄉；其中新竹縣新豐鄉一案已於 07-28 實際推播。
- **定位**：`scraper.py` `parse_tra()` 第 194-196 行；`passes_filters()` 第 796-814 行。
- **建議修法**：
  ```python
  # scraper.py，parse_tra() 內，「只保留臺北營業分處的標案」之後加一段
  NON_TAIPEI_HINTS = ["新竹市", "新竹縣", "桃園", "苗栗", "台中", "臺中", "彰化",
                       "南投", "雲林", "嘉義", "台南", "臺南", "高雄", "屏東",
                       "宜蘭", "花蓮", "台東", "臺東", "基隆"]
  items = [i for i in items if "臺北營業分處" in i.get("agency", "") + i.get("title", "")]
  items = [i for i in items if not any(h in i["title"] for h in NON_TAIPEI_HINTS)]  # 新增
  log.info(f"  [國營台鐵] {len(items)} 筆（臺北營業分處）")
  ```
- **是否已修**：未修。

### 問題 3：Google Alerts 混入房產新聞報導，非官方公告（延續）

- **現象**：31 天 sent_log 掃描到至少 2 筆推播為新聞媒體報導而非官方標案公告：
  - 07-26：「租期最長20年！國有房地標租續租新制上路台北星鑽20戶7月底打頭陣 - 樂屋網房產新聞」
  - 08-03：「北市議會舊址無償提供使用2323坪促參招商中議員：勿淪為商場| 大台北 - 聯合新聞網」
  兩者皆為媒體對政策/議題的報導，非特定案件的招標公告。
- **定位**：`parse_google_alerts()`（`scraper.py:627-663`）未對來源網域做任何過濾。
- **建議修法**：
  ```python
  # scraper.py，parse_google_alerts() 內，加入 unique 前的過濾
  NEWS_DOMAIN_BLACKLIST = ["udn.com", "housefun.com.tw", "ltn.com.tw", "chinatimes.com", "ettoday.net"]
  items = [i for i in items if not any(d in i["url"] for d in NEWS_DOMAIN_BLACKLIST)]
  ```
- **是否已修**：未修。

### 問題 4：`_is_junk()` 防呆函式定義後從未被呼叫（死代碼，延續）

- **現象**：`scraper.py:113-114` 定義了 `_is_junk(item)`，但 `main()`（第 1142-1157 行）從未呼叫它。
- **建議修法**：
  ```python
  notify_items = [
      i for i in new_items
      if (skip_filters or passes_filters(i)) and is_within_date_window(i) and not _is_junk(i)  # 加入
  ]
  ```
- **是否已修**：未修。

### 問題 5：`SOURCES` 內各來源自訂 `whitelist`/`blacklist`/`regions` 為死代碼（延續）

- **現象**：`SOURCES`（`scraper.py:703-793`）多個來源定義了自訂 `whitelist`/`blacklist`/`regions`，但 `passes_filters(item)` 簽名只吃 `item`，完全沒有讀取這些欄位——包含 Google Alerts 註解寫明想放行「網域為 gov.tw 的全台政府公告」，這個設計意圖從未真正生效。
- **建議修法**：讓 `passes_filters` 接受來源層級覆寫：
  ```python
  def passes_filters(item: dict, src: dict | None = None) -> bool:
      title  = item.get("title", "")
      agency = item.get("agency", "")
      text   = title + agency
      regions   = (src or {}).get("regions")   or GLOBAL_FILTER.get("regions", [])
      whitelist = (src or {}).get("whitelist") or GLOBAL_FILTER.get("whitelist", [])
      blacklist = list(GLOBAL_FILTER.get("blacklist", [])) + list((src or {}).get("blacklist", []))
      ...
  # 呼叫處改為 passes_filters(i, src)
  ```
  需先決定「來源 `regions`/`whitelist` 為空列表」的語意（見待決事項）。
- **是否已修**：未修。

### 問題 6：國有財產署「停車場」類別繞過黑名單（延續）

- **現象**：`FNP_PAGES` 內建 4 個類別，其中含 `"停車場"`；`SOURCES` 中國有財產署設定 `"skip_global_filter": True`，完全繞過 `GLOBAL_FILTER` 黑名單。目前該類別持續回傳 0 筆，尚未實際造成誤推，但屬潛在風險。
- **建議修法**：從 `FNP_PAGES` 移除停車場類別。
- **是否已修**：未修。

### 問題 7：土地銀行出租不動產 parser 抓到站內公用公告，非租賃清單（延續，本次重新以獨立數據驗證）

- **現象**：重新掃描 `state.json`「土地銀行出租不動產」快取的全部 25 筆，**0 筆（0%）標題含任何 `GLOBAL_FILTER.whitelist` 關鍵字**（出租/標租/租賃/招租/房地/不動產等），且 25 筆全數也同時無法通過地區篩選——內容清一色是「晶片金融卡跨國提款服務公告」「ATM 停止服務公告」「網路銀行系統維護作業」等銀行站內公用公告，與「出租不動產」完全無關。31 天彙總 fetch=62、new=13、notify=0，轉換率 0%。
- **定位**：`parse_landbank()`（`scraper.py:592-618`）的 CSS 選擇器 `table tbody tr, table tr, .bulletin-item, .list-item, article` 過於寬泛，疑似命中頁面共用的站內公告區塊，而非 `/Bulletin/RentRealty` 頁面實際的租賃清單內容區。
- **建議修法**：需先用有網路存取權限的環境（本沙盒對 `landbank.com.tw` 的連線被 proxy 403 阻擋，無法即時比對 HTML）確認 `/Bulletin/RentRealty` 頁面實際的清單容器 class/id，將選擇器收斂到該容器內，而非全站通用 `table tr`。在確認前，暫不建議直接下架此來源——土地銀行持有大量公股不動產，理論上是高相關性來源，值得修，而非移除。
- **是否已修**：未修，需先取得現場 HTML 結構才能寫出準確選擇器。

### 問題 8（新發現）：台北自來水處抓到一般採購公告頁，非「出租/招租」專屬頁面

- **現象**：重新掃描 `state.json`「台北自來水處」快取的全部 54 筆，**0 筆（0%）標題含任何 `GLOBAL_FILTER.whitelist` 關鍵字**。內容清一色是「管網改善工程採購」「清洗作業勞務採購」「財物採購」等一般標案（其中多筆標題本身就含 GLOBAL_FILTER 黑名單詞「新建工程」「清洗作業」「財物採購」「勞務採購」，只是尚未走到黑名單判斷就已被白名單擋下）。31 天彙總 fetch=55、new=2、notify=0，轉換率 0%。同時，`date` 欄位 54/54 全為空——與問題 7 的診斷方式相同，`parse_taipei_water()`（`scraper.py:128-162`）是純規則解析器（**不經過 Claude fallback**，先前 08-01/08-02 報告誤植為 Claude fallback 所致，已於本報告第一節更正），日期欄位空白代表目前抓取的頁面（`n=D2818696FF5048B8`）裡的 `data-title` 標籤與程式預期的「公告日期」不符，很可能是因為這個頁面本身就是自來水處的**一般招標公告總表**，而非「出租/招租」的專屬子分類頁。
- **定位**：`parse_taipei_water()`（`scraper.py:128-162`）、`SOURCES` 中 `台北自來水處` 的 `url`（`scraper.py:706`）。
- **建議修法**：需先用有網路存取權限的環境確認 `water.gov.taipei` 網站是否存在「出租/招租」專屬分類頁（不同的 `n=` 參數），若有則改 URL 指向該頁；若該處水處官網本身沒有獨立的不動產出租公告分類（可能混雜在總表內），則應評估是否值得繼續維護此來源，或至少放寬 `data-title` 比對欄位（目前只認「公告日期」「發布日期」「日期」三種標籤字串）。
- **是否已修**：未修，需先取得現場 HTML 結構確認正確 URL。

---

## 三、優化建議

### 方向 A：篩選精準度

**建議 1（最高優先）：先解決問題 1，並補強 3 個 Claude-only 來源的原生 parser 容錯**

- 依據：07-27 之前這 3 個來源原生 parser 穩定產出 2/30/11 筆，證明平時不需要 Claude 也能運作；Claude 中斷後已連續 8 天全滅，無備援。
- 做法：問題 1 解決後，確認原生 selector 是否仍有效，並強化容錯範圍，把「必須依賴 Claude 才有數字」的來源數量降到 0。
- 預期效果：未來 Claude API 若再中斷，不會讓 3 個來源同時斷線超過 1 天。

**建議 2：修問題 7、8（土地銀行、台北自來水處內容完全不相關）——這是本次最具體的精準度落差證據**

- 依據：兩個來源合計 31 天 fetch 117 筆、new 15 筆，notify 皆為 **0**，且逐筆核對後 **0% 命中任何白名單關鍵字**——不是「篩選過嚴擋掉真實案件」，而是**來源本身抓到完全不相關的內容**（土地銀行抓到 ATM/晶片卡公告；台北自來水處抓到工程/財物採購公告），連白名單第一關都進不去。這與國家住宅及都市更新中心（fetch 314、new 2、notify 0，但逐筆核對後皆是合理的非雙北/非租賃案件，篩選正確運作）性質不同，必須分開處理。
- 做法：優先修這兩個來源的 URL/selector（問題 7、8 已附初步定位），若短期無法連網確認 HTML 結構，兩個來源目前照常抓取不會造成誤推（因為連白名單都過不了），可以先擱置、不影響推播正確性，只是持續浪費爬取資源。
- 預期效果：若修復成功，土地銀行、台北自來水處有機會開始貢獻真實推播；若確認網站無對應分類頁，則有依據直接下架，不再浪費每日抓取資源。

**建議 3：問題 2（台鐵地區篩選漏洞）——已有 2 次實際外洩紀錄（07-17、07-28）**

- 依據：`state.json` 中「臺北營業分處」名下目前仍有 4 筆非雙北案件在快取中，皆因 agency 欄位恆含「臺北」而繞過 region 篩選。
- 做法：套用問題 2 提供的 diff，套用後以 `dry_run_all_regions.py --debug --source 國營台鐵` 驗證（需在有網路權限的環境執行）。
- 預期效果：消除已確認發生過 2 次的誤推類型。

### 方向 B：來源健康度

**建議 4：31 天 notify=0 的來源需按「內容不相關」vs「案件本身稀少」分兩類處理**

- 依據（實際數字，31 天窗口）：
  | 來源 | fetch | new | notify | 診斷 |
  |---|---|---|---|---|
  | 台北市都發局 | 660 | 0 | 0 | 案件本身極穩定（同一批公告反覆出現），非篩選問題 |
  | 郵局房地產出租 | 286 | 4 | 0 | 全國性來源，new 案件多為中南部，篩選正確運作 |
  | 國家住宅及都市更新中心 | 314 | 2 | 0 | 逐筆核對篩選正確（新建工程/物業管理/停車場皆應排除） |
  | 土地銀行出租不動產 | 62 | 13 | 0 | **內容完全不相關（問題 7），需修 selector** |
  | 台北自來水處 | 55 | 2 | 0 | **內容完全不相關（問題 8），需修 URL/selector** |
  | 教育部學產基金 | 53 | 1 | 0 | 主要受問題 1（Claude 停用）影響，非篩選問題 |
- 做法：土地銀行、台北自來水處列為「修 selector」候選（問題 7、8）；都發局、郵局、國家住宅及都市更新中心維持現狀繼續觀察，不建議移除（篩選邏輯本身正常，只是案件天然稀少）。
- 預期效果：避免把「篩選正常但案件稀少」與「抓錯內容」兩種不同成因混為一談而做出錯誤的移除/保留決策。

### 方向 C：新增來源

本次略過。問題 1（Claude API，已達 8 天未解，影響範圍最大）與問題 2～8 均未修復，在既有問題解決並觀察至少一週成效之前，新增未驗證來源的邊際效益低於修復既有問題。

---

## 四、待決事項

1. **【最優先，已連續 8 天】Anthropic API 組織已停用**（問題 1）：需人工登入 Anthropic Console 確認並恢復，或更換新的可用 API Key，並更新 GitHub repo secret `ANTHROPIC_API_KEY`。
2. 問題 2～8 的修法 diff 是否核准套用——已連續多日提出但尚未有人拍板。
3. 問題 7（土地銀行）、問題 8（台北自來水處）需要有網路存取權限的環境（GitHub Actions runner 或本機）連上實際網站確認目前 HTML 結構/正確 URL，才能寫出準確的 selector 修法；本沙盒環境對外連線（含 WebFetch）全數被 proxy 或站方回傳 403 阻擋，无法即時比對。
4. 問題 5（`SOURCES` 死代碼欄位）處理方向：(a) 直接移除從未生效的欄位，或 (b) 讓 `passes_filters` 真正套用來源層級覆寫（需先決定空列表的語意）。
5. 國有財產署「地上權設定」類別是否納入業務範圍，需人工判斷。
6. 本次環境限制說明：本環境對外連線（含 WebFetch）皆被 proxy 或站方回傳 403（`www.water.gov.taipei`、`www.railway.gov.tw`、`www.ialgo.nat.gov.tw`、`www.post.gov.tw`、`dof.gov.taipei`、`udd.gov.taipei`、`depart.moe.edu.tw`、`gpwd.mnd.gov.tw` 等皆確認被擋），無法執行 `dry_run_all_regions.py` 的即時抓取驗證，僅能透過 `sent_log.json`、`state.json`、GitHub Actions 實際 job log（跨 07-15～08-04 多筆歷史 log 交叉比對）與程式碼靜態分析得出結論。
