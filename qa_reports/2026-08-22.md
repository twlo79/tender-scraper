# 標案爬蟲監測報告 2026-08-22

## 一、狀況總覽

整體狀態：⚠️ 有注意事項（今日執行本身正常、無新增誤報，但 08-20 曾造成真實外洩的黑名單／地區篩選缺口，**連續第 3 天未修，風險持續存在**）
最近執行：2026-08-22 01:30（推播 0 筆 / 新增 0 筆 / 抓取 96 筆）

| 檢查項目 | 結果 | 說明 |
|---|---|---|
| 1. 今日已執行 | ✅ | `sent_log.json` 最新 key 為 `2026-08-22 01:30`，即今天 |
| 2. 最近一次 GitHub Actions 結論 | ✅ | run `32543611365`，2026-08-22T01:30:09Z，`workflow_dispatch` 觸發，`conclusion=success`；近 87 次歷史 run 全數 `success` |
| 3. line_pushed 與 total_notify 一致性 | ✅ | `_summary`：`total_fetched=96`、`total_new=0`、`total_notify=0`、`line_pushed=false`，一致 |
| 4. 是否所有來源 fetched=0 | ✅ | 否，13 來源全數有抓到資料（合計 96 筆），無 `note` 標記 |
| 5. 個別來源連續 3 天以上 fetched=0 | ✅ | 無來源連續 3 天以上 0 筆 |
| 6. 來源抓取量暴跌/暴增 | ⚠️ | 12/13 來源比值在 0.64x～1.50x 之間，正常；僅 Google Alerts 今日 1 筆 vs 7 日均值 2.43 筆，比值 0.41x（低於 0.5 門檻），但屬新聞類低樣本來源正常波動，非 parser 異常 |
| 7. 應擋卻漏網的案件 | ✅（今日）/ ❌（缺口未修） | 今日**無新增誤報**（推播 0 筆）。但逐行核對 `scraper.py:69-75`、`scraper.py:194-196`，確認 08-21 報告標記的三處缺口（停車場黑名單、台鐵地區篩選、決標結果黑名單）**程式碼完全未變更**（`git log 7515590..HEAD -- scraper.py` 無任何 commit，`git diff` 無差異）。`state.json` 快取中仍有 4 筆「臺北營業分處」標題含新竹縣/市字樣、4 筆「臺北營業分處」含「停車場」的舊案（詳見問題 1、2），只要下次抓到同型新案，仍會重演 08-20/08-18/08-17 的外洩 |
| 8. 標題為分類名稱／導覽文而非案名 | ⚠️ | `state.json` 中「國家住宅及都市更新中心」快取仍有「會員中心」「委員名單」「加入會員」3 筆導覽文（與前次相同，無新增），比對 `sent_log.json` 全部記錄，從未被實際推播（不含白名單詞而被攔截），僅為快取雜訊 |
| 9. url 指向列表頁而非個別公告頁 | ❌ | `state.json` 全庫 796 筆中 463 筆（58.2%）url 為空字串，其中政府採購網 95.3%、教育部學產基金 100%、國防部政治作戰局 100%、台北市都發局 100%、台北自來水處 75.4%（與 08-21 的 58.2% 完全持平，無變化） |
| 10. date 欄位為空導致日期窗被跳過放行的項目 | ❌ | 全庫 796 筆中 462 筆（58.0%）date 為空；台北自來水處 100%（57/57）、土地銀行出租不動產 100%（37/37）、政府採購網 95.3%（286/300）幾乎全空（與 08-21 的 58.0% 完全持平） |

## 二、異常與處理

### 問題 1（延續，08-20 曾造成真實外洩，**連續第 3 天未修**）：GLOBAL_FILTER 黑名單缺「停車場」類關鍵字

- **現象**：`scraper.py:69-75` 目前仍只有 `"開標結果", "自動販賣機", "場地短期出租", "機械類標租", "共享雨傘", "新建工程", "統包工程", "物業管理", "專案管理", "保險", "清洗作業", "鑄鐵直管", "延性鑄鐵", "塗裝", "管線", "財物採購", "勞務採購"`，未包含「停車場」類關鍵字。本次額外掃描 `state.json` 全庫，確認除了 08-20 造成外洩的台鐵鶯歌案之外，快取中還有多筆停車場類案件（台北市財政局 2 筆、國家住宅及都市更新中心 6 筆、政府採購網 2 筆、郵局房地產出租 1 筆），只要符合白名單詞（如「出租」「標租」）且是雙北案件，隨時可能再次外洩。
- **定位**：`scraper.py:69-75`（`GLOBAL_FILTER["blacklist"]`）。
- **建議修法**：
  ```diff
       "blacklist": [
           "開標結果",
  -        "自動販賣機", "場地短期出租", "機械類標租", "共享雨傘",
  +        "自動販賣機", "場地短期出租", "機械類標租", "共享雨傘",
  +        "停車場", "停車位", "停車格", "機械式停車",
           "新建工程", "統包工程", "物業管理", "專案管理", "保險",
           "清洗作業", "鑄鐵直管", "延性鑄鐵", "塗裝", "管線",
           "財物採購", "勞務採購",
       ],
  ```
- **是否已修**：未修。**已連續 3 份報告（08-20、08-21、08-22）標記為最高優先，建議儘速拍板套用。**

### 問題 2（延續，08-18 曾造成真實外洩，**連續第 3 天未修**）：國營台鐵「臺北營業分處」地區篩選誤判

- **現象**：`scraper.py:194-196` 的 `parse_tra()` 目前仍為 `items = [i for i in items if "臺北營業分處" in i.get("agency", "") + i.get("title", "")]`，只以管理單位名稱篩選。本次掃描 `state.json` 國營台鐵全部 33 筆快取，發現標題含「臺北營業分處」但實際落在新竹縣/市的案件有 **4 筆**（竹東鎮、香山區、新豐鄉×2、湖口鄉），皆因標題含「臺北」二字被 `GLOBAL_FILTER["regions"]` 誤判為雙北案件而放行，僅因未含白名單詞未被推播，漏洞邏輯確認仍在。
- **定位**：`scraper.py:194-196`。
- **建議修法**：
  ```diff
       a = item.select_one("div.btn-box a[href]")
       href = urljoin(BASE, a["href"]) if a else URL
       items.append({"title": title, "date": dt, "url": href, "agency": unit})
  -    # 只保留臺北營業分處的標案
  -    items = [i for i in items if "臺北營業分處" in i.get("agency", "") + i.get("title", "")]
  +    # 只保留臺北營業分處管轄「且」案件座落雙北的標案
  +    # 臺北營業分處轄區橫跨新竹、花蓮等縣市，僅比對單位名無法排除外縣市案件，需再依標題地名二次篩選
  +    NON_TAIPEI_COUNTIES = ["新竹縣", "新竹市", "苗栗", "宜蘭", "花蓮", "台中", "臺中", "台東", "臺東"]
  +    items = [
  +        i for i in items
  +        if "臺北營業分處" in i.get("agency", "") + i.get("title", "")
  +        and not any(c in i.get("title", "") for c in NON_TAIPEI_COUNTIES)
  +    ]
       log.info(f"  [國營台鐵] {len(items)} 筆（臺北營業分處）")
       return items
  ```
- **是否已修**：未修。**已連續多份報告記錄同一未拍板缺口，建議與問題 1 一併拍板。**

### 問題 3（延續，08-17 曾造成真實外洩，**連續第 3 天未修**）：GLOBAL_FILTER 黑名單未涵蓋「決標結果」

- **現象**：`scraper.py:69-75` blacklist 仍只有「開標結果」，未含「決標結果」。`state.json` 中 Google Alerts 快取仍有 1 筆「新北市萬里區設置社區式長照機構」決標結果公告未被此規則擋下（僅因未含白名單詞未推播）。
- **建議修法**：
  ```diff
       "blacklist": [
  -        "開標結果",
  +        "開標結果", "決標結果",
           ...
       ],
  ```
- **是否已修**：未修。

### 問題 4（延續，長期未拍板）：url/date 欄位大量空白

- **現象**：見狀況總覽第 9、10 項，全庫 58.2%／58.0% 欄位空白，與 08-21 報告完全持平（無改善也無惡化）。主要集中在台北自來水處、政府採購網、教育部學產基金、國防部政治作戰局、台北市都發局、土地銀行出租不動產 6 個來源。本 session 網路出口再次阻擋所有來源網域（`www.railway.gov.tw` 等回傳 `ProxyError 403 Forbidden`），無法即時核對正確 selector，沿用歷次建議：需在可連線環境重新確認各站表格欄位標籤並逐一修正。
- **是否已修**：未修。

### 問題 5（延續，長期未拍板）：土地銀行出租不動產／台北自來水處抓取內容與業務目標不符

- **現象**：近 7 天 `土地銀行 fetched_sum≈14, notify_sum=0`；`台北自來水處 fetched_sum≈21, notify_sum=0`。與前次報告一致，兩來源持續 0 相關產出。
- **建議修法**：二選一：移除來源，或改抓正確的「不動產出租」分類頁（需人工在有網路權限環境確認正確 URL）。
- **是否已修**：未修。

## 三、優化建議

### 建議 1（方向 A）：問題 1、2、3 的 diff 已連續 3 天待拍板，建議立即套用降低外洩風險

- **依據**：`state.json` 全庫快取中已確認命中三項缺口的舊案共 12+ 筆（停車場類 8 筆跨 4 個來源、台鐵新竹案 4 筆、決標結果 1 筆），這些案件目前僅因未含白名單詞而未推播，屬僥倖而非防禦。08-17、08-18、08-20 已分別實際外洩過決標結果、台鐵外縣市案、台鐵停車場案三種型態，diff 三者互不衝突可一次套用。
- **做法**：套用「二、異常與處理」問題 1、2、3 的 diff。
- **預期效果**：排除已確認的 3 類誤報型態，消除已知外洩風險。

### 建議 2（方向 B）：土地銀行／自來水處連續多份報告 0 相關產出，建議儘速二選一拍板

- **依據**：土地銀行近 7 天 `notify_sum=0`（快取累計 37 筆，抽查內容多為系統維護公告等，非雙北租賃標案）；台北自來水處近 7 天 `notify_sum=0`（快取累計 57 筆，且 `date` 欄位 100% 空白，日期窗形同虛設）。
- **做法**：兩來源各自決定移除或改抓正確分類頁；其餘 11 個低產出但非零 fetched 的來源（如台北市都發局 fetched 210+、瑠公 15/日）經歷次報告確認屬「官方公告本身低頻更新」，非篩選過嚴，維持現狀。
- **預期效果**：減少每日抓取但完全無效的 2 個來源，降低維運雜訊。

## 四、待決事項

1. **【連續 3 份報告未拍板，08-20 已造成真實外洩】問題 1（GLOBAL_FILTER 黑名單缺「停車場」類關鍵字）**：diff 是否核准套用？最高優先。
2. **【連續多份報告未拍板，08-18 曾造成真實外洩】問題 2（國營台鐵地區篩選誤判）**：diff 是否核准套用？
3. **【延續，08-17 曾造成真實外洩】問題 3（GLOBAL_FILTER 黑名單缺「決標結果」）**：diff 是否核准套用？
4. **【長期未拍板】問題 4（url/date 欄位大量空白）**：是否安排人工在有網路權限環境重新核對各站 HTML 結構並修正 parser？
5. **【長期未拍板】問題 5（土地銀行/自來水處來源與業務目標不符）**：兩來源是否移除，或安排人工確認正確的公告分類頁？請明確二選一。
6. 國有財產署「地上權設定」類別是否納入業務範圍，需人工判斷（延續多份前次報告的待決事項）。
7. 08-20 報告提出的「攤位標租」類案件（公有零售市場攤位）是否納入業務範圍，仍待人工確認。
8. 本 QA routine 執行環境的網路出口政策是否調整，以便「階段二」除錯能做即時 HTML 驗證。
