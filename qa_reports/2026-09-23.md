# 標案爬蟲監測報告 2026-09-23

## 一、狀況總覽

整體狀態：❌ 有異常（問題 1、3、4、5、6、7、8、9、10、11、13、14、15 全數延續未修，天數持續累加；**本次新增 3 項問題（16、17、18），皆為靜態程式碼比對 `state.json` 累積快取後新發現，非今日新執行觸發**）

**重要時序說明**：本報告產生於 2026-09-23 01:1x UTC（台灣時間約 09:1x）。核對 `daily.yml` 近 6 次 run 記錄，Make.com 觸發時間穩定落在 UTC 01:30 附近（台灣時間約 09:30），**今日排程尚未到觸發時間**，`sent_log.json` 最新一筆仍是 09-22 01:30，非執行失敗。本次「一、狀況總覽」多數執行層面數字沿用 09-22 report 的最終結果；品質層面（問題 16-18）則是本次新做的靜態分析成果。**建議待決事項新增一項：本 QA routine 的排程時間應晚於 daily.yml 實際觸發時間（例如 UTC 02:15 之後），否則「今日已執行」欄位每天都會顯示尚未執行。**

最近執行：2026-09-22 01:30（推播 2 筆 / 新增 17 筆 / 抓取 105 筆）— 今日尚無新執行

| 檢查項目 | 結果 | 說明 |
|---|---|---|
| 1. 今日已執行 | ✅（時序正常） | `sent_log.json` 最新 key 仍為 `2026-09-22 01:30`；GitHub Actions 最近一次 run（`35675978712`）`created_at=2026-09-22T01:30:03Z`。核對近 6 次 run，皆由 `workflow_dispatch` 於 UTC 01:30 附近觸發；現在（UTC 01:1x）尚未到今日觸發時間，非 Make.com 未觸發 |
| 2. 最近一次 GitHub Actions 結論 | ✅ | run `35675978712`（`daily.yml` run #119）`conclusion=success`；近 6 次 run（09-17～09-22）全數 `success` |
| 3. line_pushed 與 total_notify 一致性 | ✅ | 近 7 天逐日核對 `sent_log.json`：`total_notify>0` 時 `line_pushed=true`（09-19/21/22），`total_notify=0` 時 `line_pushed=false`（09-18/20），無不一致 |
| 4. 是否所有來源 fetched=0 | ✅ | 否，09-22 當日 13 個來源全數 `fetched>0`（合計 105 筆） |
| 5. 個別來源連續 3 天以上 fetched=0 | ✅ | 目前無來源處於連續掛零狀態（政府採購網已於 09-22 中止 18 天 streak，國有財產署已連續 2 天恢復非零） |
| 6. 來源抓取量暴跌/暴增 | ⚠️ | 近 7 天無單日暴跌（>50%）或暴增（>3倍）達標，但**台北自來水處 09-17→09-22 由 4 筆穩定成長至 12 筆**，經本次深入比對 `state.json` 的 `id` 欄位序列，發現其中隱藏一個既有推播機制的缺陷（見新增問題 16），並非單純業務成長 |
| 7. 應擋卻漏網的案件 | ❌ | 延續問題 1：國營台鐵地區篩選對此來源完全失效（歷史已有 09-17「基隆市安樂區西定段…」真實外洩案例），根因與修法見問題 1 |
| 8. 標題為分類名稱／導覽文而非案名 | ⚠️ | 本次新查：`state.json['國家住宅及都市更新中心']` 中有 1 筆標題為純導覽文「會員中心」（`date` 空白），因不含任何 `GLOBAL_FILTER` 白名單詞而從未被推播，屬於「parser 撿到雜訊但被白名單擋住」的低風險雜訊，非漏網 |
| 9. url 指向列表頁而非個別公告頁 | ❌ | 本次新發現：`state.json['國家住宅及都市更新中心']` 60 筆快取中，**38 筆（63%）** `url` 等於清單頁本身（`https://www.hurc.org.tw/hurc/procurement`），非個別公告頁，見新增問題 17 |
| 10. date 欄位為空、導致日期窗被跳過放行 | ❌ | 本次逐來源統計 `state.json`：台北自來水處 **103/103（100%）**、土地銀行 **43/43（100%，延續問題 10）**、政府採購網 **283/300（94%）**、HURC **21/60（35%）** 皆為空白日期，見新增問題 18 |

## 二、異常與處理

> **環境限制說明**：本次 sandbox 對所有目標網站（`water.gov.taipei`、`railway.gov.tw`、`landbank.com.tw`、`google.com` 等）的出站連線皆被 agent proxy 以 403 Forbidden 阻擋，無法執行即時 `dry_run_all_regions.py`。本次改採：① 讀取 GitHub Actions API 取得近 6 次 run 的 metadata（時間、結論）；② 直接分析 repo 內 `state.json`（累積最近 300 筆快取，含每來源歷史抓取內容）與 `sent_log.json`（近 30 天執行摘要）的實際資料；③ 通讀 `scraper.py` 相關 parser／篩選／去重程式碼，交叉比對假說與實際快取數據，找出 3 項先前報告未記錄的新問題（16、17、18）；④ 執行 `DRY_RUN=true python scraper.py` 確認程式碼本身無例外、可正常啟動至結束（僅網路連線被 sandbox 阻擋，13 個來源皆 `WARNING GET 失敗`，非程式錯誤）。

問題編號延續 09-22 報告。以下僅列**有新資訊**或**新增**的問題；問題 6、12、15 因無今日新執行資料，狀態原樣延續，不重複列出（請見 09-22 報告或四、待決事項）。

### 問題 1（延續，自 09-03 起第 21 天，累計 3 次真實外洩，優先度最高）：國營台鐵地區篩選對此來源完全失效

- **現象**：`parse_tra()`（`scraper.py:167-199`）用「臺北營業分處」是否出現在 `agency+title` 決定是否收錄（第 197 行），但**該分處轄區橫跨基隆、宜蘭等非雙北地區**。更嚴重的是，本次逐行核對發現：該分處名稱同時被寫入 `title` 本身（原始標題固定格式為「【臺北營業分處】【土地出租】【地點】【日期】」）與 `agency` 欄位（`unit` = 業管單位文字，同樣是「臺北營業分處」）。`passes_filters()`（`scraper.py:826-827`）的地區檢查是 `text = title + agency`，只要含「臺北」子字串就放行——而「臺北營業分處」本身就含「臺北」，等於**此來源的地區篩選對任何項目都必然通過，與物件實際所在城市完全無關**。09-17 已有真實外洫案例佐證（推播「基隆市安樂區西定段326之2地號部分土地(土地供設置電動汽車公共充電樁)」，非雙北）。
- **定位**：`scraper.py:183-198`。
- **建議修法**（本次新增更貼近根因的版本，與 09-22 報告的方案並列供人工擇一）：
  ```diff
           a = item.select_one("div.btn-box a[href]")
           href = urljoin(BASE, a["href"]) if a else URL
           items.append({"title": title, "date": dt, "url": href, "agency": unit})
       # 只保留臺北營業分處的標案
       items = [i for i in items if "臺北營業分處" in i.get("agency", "") + i.get("title", "")]
  +    # 分處轄區含基隆、宜蘭等非雙北地區，僅用來決定是否收錄；篩完後把
  +    # 「臺北營業分處」字樣從 title/agency 清掉，避免這串固定文字讓
  +    # GLOBAL_FILTER 的地區關鍵字誤判為雙北案件（實際地點應看 title 內
  +    # 「【地點】」括號中的市/縣名，如「基隆市」「新北市」）
  +    for i in items:
  +        i["title"] = i["title"].replace("【臺北營業分處】", "").strip()
  +        i["agency"] = ""
       log.info(f"  [國營台鐵] {len(items)} 筆（臺北營業分處）")
       return items
  ```
  兩個方案本質相同（都是不讓「臺北營業分處」字樣污染地區判斷），此版本改動面較小，僅動 `parse_tra()` 內部，不需額外定義新函式。
- **是否已修**：未修。

### 問題 4、問題 2、問題 12（延續，**今日無新執行資料可供確認**）

- ANXING ERP header 未 `.strip()`（`scraper.py:1159-1160`，同 09-22 核對未變）、政府採購網 18 天空窗期後第 2 天是否維持非零、國有財產署連續第 3 天是否維持恢復，三者皆需**今日實際 daily.yml 執行後**才能確認，目前僅能確認程式碼本身未變更。待下次執行後的 QA 檢查補上。

### 問題 16（**新增**）：台北自來水處「編號」欄位為頁面相對排序，新項目插入會讓舊項目被誤判為「新增」，有重複推播風險

- **現象**：`parse_taipei_water()`（`scraper.py:128-161`）把網頁上的「編號」欄位直接當作 `id` 存入 `case_id`（第 159-160 行），而 `item_key()`（`scraper.py:991-996`）把 `id` 一併算進去重 key（`id+title+date+url`）。比對 `state.json['台北自來水處']` 中 09-21 與 09-22 兩次快照：09-21 快照裡「116年直潭場淤泥處理廠委外操作維護勞務採購」等 3 筆的 `id` 為 `9/10/11`；09-22 新增「新北市新店區安德段923、928、929及930地號部分土地公開招租」被插入清單第 1 位後，**同 3 筆項目（title、url 完全相同）的 `id` 全部順移為 `10/11/12`**。因為 `id` 是 `item_key()` 的一部分，這 3 筆（以及該次抓取的全部 11 筆舊項目）在 09-22 都被 `find_new_items()` 誤判為「新增」——這正是 `_summary` 顯示「台北自來水處 fetched=12, new=12」的真實原因（實際只有 1 筆是真正的新案）。本次 09-22 因為那 11 筆誤判「新增」的項目標題都含「財物採購」「勞務採購」「延性鑄鐵」等 `GLOBAL_FILTER` 黑名單詞，被 `passes_filters()` 擋下，才沒有造成使用者看到的重複推播；但只要哪天被誤判「新增」的項目剛好是**真正的雙北租賃案件（已推播過的舊案）**，此機制會讓它被判定為「新案」而重新推播一次，對使用者造成重複打擾。
- **定位**：`scraper.py:159-162`（`parse_taipei_water()` 的 `id`/`case_id` 賦值）、`scraper.py:991-996`（`item_key()` 把 `id` 納入去重 key）。
- **建議修法**：`url` 本身已帶穩定的 `pkPmsMain=` 參數（每個標案唯一、不受頁面排序影響），不需要再靠「編號」欄位輔助去重：
  ```diff
           date_td = td_map.get("公告日期") or td_map.get("發布日期") or td_map.get("日期")
           dt = date_td.get_text(strip=True) if date_td else ""
  -        id_td = td_map.get("編號")
  -        case_id = id_td.get_text(strip=True) if id_td else ""
           if title and len(title) > 3:
  -            items.append({"id": case_id, "title": title, "date": dt, "url": href})
  +            items.append({"title": title, "date": dt, "url": href})
  ```
  套用後 `item_key()` 會回退成用 `title+date+url` 去重，`url` 內的 `pkPmsMain` 已足夠穩定識別同一標案。
- **是否已修**：未修。**驗證方式**：因 sandbox 無法連線 `water.gov.taipei`，無法用 `dry_run_all_regions.py --debug --source 台北自來水處` 即時驗證；建議在有網路權限環境套用後執行該指令，確認同一批 raw item 在「模擬新增 1 筆插到最前面」前後，其餘項目的 `id` 欄位不再出現於輸出（因為已不再產生 `id` 欄位）。

### 問題 17（**新增**）：國家住宅及都市更新中心（HURC）63% 項目的推播連結指向清單頁，而非個別公告頁

- **現象**：`parse_hurc()`（`scraper.py:286-330`）在 `tds[2]`（案名欄）找不到 `<a href>` 標籤、或找到的 `href` 是 `javascript:...`（第 316-323 行）時，會退回整個清單頁網址 `https://www.hurc.org.tw/hurc/procurement`（第 320-323 行）。本次統計 `state.json['國家住宅及都市更新中心']` 累積的 60 筆快取，**38 筆（63%）`url` 就是這個清單頁本身**，而非個別案件的詳情連結。若使用者點開這類推播訊息的連結，只會看到整份清單，需自行搜尋案名才能找到對應公告，體驗不佳。目前該來源近 7 天 `notify=0`（尚未實際推播過這類壞連結），但一旦出現新案件且該筆剛好命中此路徑，使用者拿到的就會是壞連結。
- **定位**：`scraper.py:316-323`。
- **建議修法**：因案號（`case_id`，第 308-309 行）已擷取成功，若 HURC 站方支援用案號組出查詢式網址（類似 `parse_fnp()` 用 `onclick` 中的 `msgId` 組 URL 的做法，見 `scraper.py:381-385`），可比照辦理；但目前無法連線確認該站是否有這類網址規則或 `onclick` 屬性可用。建議下一步：
  ```
  在有網路權限環境，對「javascript:」href 的那幾筆做 debug，印出完整 <a> 標籤（含 onclick／data- 屬性），
  確認是否有可組成穩定詳情頁 URL 的欄位（例如案號、內部 ID）；若無，退而求其次至少讓推播文字附上案號，
  方便使用者在清單頁用 Ctrl+F 搜尋。
  ```
- **是否已修**：未修（需先在有網路權限環境確認 HTML 結構才能定案）。

### 問題 18（**新增**）：多個來源 date 欄位大量空白，日期窗（±10 天）篩選對這些來源形同虛設

- **現象**：`is_within_date_window()` 對「無法解析日期」的項目採取**放行**策略（註解：「無法解析日期時放行」）。本次統計 `state.json` 各來源累積快取的空白日期比例：

  | 來源 | 空白日期筆數 | 總筆數 | 比例 |
  |---|---|---|---|
  | 台北自來水處 | 103 | 103 | 100% |
  | 土地銀行出租不動產 | 43 | 43 | 100%（延續問題 10） |
  | 政府採購網 | 283 | 300 | 94% |
  | 國家住宅及都市更新中心 | 21 | 60 | 35% |

  以台北自來水處為例，`parse_taipei_water()`（`scraper.py:155`）用 `td_map.get("公告日期") or td_map.get("發布日期") or td_map.get("日期")` 靠 `data-title` 屬性比對欄名，但同一段程式邏輯（`scraper.py:279`，`parse_taipei_dof` 台北市財政局）套用在同樣的 CCMS 系統卻只有 3/21（14%）空白——顯示不是比對邏輯本身有問題，而是**台北自來水處頁面的日期欄 `data-title` 屬性值和財政局頁面不同**（水處可能已改版或欄名不同），需要現場 HTML 比對才能精準修。政府採購網（`scraper.py:439`，`date_str = tds[5].get_text(strip=True)`）用固定欄位索引 `tds[5]` 取日期，94% 空白高度可疑是欄位索引錯位或該欄本身常態性無值。
- **定位**：`scraper.py:155`（台北自來水處）、`scraper.py:439`（政府採購網）、`scraper.py:314-318`（HURC，部分列缺日期前綴）。
- **建議修法**：因無法連線現場核對正確欄位，本次不猜測性下 diff（避免修出一個「看似正確但實際仍抓錯欄位」的假修復）。建議参考 `parse_moe_xuechan()`（`scraper.py:509-514`）已採用的做法——用正規表示式驗證抓到的字串是否像日期格式，不像才 fallback 到另一欄——把這個較穩健的 pattern 套用到台北自來水處與政府採購網：
  ```python
  dt = date_td.get_text(strip=True) if date_td else ""
  if not re.search(r"\d{2,4}[/.\-年]\d{1,2}", dt):
      # fallback 到其他候選欄位或整列文字重新找日期
      ...
  ```
  但實際要 fallback 到哪一欄，需要先看到真實 HTML 結構才能決定，建議列入待決事項待人工在有網路權限環境協助。
- **是否已修**：未修。

### 問題 14（延續，補充今日新發現的細節）：跨來源／同來源重複推播同一案件

- **現象**：09-22 報告已記錄 09-19 曾有「瑠公管理處 115 年度第 6 批」被自有 parser 與 Google Alerts 共 3 次重複推播。本次重讀 `parse_google_alerts()`（`scraper.py:651-687`）確認其中一個獨立子成因：**該 3 次裡有 2 次其實同屬 Google Alerts 這一個來源**，因為 `GOOGLE_ALERT_FEEDS`（`scraper.py:649-650`）設定了 2 個不同查詢的 Atom feed，同一則新聞被兩個 feed 各自收錄一次，兩者的 `link`（Google 轉址連結）帶有不同追蹤參數、`url` 不同，`parse_google_alerts()` 自身的去重（第 681-686 行，僅比對 `url`）與 `item_key()`（比對 `id+title+date+url`）都因 `url` 不同而失效。這個子成因不需要等「跨來源整併」這個較大的架構改動，可以先在 `parse_google_alerts()` 內部用標題正規化多加一層防護：
  ```diff
       seen, unique = set(), []
       for i in items:
  -        if i["url"] not in seen:
  -            seen.add(i["url"])
  -            unique.append(i)
  +        norm_title = re.sub(r"\s+", "", i["title"])
  +        if i["url"] in seen or norm_title in seen:
  +            continue
  +        seen.add(i["url"])
  +        seen.add(norm_title)
  +        unique.append(i)
  ```
- **是否已修**：未修。此為問題 14 的補充修法（先解決 Google Alerts 內部重複，跨來源整併仍待問題 14 原本的架構決策）。

## 三、優化建議

### 建議 1（**新增，依據問題 16**）：修正台北自來水處的去重 key，消除虛假「新增」與潛在重複推播

- **依據**：`state.json` 實測數據顯示，09-22 該來源 `fetched=12, new=12`，但用 `id` 欄位逐筆比對前後兩次快照，其中 11 筆的 `title`/`url` 與前一天完全相同、只有 `id` 因排序位移而改變，代表真正的新案只有 1 筆，`new` 被高估 1100%。
- **做法**：套用問題 16 的 diff，移除 `id` 對此來源去重的影響，改用 `title+date+url`。
- **預期效果**：`new` 統計回歸真實新增數量；消除「舊案因位置位移被誤判為新案並重新推播」的風險（目前尚未實際發生使用者可見的重複推播，屬於「運氣好」而非「機制正確」）。

### 建議 2（**新增，依據問題 18**）：優先修正政府採購網（94% 空白）與台北自來水處（100% 空白）的日期欄位

- **依據**：`is_within_date_window()` 的「無法解析日期則放行」設計，在日期抓取失敗率如此高（94%、100%）的情況下，等同該來源的 `DATE_WINDOW_DAYS=10` 篩選對多數項目沒有實際作用，任何舊案只要因為 `item_key` 變動（如問題 16 的位移現象、或改版後案號變動）被誤判為新案，都會被當成「近 10 天公告」直接放行推播，不會被日期窗攔下。
- **做法**：優先度上建議先修台北自來水處（因問題 16 已證實其 `id`/去重機制不穩定，兩個問題疊加風險更高），再處理政府採購網；兩者都需要現場 HTML 核對正確欄位，非本次 sandbox 環境可完成，建議排入下次有網路權限的人工核對排程。
- **預期效果**：讓日期窗篩選對這兩個來源恢復實際攔截效果，降低舊案被誤推播的風險。

### 建議 3（延續 09-22 建議 2，未變更）：擴充 `GLOBAL_FILTER["regions"]` 涵蓋雙北行政區名

- 內容同 09-22 報告建議 2（林口世大運選手村社會住宅店鋪招租 2 筆真實漏網案例），本次無新增案例，狀態延續。

方向 C（新增來源）：因方向 A/B 本次仍有多項待修問題（含 2 項新發現），依既有慣例暫緩新來源建議，待前述問題排除後再評估擴充。

## 四、待決事項

1.【延續，自 09-03 起第 21 天】問題 1（國營台鐵地區篩選失效）：是否核准本次的簡化版 diff（清空 title/agency 中的「臺北營業分處」字樣）？
2.【延續，今日無新執行資料】問題 2（政府採購網 18 天空窗期）：是否延續「觀察 2-3 天」的既定方向？
3.【延續，長期未拍板】問題 3（黑名單缺「停車場」+ `FNP_PAGES` 含停車場分類 + `skip_global_filter`）
4.【延續，長期未拍板，今日無新執行資料可確認第 6 次重現】問題 4（ANXING ERP header 未 `.strip()`）
5.【延續，長期未拍板】問題 5（`SOURCES` 逐來源篩選設定死碼，是否刪除或改為真正生效）
6.【延續，自 09-05 起第 19 天，低優先度】問題 6（國有財產署 state.json 重複紀錄）
7.【延續，自 09-09 起第 15 天】問題 7（`log_checker.py` 編碼錯誤）
8.【延續，自 09-10 起第 14 天】問題 8（Claude fallback 結構性遺失 href）
9.【延續，自 09-16 起第 8 天，已有具體案例】問題 9（雙北行政區名未涵蓋於 regions）
10.【延續，自 09-18 起第 6 天】問題 10（土地銀行長期抓錯內容）：本次未新增進展，仍待有網路權限環境重新確認頁面結構
11.【延續，自 09-18 起第 6 天】問題 11（`qa_report.py` 連續天數計算被顯示視窗封頂）
12.【延續，今日無新執行資料確認第 3 天】問題 12（國有財產署恢復狀態）
13.【延續，自 09-19 起第 5 天，高優先度】問題 13（`get()` 對非 2xx 無 log）
14.【延續，自 09-19 起第 5 天，本次補充 Google Alerts 內部重複的獨立修法】問題 14（跨來源/同來源重複推播）：是否先核准 Google Alerts 內部標題去重這個較小範圍的 diff？
15.【延續】國防部政治作戰局是否評估移除或改人工抽查
16.【**新增**】問題 16（台北自來水處 `id` 位移造成假新增）：是否核准移除 `id` 欄位、改用 `title+date+url` 去重的 diff？
17.【**新增**】問題 17（HURC 63% 連結指向清單頁）：是否授權在有網路權限環境 debug `<a>` 標籤的 `onclick`/`data-` 屬性，尋找可組出詳情頁 URL 的欄位？
18.【**新增**】問題 18（多來源 date 空白：台北自來水處 100%、政府採購網 94%）：是否排入下次人工現場核對排程？
19.【**新增，操作面**】本 QA routine 的排程時間早於 `daily.yml` 實際觸發時間（皆為 UTC 01:30 附近），導致「今日已執行」檢查項目結構性地每天顯示「尚未執行」。建議將此 routine 的排程時間調整至 UTC 02:00 之後（daily.yml 執行 + `log-check.yml` 完成之後），是否核准？
20.【延續，低優先度】`state.json` 中 3 個已無對應 `SOURCES` 設定的舊來源鍵值
21.【延續】台北市都發局是否同意「先修問題 8/17 補齊 url/agency、暫不移除」的處理順序
