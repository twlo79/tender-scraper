# 標案爬蟲監測報告 2026-09-18

## 一、狀況總覽

整體狀態：❌ 有異常（今日執行與 GitHub Actions 本身正常，且今日無推播、問題 1 未造成新外洩；但問題 1～9 全數延續未修、天數持續累加；本次另新增 2 項此前從未被發現的問題——問題 10〔土地銀行來源長期抓錯內容，0 筆真實出租案件〕、問題 11〔QA 工具 `qa_report.py` 自身的「連續 N 天」計算被 `--days` 視窗封頂，嚴重低報政府採購網的斷線天數〕；國有財產署 fetched=0 今日首度達到 3 天門檻，新增列入 ⚠️）

最近執行：2026-09-18 01:30（推播 0 筆 / 新增 17 筆 / 抓取 106 筆）

| 檢查項目 | 結果 | 說明 |
|---|---|---|
| 1. 今日已執行 | ✅ | `sent_log.json` 最新 key 為 `2026-09-18 01:30`；GitHub Actions run `35295554351` created_at `2026-09-18T01:30:02Z`，與排程時間相符，Make.com 有觸發 |
| 2. 最近一次 GitHub Actions 結論 | ✅ | run `35295554351` `conclusion=success`，job `scrape` 9 個 step 全部 success；近 5 次 run（09-14～09-18：`34796112402`／`34917502965`／`35044328786`／`35170789697`／`35295554351`）全數 success |
| 3. line_pushed 與 total_notify 一致性 | ✅ | `_summary`：`total_fetched=106`、`total_new=17`、`total_notify=0`、`line_pushed=false`，一致；今日全 13 來源皆無符合白名單／地區篩選的新案件，屬正常無推播（非漏推） |
| 4. 是否所有來源 fetched=0 | ✅ | 否，13 來源中 11 個 >0；國有財產署、政府採購網今日為 0（延續，見問題 2、12） |
| 5. 個別來源連續 3 天以上 fetched=0 | ❌ | 政府採購網：`sent_log.json` 顯示自 09-04 起連續第 **15 天** fetched=0（延續，見問題 2）；**國有財產署：自 09-16 起連續第 3 天 fetched=0，今日首度達到 3 天門檻，新增列入 ⚠️（見問題 12）**。註：`qa_report.py --days 7` 自行印出「政府採購網 連續 8 天 fetched=0」，與上述 15 天的事實不符，此為工具本身的計算錯誤，見問題 11 |
| 6. 來源抓取量暴跌/暴增 | ⚠️ | 台北自來水處今日 `fetched=10`，較近 7 天均值（09-11～09-17：1,3,3,3,3,4,4，均值 3.0）暴增達 **3.33 倍**，超過 3 倍門檻。逐筆核對：今日新增 10 筆中多為「換表作業勞務採購」「淤泥處理廠委外操作維護勞務採購」「延性鑄鐵直管及管件財物採購」等工程／財物／勞務採購類公告，皆因不含 `GLOBAL_FILTER` 白名單關鍵字（出租/標租/租賃…）而未新增推播，判定為該來源網站當日公告量真實增加，非 parser 異常，但已達統計門檻列入 ⚠️。其餘 10 個來源今日數字與 7 天趨勢一致 |
| 7. 應擋卻漏網的案件 | — | 今日 `total_notify=0`，無推播項目可供檢查。問題 1（國營台鐵地區篩選失效）程式碼層面仍未修，屬結構性風險延續，非今日新增外洩 |
| 8. 標題為分類名稱／導覽文而非案名 | ✅ | 今日新增 17 筆標題皆為具體案名，非導覽文。延續性風險不變：`state.json['國家住宅及都市更新中心']` 快取中「會員中心」「委員名單」「加入會員」共 3 筆非公告導覽文字，數量與 09-17 相同 |
| 9. url 指向列表頁而非個別公告頁 | ❌ | 延續且盤點範圍擴大：政府採購網 300 筆中 284 筆（94.7%）url 為空；台北自來水處 80 筆中 43 筆（**54%，較 09-17 的 61% 下降**，因今日新增 10 筆皆帶正確 PCC 詳情頁 url）；國家住宅及都市更新中心 60 筆中 21 筆（35%）；國防部政治作戰局 11 筆全數（100%）；**新增盤點：教育部學產基金 5 筆全數（100%）、台北市都發局 35 筆全數（100%）url 為空——這兩個來源此前的報告未列入統計，經今日重新核對確認同樣受影響**。根因分析見問題 8 |
| 10. date 欄位為空、導致日期窗被跳過放行 | ⚠️ | 今日新增 17 筆 date 欄位均非空（含教育部學產基金新增 1 筆「115-09-17」ROC 格式日期，可正確解析）。延續性風險：台北自來水處 80 筆中 80 筆（100%）、政府採購網 284 筆（95%）、**新增盤點：土地銀行出租不動產 43 筆全數（100%）date 為空**。`is_within_date_window()`（`scraper.py:1025-1037`）確認：日期與標題皆無法解析日期時直接放行（`return True`），目前尚未觀察到因此誤放行的實際案例，但風險面隨土地銀行來源的新發現（問題 10）而擴大 |

## 二、異常與處理

問題 1～9 編號與 09-17 報告一致，僅更新天數與數字；問題 10、11、12 為本次新增。

### 問題 1（延續，自 09-03 起第 16 天未修，今日無新外洩）：國營台鐵地區篩選對此來源完全失效

- **現象**：`parse_tra()`（`scraper.py:167-197`）僅用「臺北營業分處」是否出現在 `agency+title` 篩選項目，其轄區橫跨全台（含基隆、桃園等），地區篩選對此來源形同虛設。累計已造成 3 次真實外洩（09-01、09-03、09-17）。今日 `notify=0`，該來源本身 `fetched=5, new=0`（全為快取內既有項目），未觸發新的外洩，但程式碼本身仍未修改，風險依然存在，任何一次有新案件時都可能重演。
- **定位**：`scraper.py:197`。
- **建議修法**（與前次相同，尚未套用）：
  ```diff
       items = [i for i in items if "臺北營業分處" in i.get("agency", "") + i.get("title", "")]
  +    def _tra_is_shuangbei(title: str) -> bool:
  +        parts = re.findall(r"【(.*?)】", title)
  +        loc = parts[2] if len(parts) > 2 else title
  +        return any(k in loc for k in ("台北", "臺北", "新北"))
  +    items = [i for i in items if _tra_is_shuangbei(i["title"])]
       log.info(f"  [國營台鐵] {len(items)} 筆（臺北營業分處）")
       return items
  ```
- **驗證阻礙**：sandbox 環境對外部標案來源網域出站連線全域被 proxy 阻擋（今日再次確認：`https://www.railway.gov.tw` 對應 `dry_run_all_regions.py --debug --source 國營台鐵` 需即時連線，無法驗證）；production（GitHub Actions runner）不受此限制。
- **是否已修**：未修。已連續造成 3 次真實外洩，仍是所有問題中優先度最高者。

### 問題 2（延續，自 09-04 起第 15 天未修）：政府採購網連續 15 天 fetched=0

- **現象**：`sent_log.json` 逐日核對，09-04～09-18 共 **15 天連續 0 筆**（無中斷）。今日 production log 顯示兩個查詢關鍵字頁面（`出租`、`標租`）皆回傳 0 筆，且無任何 `GET 失敗` 警告，代表連線成功但查詢結果為空，與「網站改版或查詢詞失效」的假設一致。
- **定位**：`scraper.py:397-462`（`_pcc_fetch_keyword()`），`target_table` 選取失敗時無 log，無法分辨是「查無資料」還是「解析失敗」。
- **建議修法**：同 09-16/09-17 報告——在 `target_table is None` 分支加一行 `log.warning(f"[政府採購網] {keyword} target_table 未找到，回應長度 {len(r.text)}")`，下次執行即可分辨是空結果還是解析失敗，未套用。
- **是否已修**：未修。

### 問題 3（延續，長期未拍板）：`GLOBAL_FILTER` 黑名單缺「停車場」；國有財產署 `skip_global_filter=True` 讓停車場類公告可繞過所有篩選

- **現象**：今日重新核對 `scraper.py:69-75`（黑名單清單）、`scraper.py:337-342`（`FNP_PAGES`）、`scraper.py:757`（`skip_global_filter=True`），三處皆無變化。
- **建議修法**：同前（未變更）。
- **是否已修**：未修。

### 問題 4（延續，長期未拍板；今日 log-check.yml 已產生 09-17 資料，確認 ERP header 錯誤再次重現）：安幸 ERP 匯入 header 值含換行字元

- **現象**：`push_to_anxing()`（`scraper.py:1147-1148、1168-1169`）直接用 `os.getenv()` 當 HTTP header 值，未 `.strip()`。**今日核對 `daily_report.json`，09-17 條目的 `errors` 欄位已產生，內容為：`⚠ ERP 匯入異常（不影響 LINE 推播）：Invalid leading whitespace, reserved character(s), or return character(s) in header value: '***\n'`**——證實 09-17 當日 `total_notify=4`（真實觸發 ERP 呼叫）確實再次出現此錯誤，非單次意外。今日（09-18）`notify=0`，log 明確顯示「今天沒有要推播的，不打 ERP」，ERP 分支未被觸發，本次無法新增證據，但既有證據已足以確認問題持續存在。
- **定位**：`scraper.py:1147-1148`（`os.getenv("ANXING_URL","")` / `os.getenv("ANXING_KEY","")` 未 `.strip()`）。
- **建議修法**：
  ```diff
       url = os.getenv("ANXING_URL", "")
       key = os.getenv("ANXING_KEY", "")
  +    url = url.strip()
  +    key = key.strip()
       if not url or not key:
  ```
- **是否已修**：未修。**已確認連續 2 次真實重現（09-15、09-17），建議提升優先度**。

### 問題 5（延續，長期未拍板）：`SOURCES` 中各來源自訂 `whitelist`/`blacklist`/`regions` 從未生效（死碼）

- **現象**：今日重新核對 `passes_filters()`（`scraper.py:808-826`）簽名未變，仍只吃模組層級 `GLOBAL_FILTER`，`SOURCES` 內逐來源設定的 `whitelist`/`blacklist`/`regions` 欄位（含土地銀行、國營台鐵等）從未被讀取。此死碼也是問題 10（土地銀行）目前無法用「per-source 黑名單」快速攔截雜訊的原因——必須直接改 parser 本身。
- **建議修法**：同前（未變更）。
- **是否已修**：未修。

### 問題 6（延續，自 09-05 起第 15 天）：國有財產署「115年第17批」重複紀錄仍原樣保留在 `state.json`

- **現象**：`state.json['國有財產署']` 現存 8 筆，重複紀錄（x2）仍在；因該來源今日 `fetched=0`（見問題 12）未有新資料觸發變化。依規則本次不修改 `state.json`。
- **是否已修**：未修。

### 問題 7（延續，自 09-09 起第 10 天；今日取得新證據，確認編碼問題導致統計失真）：`log_checker.py` 讀取 GitHub Actions log 未指定編碼，`daily_report.json` 長期無法正確解析

- **現象**：核對 `daily_report.json` 09-17 條目：`"sources": {}`、`"total_new": 0`、`"total_pushed": 0`，但 `sent_log.json` 顯示 09-17 實際 `total_new=5`、`total_notify=4`（真實推播 4 筆）。兩者完全不符，直接證實 `log_checker.py` 的 regex 解析（`parse_log()`，`log_checker.py:57-64`）因編碼錯誤未能比對到中文字串「抓取：」「合計新增：」，導致所有來源統計與總計欄位長期回傳空/零值，僅 `errors` 欄位（英文/ASCII 為主）尚能正確擷取。
- **定位**：`log_checker.py:46-50`（`get_latest_log()` 的 `requests.get()` 未設定 `r.encoding`）。
- **建議修法**：
  ```diff
       r = requests.get(
           f"https://api.github.com/repos/{GITHUB_REPO}/actions/jobs/{jobs[0]['id']}/logs",
           headers=GH, timeout=30, allow_redirects=True,
       )
  +    r.encoding = "utf-8"
       return r.text
  ```
- **是否已修**：未修。**今日已取得具體失真證據（09-17 條目），建議優先套用**。

### 問題 8（延續，自 09-10 起第 9 天，本次盤點範圍擴大並找到疑似根因）：多個來源 `url` 欄位大範圍空白

- **現象**：今日重新統計（見一、狀況總覽第 9 項），除既有 4 個來源外，新確認教育部學產基金（100%，5/5）、台北市都發局（100%，35/35）同樣受影響。**本次進一步比對根因**：`parse_with_claude_fallback()`（`scraper.py:687-711`）呼叫 Claude 時傳入的內容是 `soup.get_text("\n")[:8000]`——`get_text()` 會把所有 `<a href>` 屬性都剝除，只留下純文字，因此凡是主要 table/list 選擇器抓不到資料而落入 Claude fallback 的來源（今日核對台北市都發局、國防部政治作戰局、教育部學產基金皆屬此情況），**Claude 從一開始就拿不到任何 href 資訊，url 欄位在設計上必然為空字串**，並非 Claude 抽取品質問題。政府採購網、台北自來水處、國家住宅及都市更新中心的 url 缺失則各有各的表格解析問題（非 Claude fallback 路徑），根因不同，維持個別追蹤。
- **建議修法**（新增，鎖定 Claude fallback 路徑的結構性根因）：
  ```diff
   def parse_with_claude_fallback(text: str, name: str, base: str) -> list[dict]:
  -    if not CONFIG["api_key"]: return []
  +    if not CONFIG["api_key"]: return []
  +    # text 目前用 soup.get_text() 產生，已經遺失所有 href；
  +    # 呼叫端應改傳「保留連結」的文字版本，例如：
  +    #   text = "\n".join(f"{a.get_text(strip=True)} ({urljoin(base, a['href'])})"
  +    #                    for a in soup.find_all('a', href=True))
  +    # 讓 Claude 能在輸出 JSON 的 url 欄位填入實際連結，而非固定留空。
       try:
  ```
  （此為呼叫端需搭配修改：所有使用 `parse_with_claude_fallback(soup.get_text(...))` 的呼叫處都要改成保留連結的文字格式，屬中等工作量，建議與問題 5 死碼死碼清理一併排入下次修改批次）
- **驗證阻礙**：sandbox 環境對相關來源網域出站連線與 WebFetch 皆被阻擋，無法即時重跑驗證。
- **是否已修**：未修。

### 問題 9（延續，自 09-16 重新核實起第 3 天）：雙北案件標題只寫行政區名（未寫「台北/新北」）被地區篩選誤殺

- **現象**：`GLOBAL_FILTER["regions"]`（`scraper.py:76`）仍僅比對「台北／臺北／新北／瑠公管理處」，未涵蓋雙北各行政區名稱。今日 Google Alerts 新抓到一筆「新化果菜市場飲食區攤位」標租公告（台南市），因不含任何雙北關鍵字被地區篩選正確擋下——這是「非雙北案件被正確擋下」的正向案例，但同一套機制若遇到雙北案件卻只寫區名（如「中正區」而非「台北市中正區」）時會被誤殺，缺口本身未變。
- **建議修法**：同前（未變更）。
- **是否已修**：未修。

### 問題 10（新增）：土地銀行出租不動產來源長期抓錯內容，快取 43 筆中僅 1 筆勉強沾邊、0 筆為真正不動產出租案件

- **現象**：逐筆核對 `state.json['土地銀行出租不動產']` 全部 43 筆快取，內容幾乎清一色為該行網站的**全站公告與行銷訊息**（ATM／網路銀行系統維護公告、晶片金融卡跨境提款服務中斷、抽獎活動、定存專案、貸款利率調整、吉祥物命名活動等），僅 1 筆（「本行修訂『保管箱出租契約』」）與「出租」字面沾邊但仍非不動產案件，**沒有任何一筆是真正的不動產出租公告**——儘管此來源存在的目的正是抓取土地銀行不動產出租公告（`SOURCES` 設定 `url: https://www.landbank.com.tw/Bulletin/RentRealty`）。
  比對 `parse_landbank()`（`scraper.py:604-630`）程式碼：`URL` 本身指向正確的 `/Bulletin/RentRealty` 頁面，但 CSS 選擇器 `table tbody tr, table tr, .bulletin-item, .list-item, article` 範圍過寬，極可能抓到的是該站**每頁共用的「最新消息」全站公告區塊**，而非 RentRealty 頁面真正的不動產列表（也可能後者是 JS 動態渲染、`requests` 抓到的靜態 HTML 中根本不存在該表格，導致寬鬆選擇器改抓到旁邊的公告清單）。此外，該來源每日 `fetched` 長期穩定在 1～2 筆，與「一家全國性銀行的招租不動產」規模明顯不成比例，佐證抓取對象錯誤。
  **近一步風險**：該僅存的「保管箱出租契約」項目因不含任何雙北關鍵字才被地區篩選（問題 9 同機制）擋下，屬僥倖，並非制度性防護——若日後行銷公告剛好帶有雙北字樣（如分行活動公告），有機會被 `GLOBAL_FILTER` 白名單的「出租」關鍵字誤判為不動產案件而推播。
- **定位**：`scraper.py:604-630`（`parse_landbank()` 的 CSS 選擇器）。
- **建議修法**（第一層防禦性修法，可立即套用；根本修法需在有網路權限環境重新確認 `/Bulletin/RentRealty` 實際 DOM 結構後才能給出精準 selector diff）：
  ```diff
       soup  = BeautifulSoup(r.text, "lxml")
       items = []
  +    _NOISE_RE = re.compile(
  +        r"金融卡|自動櫃員機|ATM|網路銀行|行動銀行|系統維護|停止服務|信用卡|基金交易|"
  +        r"抽獎|定存專案|貸款|台灣Pay|智能客服|吉祥物|行史展|存股|投保"
  +    )
       for row in soup.select("table tbody tr, table tr, .bulletin-item, .list-item, article"):
           a   = row.find("a", href=True)
           tds = row.find_all("td")
           if not a:
               continue
           title = a.get_text(strip=True)
           if len(title) < 5:
               continue
  +        if _NOISE_RE.search(title):
  +            continue
           href = a["href"] if a["href"].startswith("http") else urljoin(BASE, a["href"])
  ```
- **驗證阻礙**：sandbox 環境對 `landbank.com.tw` 出站連線被 proxy 阻擋，無法取得 `/Bulletin/RentRealty` 實際 HTML 結構以確認選擇器是否命中錯誤區塊、或該頁是否為 JS 渲染。上述 diff 僅為關鍵字防禦網，**不是根本修法**，建議待有網路權限環境時用 `dry_run_all_regions.py --debug --source 土地銀行出租不動產` 搭配瀏覽器人工比對頁面結構後再定案。
- **是否已修**：未修。

### 問題 11（新增）：QA 工具 `qa_report.py` 自身的「連續 N 天 fetched=0」計算被 `--days` 顯示視窗封頂，嚴重低報

- **現象**：今日執行 `python qa_report.py --days 7`，工具印出「政府採購網 連續 8 天 fetched=0」；但直接核對 `sent_log.json` 全部歷史，政府採購網實際已**連續 15 天**（自 09-04 起）fetched=0。根因：`zero_streak` 的計算迴圈（`qa_report.py:238`）只掃描 `recent`——也就是被 `--days SHOW_DAYS` 篩出的顯示視窗（本次為近 8 個日期），而非完整歷史 `valid_entries`，因此連續天數永遠不會超過 `SHOW_DAYS` 左右的上限，數字越長期的斷線問題被低報得越嚴重。這會讓維運人員在只看工具輸出、未交叉核對原始資料時，低估問題 2 的嚴重程度與處理急迫性。
- **定位**：`qa_report.py:237-246`（`zero_streak` 迴圈使用 `recent` 而非 `valid_entries`）。
- **建議修法**：
  ```diff
           # 找哪些來源有問題
  +        # 連續天數需看完整歷史，只在 recent（受 --days 限制的顯示視窗）內累計會被視窗封頂而低報
           zero_streak: dict[str, int] = {}
  -        for key in sorted(recent.keys()):
  -            entry = recent[key]
  +        for key in sorted(valid_entries.keys()):
  +            entry = valid_entries[key]
               for name in SOURCE_NAMES or []:
  ```
- **是否已修**：未修。**建議與問題 2 一併排入下次修改，避免持續誤導判讀**。

### 問題 12（新增，自 09-16 起第 3 天，首度達 3 天門檻）：國有財產署連續 3 天 fetched=0

- **現象**：`sent_log.json` 顯示國有財產署 09-09～09-15 每天穩定 `fetched=1`，09-16、09-17、09-18 三天連續為 0，今日首度達到規則 5 的 3 天門檻。今日 production log 顯示 4 個子分類頁面（招標公告／地上權設定／停車場／創業租賃）皆回傳「0 筆」，過程無任何 `GET 失敗` 或例外，顯示連線成功但解析／查詢結果為空——與問題 2（政府採購網）的模式相似，但目前僅 3 天，無法排除是「北區分署近期恰好無任何有效公告（4 類別同時掛零屬巧合但非不可能）」與「網站小幅改版/查詢條件過期」兩種可能。
- **定位**：`scraper.py:337-396` 附近（`FNP_PAGES` 對應的抓取邏輯，需比對目前 4 個子頁面 HTML 結構）。
- **驗證阻礙**：sandbox 對 `esvc.fnp.gov.tw` 出站連線被 proxy 阻擋（`dry_run_all_regions.py --debug --source 國有財產署` 今日執行結果為 `ProxyError ... 403 Forbidden`），無法確認是否為選擇器失效。
- **建議修法**：暫無法給出具體 diff（尚未定位到失敗原因），建議下次若連續達 5～7 天仍為 0，比照問題 2 的作法在解析失敗分支補上診斷 log；若有網路權限環境，優先用瀏覽器人工核對北區分署 4 個子頁面目前是否確實無公告。
- **是否已修**：未修，列入觀察。

## 三、優化建議

### 建議 1（方向 A，延續 09-17，優先度最高）：問題 1（國營台鐵）已連續第 3 次造成真實外洩，建議優先於其餘問題單獨排入下次修改

- **依據**：09-01、09-03、09-17 三次真實推播了非雙北案件（基隆市 x2、桃園市 x1），發生頻率約每 8～14 天一次，且每次都透過 LINE OA broadcast 直接送達使用者，是目前唯一已對外造成實質影響、且仍完全未修的缺口。
- **做法**：套用問題 1 的 `parse_tra()` diff；待有網路權限環境時用 `dry_run_all_regions.py --debug --source 國營台鐵` 驗證抓到筆數與地點判斷正確。
- **預期效果**：消除目前唯一已知會持續造成真實外洩的缺口。

### 建議 2（方向 B，新增，依據本次問題 10 的發現）：土地銀行出租不動產來源應優先於其他低效來源處理——目前是「掛名不動產來源，實為 0 產出」

- **依據**：逐筆核對 43 筆快取，0 筆為真正的不動產出租案件（僅 1 筆勉強沾邊），100% 為銀行網站行銷/系統維護公告；相較於國防部政治作戰局（09-15/09-16/09-17 已建議評估的「有案件但無法辨識」低價值來源），土地銀行的問題更嚴重——**抓的內容根本不是目標資料**，且今日發現其僅存的「沾邊」項目差點因缺乏地區關鍵字而繞過白名單防線（問題 10 所述僥倖案例）。
- **做法**：短期先套用問題 10 的關鍵字防禦 diff 降低誤推風險；中期需在有網路權限環境重新確認 `/Bulletin/RentRealty` 真實頁面結構，改用精確選擇器或改抓 JSON API（若有）；若確認該頁面本身數月來就沒有新增不動產公告、或前端已改為 JS 動態渲染導致 `requests` 無法取得，建議與國防部政治作戰局一併評估是否移除或改為人工定期抽查。
- **預期效果**：避免僥倖防護失效造成的潛在誤推，並讓「土地銀行」這個來源名副其實地產出不動產案件而非行銷雜訊。

### 建議 3（方向 B，延續 09-15/09-16/09-17）：國防部政治作戰局應歸類為「結構性低價值來源」，建議人工拍板是否移除

- **依據**：近 7 天 `notify` 總數為 0（09-12～09-18：fetched 皆為 11，notify 皆為 0），url 100% 空白（且今日確認根因為 Claude fallback 路徑結構性遺失 href，見問題 8），快取標題皆為跨縣市年度彙總公告格式。
- **做法**：人工評估是否移除此來源；若保留，應改為定期人工開啟彙總 PDF 抽查，爬蟲無法自動化此步驟。
- **預期效果**：降低維運關注度分散到「結構性零產出」來源上的成本。

## 四、待決事項

1. 【延續，自 09-03 起第 16 天，累計第 3 次真實外洩，優先度最高】問題 1（國營台鐵地區篩選失效）：是否核准 `parse_tra()` 的地點判定 diff？並授權在有網路權限環境驗證？
2. 【延續，自 09-04 起第 15 天】問題 2（政府採購網連續 15 天 fetched=0）：是否請人工用瀏覽器手動核對同組查詢參數，確認是網站改版還是查詢詞過窄？是否核准新增診斷 log 的 diff？
3. 【延續，長期未拍板】問題 3（`GLOBAL_FILTER` 缺「停車場」黑名單 + 國有財產署 `FNP_PAGES` 含停車場分類 + `skip_global_filter`）：三個 diff 是否核准套用？
4. 【延續，長期未拍板；今日已確認 09-15、09-17 兩次真實重現，建議提升優先度】問題 4（安幸 ERP header 值未 `.strip()`）：`.strip()` 防禦修法是否核准？是否需重新設定 GitHub Secrets 中的 `ANXING_KEY`／`ANXING_URL`（清除其中的換行字元）？
5. 【延續，長期未拍板】問題 5（`SOURCES` 逐來源篩選設定從未生效）：產品面是否還需要「逐來源覆蓋」能力？需要則授權重新設計 `passes_filters()`，不需要則授權刪除死碼欄位。
6. 【延續，自 09-05 起】問題 6：國有財產署「115年第17批」在 `state.json` 中的重複紀錄是否需要人工手動清理？
7. 【延續，自 09-09 起，今日已取得具體失真證據，建議優先套用】問題 7（`log_checker.py` 編碼錯誤導致 `daily_report.json` 長期無法正確解析、推播統計失真）：是否核准套用 `r.encoding = "utf-8"` 一行修法？
8. 【延續，自 09-10 起，本次盤點範圍擴大並定位疑似根因】問題 8（多個來源 url 欄位大範圍空白，含 Claude fallback 結構性遺失 href）：是否授權修改 `parse_with_claude_fallback()` 呼叫端改傳保留連結的文字格式？是否授權在有網路權限環境進一步除錯其餘來源？
9. 【延續，自 09-16 起】問題 9（雙北行政區名未涵蓋於 `GLOBAL_FILTER["regions"]`）：是否核准擴充 regions 清單的 diff？
10. 【新增】問題 10（土地銀行出租不動產來源長期抓錯內容，0 筆真實出租案件）：是否核准立即套用關鍵字防禦 diff 作為短期防護？是否授權在有網路權限環境重新確認頁面結構、改寫選擇器？若查明該來源長期無法產出目標資料，是否評估移除？
11. 【新增】問題 11（`qa_report.py` 的「連續 N 天」計算被顯示視窗封頂，低報政府採購網斷線天數）：是否核准修正 `zero_streak` 改讀完整歷史的 diff？
12. 【新增，觀察中】問題 12（國有財產署連續 3 天 fetched=0，剛達門檻）：是否需要人工瀏覽器核對北區分署 4 個子頁面目前是否確實無公告？若連續達 5～7 天仍為 0，是否授權比照問題 2 補上診斷 log？
13. 【延續，09-15/09-16/09-17 建議】國防部政治作戰局（近 7 天 fetched=77+、notify=0，全數為跨縣市彙總公告）是否評估移除？或改為定期人工抽查彙總 PDF？
