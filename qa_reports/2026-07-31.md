# 標案爬蟲監測報告 2026-07-31

## 一、狀況總覽

整體狀態：❌ 有異常
最近執行：2026-07-31 01:30（UTC，GitHub Actions run [30596497447](https://github.com/twlo79/tender-scraper/actions/runs/30596497447) success）（推播 0 筆 / 新增 3 筆 / 抓取 58 筆）

| 檢查項目 | 結果 | 說明 |
|---|---|---|
| 1. 今日已執行 | ✅ | `sent_log.json` 有 `2026-07-31 01:30` 記錄，對應 Actions run 於 `2026-07-31T01:30:08Z` 觸發（workflow_dispatch），`conclusion: success` |
| 2. 最近一次 Actions run 結論 | ✅ | success（近 5 次 run 07-27～07-31 全數 success，非 Make.com 未觸發問題） |
| 3. line_pushed 與 total_notify 一致性 | ✅ | 逐日比對 07-25～07-31 共 7 筆 `_summary`：`total_notify>0` 時 `line_pushed=true`（07-26/07-28/07-30），`total_notify=0` 時 `line_pushed=false`（07-25/07-27/07-29/07-31），全數一致 |
| 4. 所有來源 fetched=0 | ✅ 否 | 13 個來源中 9 個抓到資料（合計 58 筆），4 個為 0（教育部學產基金、台北市都發局、國防部政治作戰局、Google Alerts） |
| 5. 個別來源連續 3 天以上 fetched=0 | ❌ | 教育部學產基金／台北市都發局／國防部政治作戰局：**連續第 4 天** fetched=0（07-28、07-29、07-30、07-31）。根因見問題 1，仍未解決 |
| 6. 來源 fetched 暴跌/暴增 | ✅ | 其餘 9 個來源 7 日內波動皆在合理範圍（如台北自來水處 1→2、國有財產署 2→3），無 >50% 驟降或 >3 倍暴增。Google Alerts 今日 0 筆屬 RSS feed 當日無新項目，非解析失敗（該 parser 不依賴 Claude，純 XML 解析） |
| 7. 應被黑名單擋掉卻漏網 | ⚠️ | 今日無新推播可供逐筆檢視，但覆核近 7 天：07-26 推播的 Google Alerts 新聞報導（問題 3，07-28 報告已提出但診斷有誤，見下方修正）、07-28 推播的台鐵新竹縣案件（問題 2）根因均**仍未修復**，只是今日未觸發新的漏網事件 |
| 8. 標題為分類/導覽文而非真正案名 | ⚠️ | `_is_junk()`（問題 3-舊編號／本次問題 4）與 FNP「種類」欄位失敗退回類別標籤（問題 7）皆仍未修復，惟今日國有財產署 notify=0，未觸發新實例 |
| 9. url 指向列表頁而非個別公告頁 | ✅ | 今日無新推播；但土地銀行出租不動產（問題 5）持續混入非租賃案件，雖 url 為個別頁面，內容並非招租公告，屬相關但不同類的資料品質問題 |
| 10. date 欄位為空放行 | ✅ 設計如此 | 今日新增 3 筆（台北自來水處 1、土地銀行 2）`date` 欄位均有值；無法解析日期時放行為既有設計（`is_within_date_window()`），非新增風險 |

## 二、異常與處理

### 問題 1（延續，惡化為連續 4 天）：教育部學產基金／台北市都發局／國防部政治作戰局／政府採購網（出租類）— Anthropic API 組織仍處於停用狀態

- **現象**：與 07-28～07-30 完全相同的錯誤，**連續第 4 天**：
  ```
  01:30:32 WARNING [政府採購網] Claude API 錯誤：invalid_request_error — This organization has been disabled.
  01:30:36 WARNING [教育部學產基金] Claude API 錯誤：invalid_request_error — This organization has been disabled.
  01:30:39 WARNING [台北市都發局] Claude API 錯誤：invalid_request_error — This organization has been disabled.
  01:30:41 WARNING [國防部政治作戰局] Claude API 錯誤：invalid_request_error — This organization has been disabled.
  ```
  （來源：run [30596497447](https://github.com/twlo79/tender-scraper/actions/runs/30596497447) job id 91049889810）。追溯到 07-27 的 job log（run 30229945501）確認當天這 3 個來源仍正常運作（教育部 2 筆／都發局 30 筆／國防部 11 筆，無 Claude 錯誤），代表故障精準始於 **07-28**，至今已滿 4 天未解。
- **定位**：與前 3 日相同，`ANTHROPIC_API_KEY` 對應的 Anthropic 組織被停用。`scraper.py` 中這 3 個來源的原生 parser（`parse_moe_xuechan`／`parse_taipei_udd`／`parse_gpwd`）在 07-27 仍能直接抓到資料而不需要 Claude fallback，07-28 起才同時降為 0 並轉呼叫 Claude fallback（均失敗）。三個不同機關網站的原生解析在同一天一起失效，較不可能是三個網站剛好同時改版，也可能與同一套政府共用 CMS（CCMS，程式碼註解已提及）樣板更新有關；但本次執行環境對外連線遭代理／站方回應 403（`depart.moe.edu.tw`、`udd.gov.taipei`、`gpwd.mnd.gov.tw` 皆測試過），無法直接比對即時 HTML 結構驗證這個假設，仍以 Claude fallback 失效（可確定的根因）為主要結論。
- **建議修法**：非程式碼問題，需人工處理（同前 3 日報告，尚未見改善）：
  1. 登入 [Anthropic Console](https://console.anthropic.com) 確認組織停用原因並恢復。
  2. 恢復後檢查 GitHub repo secret `ANTHROPIC_API_KEY` 是否需要更新。
  3. 中期建議：這 3～4 個來源應補上不依賴 Claude 的原生 parser（見優化建議 2），避免單一 API 供應商中斷造成整批來源歸零。
- **是否已修**：未修，且已連續 4 天。**維持最優先待決事項。**

### 問題 2（延續，仍未修復）：國營台鐵地區篩選漏洞

- **現象**：`scraper.py:194-196` 的 `parse_tra()` 僅用「臺北營業分處」這個管理單位名稱篩選，但該單位轄區橫跨全台。截至目前已累積至少 2 起漏網案例：07-16「新竹市香山區」、07-28「新竹縣新豐鄉」（後者已實際推播）。今日程式碼比對確認**修法仍未套用**，今日台鐵新增 0 筆，未觸發新的漏網推播，但根因未解除。
- **定位**：同前 3 日報告。
- **建議修法**（沿用，尚未套用）：
```diff
--- a/scraper.py
+++ b/scraper.py
@@ -193,6 +193,9 @@ def parse_tra() -> list[dict]:
     # 只保留臺北營業分處的標案
     items = [i for i in items if "臺北營業分處" in i.get("agency", "") + i.get("title", "")]
+    # 臺北營業分處轄區橫跨全台，僅靠單位名無法排除外縣市案件，需再依標題地名二次篩選
+    NON_TAIPEI_HINTS = ["新竹", "桃園", "苗栗", "台中", "臺中", "彰化", "宜蘭", "花蓮", "台東", "臺東"]
+    items = [i for i in items if not any(h in i["title"] for h in NON_TAIPEI_HINTS)]
     log.info(f"  [國營台鐵] {len(items)} 筆（臺北營業分處）")
     return items
```
- **是否已修**：未修。套用後請執行 `python dry_run_all_regions.py --debug --source 國營台鐵` 驗證新竹案件被排除、雙北案件（如樹林車站）仍保留。

### 問題 3（修正舊診斷）：Google Alerts 混入房產媒體新聞報導 — 07-28 報告提出的修法實際上無效

- **現象**：07-26 推播「租期最長20年！國有房地標租續租新制上路台北星鑽20戶7月底打頭陣 - 樂屋網房產新聞」，是媒體對政策的報導文章，不是具體招標/標租公告。07-28 報告（問題 3）已指出此案，並提議在 `SOURCES` 中該來源的 `blacklist` 加入「樂屋網」「新制上路」等關鍵字。
- **重要修正**：07-29 報告發現的問題 6（本次編號問題 6）指出 `passes_filters()` **完全不讀取** `SOURCES` 內各來源自訂的 `whitelist`/`blacklist`/`regions`，只讀取模組層級的 `GLOBAL_FILTER`。這代表 07-28 報告提出的 diff（寫入 Google Alerts 的 `SOURCES` 條目 `blacklist`）**即使套用了也不會生效**，因為那個欄位是死代碼。必須改寫在 `GLOBAL_FILTER["blacklist"]`（全域黑名單）才會真正發揮作用。
- **定位**：`scraper.py:69-78`（`GLOBAL_FILTER["blacklist"]`）。
- **建議修法**（修正版，取代 07-28 報告的版本）：
```diff
--- a/scraper.py
+++ b/scraper.py
@@ -69,6 +69,7 @@ GLOBAL_FILTER = {
     "blacklist": [
         "開標結果",
+        "樂屋網", "591", "好房網", "蘋果地產", "房產新聞", "新制上路",
         "自動販賣機", "場地短期出租", "機械類標租", "共享雨傘",
         "新建工程", "統包工程", "物業管理", "專案管理", "保險",
         "清洗作業", "鑄鐵直管", "延性鑄鐵", "塗裝", "管線",
         "財物採購", "勞務採購",
     ],
```
  加在 `GLOBAL_FILTER`（而非 SOURCES 內 Google Alerts 專屬設定）是因為目前唯一真正生效的黑名單就是這一份；且這些關鍵字（媒體網站名稱／報導慣用語）不太可能出現在其他 12 個政府機關來源的公告標題中，加入全域黑名單風險低。
- **是否已修**：未修。套用後請執行 `python dry_run_all_regions.py --debug --source "Google Alerts"` 驗證同類新聞報導被排除，且真正的標租公告仍正常通過。

### 問題 4（延續，仍未修復）：`_is_junk()` 導覽文防呆函式從未被呼叫

- **現象**：`scraper.py:113` 定義 `_is_junk(item)`，今日以 `grep -n "_is_junk("` 確認全檔僅該定義一行出現，仍無任何呼叫點。
- **定位**：同前 3 日報告，`parse_with_claude_fallback()` 第 696 行左右仍直接回傳結果，未套用過濾。
- **建議修法**（沿用，尚未套用）：
```diff
--- a/scraper.py
+++ b/scraper.py
@@ -693,7 +693,8 @@ def parse_with_claude_fallback(text: str, name: str, base: str) -> list[dict]:
             u = item.get("url", "")
             if u and not u.startswith("http"):
                 item["url"] = urljoin(base, u)
-        return items if isinstance(items, list) else []
+        items = items if isinstance(items, list) else []
+        return [i for i in items if not _is_junk(i)]
     except Exception as e:
         log.warning(f"  [{name}] Claude fallback 失敗：{e}")
         return []
```
- **是否已修**：未修。待 Anthropic API 恢復（問題 1）後，用 `dry_run_all_regions.py --debug --source 教育部學產基金` 等驗證。

### 問題 5（延續，仍未修復，本次環境仍無法連網驗證）：郵局房地產出租／國家住宅及都市更新中心 parser 未設定 `agency` 欄位

- **現象**：同 07-29/07-30 報告，`parse_post()`／`parse_hurc()` 產出的 item dict 今日程式碼比對確認**仍沒有 `"agency"` 欄位**。這兩來源近 30 天 `total_notify` 皆為 0（本次以 `sent_log.json` 31 天彙總重新確認：郵局房地產出租 fetch=294／new=5／notify=0，國家住宅及都市更新中心 fetch=332／new=2／notify=0），根因是全國性機構公告標題只寫「內湖」「南港區」「林口」等行政區名，未含「台北/新北」，被 `GLOBAL_FILTER["regions"]` 誤殺（07-29 報告已具體列出至少 3 筆被誤殺的雙北案件）。
- **定位**：同前 2 日報告。
- **建議修法**（沿用，尚未套用）：
```diff
--- a/scraper.py
+++ b/scraper.py
@@ -60,6 +60,13 @@
+TAIPEI_NEWTAIPEI_DISTRICTS = [
+    "松山區","信義區","大安區","中山區","中正區","大同區","萬華區","文山區","南港區","內湖區","士林區","北投區",
+    "板橋區","新莊區","中和區","永和區","土城區","樹林區","三峽區","鶯歌區","三重區","蘆洲區","五股區","泰山區",
+    "林口區","淡水區","三芝區","石門區","八里區","金山區","萬里區","汐止區","瑞芳區","貢寮區","平溪區","雙溪區",
+    "深坑區","石碇區","坪林區","烏來區",
+]
+
 def passes_filters(item: dict) -> bool:
     """套用 GLOBAL_FILTER 的白名單、黑名單、地區篩選（所有來源共用）。"""
     title  = item.get("title", "")
     agency = item.get("agency", "")
     text   = title + agency

     regions = GLOBAL_FILTER.get("regions", [])
-    if regions and not any(k in text for k in regions):
+    if regions and not any(k in text for k in regions) and not any(d in title for d in TAIPEI_NEWTAIPEI_DISTRICTS):
         return False
```
- **是否已修**：未修。

### 問題 6（延續，仍未修復，本次環境仍無法連網驗證）：土地銀行出租不動產 parser 選擇器過於寬泛

- **現象**：今日重新抽查 `state.json` 中「土地銀行出租不動產」最新 20 筆記錄，**24 筆歷史紀錄無一筆是真正的租賃/招租公告**，全部是銀行系統維護公告、行銷活動（「鈦金卡新卡面」「網路投保抽獎」）、貸款產品廣告（「青年安心成家貸款」）等站內公用公告，與昨、前日報告結論一致。30 天彙總：fetch=62／new=15／notify=0，轉換率 0%。
- **定位**：同前 2 日報告，`parse_landbank()` 的 CSS selector `table tbody tr, table tr, .bulletin-item, .list-item, article` 範圍過廣，疑似命中全站公用公告區塊而非 `/Bulletin/RentRealty` 頁面裡的租賃清單表格。
- **建議修法**（沿用治標方案，尚未套用；根本解法仍需實際 HTML 比對）：
```diff
     for row in soup.select("table tbody tr, table tr, .bulletin-item, .list-item, article"):
+        row_text = row.get_text()
+        if any(kw in row_text for kw in ["系統維護", "網路銀行", "客服", "資訊線路", "抽獎", "行動Pay"]):
+            continue
```
- **是否已修**：未修。本次環境對 `www.landbank.com.tw` 的 WebFetch/連線仍回傳 403（代理層與站方皆有），無法即時驗證 HTML 結構。需有網路存取權限的環境重新確認租賃清單表格的專屬 class/id 後收斂 selector，才是根本解。

### 問題 7（延續，仍未修復）：SOURCES 內個別來源自訂的 whitelist／blacklist／regions 欄位是死代碼

- **現象**：`passes_filters(item: dict)` 簽名今日確認仍只有 `item` 一個參數，`main()` 呼叫處（`scraper.py:1151`）也仍是 `passes_filters(i)`，`SOURCES` 中 6 個來源自訂的 `whitelist`/`blacklist`/`regions`（政府採購網、教育部學產基金、國防部政治作戰局、土地銀行出租不動產、郵局房地產出租、Google Alerts）全部未被讀取。
- **重要性上修**：原先（07-29）評為低優先級，因為「這些死代碼欄位目前的值大多與 GLOBAL_FILTER 重疊或為空」。但本次（問題 3）具體驗證了一個實際後果：**任何人依照過去報告的建議去修改某來源的 `blacklist`/`regions`，改了也不會生效**，是真實發生過的誤導案例（07-28 報告本身就踩到這個坑）。建議優先級上修為中。
- **建議修法**：二擇一：(a) 移除這些未使用的欄位並在註解說明篩選規則統一由 `GLOBAL_FILTER` 控制，避免未來繼續誤用；(b) 修改 `passes_filters(item, source=None)`，真正合併套用來源層級覆寫（需注意：多數來源目前 `regions`/`whitelist` 為空列表 `[]`，若照字面「空列表=不限」語意直接套用，會讓這些來源失去 `GLOBAL_FILTER` 的地區限制，屬於危險的行為變更，需要明確語意設計後才能實作，**不建議未經人工確認就直接套用**）。
- **是否已修**：未修。方向 (a)/(b) 選擇列入待決事項。

## 三、優化建議

### 建議 1（延續 07-29/07-30 方向）：郵局房地產出租、國家住宅及都市更新中心優先修 bug，而非降級/移除

- **依據**：31 天彙總（`sent_log.json`）：郵局房地產出租 fetch=294／new=5／notify=0；國家住宅及都市更新中心 fetch=332／new=2／notify=0。兩者轉換率皆為 0%，但根因已由 07-29 報告具體定位（問題 5，本次編號問題 5）為 `agency` 欄位缺失導致地區篩選誤殺，並非案源真的是零。
- **做法**：套用問題 5 的 diff。
- **預期效果**：預估可追回過去已確認的至少 3 筆雙北租賃案件，並修正未來同類漏網。

### 建議 2（延續，急迫性隨天數上升）：4 個依賴 Claude fallback 的來源應補上原生 parser

- **依據**：問題 1 顯示教育部學產基金／台北市都發局／國防部政治作戰局／政府採購網（出租類）已連續 **4 天**（07-28～07-31）因 Anthropic API 組織停用而歸零或大幅降載，且 07-27 前的 30 天趨勢顯示這些來源的原生 parser 平時抓到的數字高度穩定（都發局恆為 ~28-30 筆），代表 Claude fallback 實質上長期是唯一有效解析路徑，而非真正的備援。
- **做法**：Anthropic 帳號恢復後，逐一為這 4 個來源重寫或修正原生 CSS/regex parser。
- **預期效果**：降低對單一 API 供應商的相依性，未來 Claude API 再次中斷時這些來源仍能維持基本抓取能力。

### 建議 3（新發現）：過去 3 天累積的「已提修法但實際上失效」的 diff，套用前應先確認寫入 `GLOBAL_FILTER` 而非 `SOURCES` 死代碼欄位

- **依據**：問題 3 具體證實 07-28 報告提出的 Google Alerts 專屬黑名單修法因問題 7（SOURCES 死代碼）而實際無效。
- **做法**：日後任何「加入某來源專屬 whitelist/blacklist/regions」的修法建議，套用前應先確認問題 7 是否已修復；在問題 7 修復之前，所有此類規則一律應寫入 `GLOBAL_FILTER`。
- **預期效果**：避免重複發生「修了但沒生效」的情況，降低維運誤判風險。

### 方向 C（新來源）：本次略過

既有問題 1～7 均尚未修復，且問題 1（Claude API）已達 4 天未解、影響範圍最大，預期修復既有問題對推播量與精準度的邊際效益仍顯著高於新增一個未驗證來源。建議待問題 1～2 修復並觀察至少一週成效後，再評估是否需要新增來源。

## 四、待決事項

1. **【最優先，已連續 4 天，建議立即處理】Anthropic API 組織已停用**（問題 1）：需人工登入 Anthropic Console 確認並恢復，並視需要更新 GitHub repo secret `ANTHROPIC_API_KEY`。這是本次唯一持續惡化（天數增加）且無法透過程式碼修復的項目。
2. 問題 2～6 的修法 diff 是否核准套用（均為建議，尚未套用）。問題 6（土地銀行）因本次環境仍無法連線 `landbank.com.tw`，套用前建議先在有網路存取權限的環境確認實際 HTML 結構。
3. 問題 7（SOURCES 死代碼欄位）處理方向：(a) 移除欄位 或 (b) 讓 `passes_filters` 真正套用來源層級覆寫。若選 (b)，需先決定「來源 `regions`/`whitelist` 為空列表」的語意（沿用 GLOBAL_FILTER 或視為明確不限），避免造成篩選規則意外放寬。
4. 07-30 報告問題 7（國有財產署「地上權設定」類別）是否納入業務範圍，需人工判斷（地上權為長期用益物權，與「租賃/標租」性質相近但非同一法律關係）。
5. 07-30 報告建議 3：「太陽光電發電系統標租」類案件是否應納入黑名單排除，需業主確認業務範圍認定。
6. **環境限制說明**：本次執行環境對 `esvc.fnp.gov.tw`、`landbank.com.tw`、`udd.gov.taipei`、`depart.moe.edu.tw`、`gpwd.mnd.gov.tw` 等網域的直接連線／WebFetch 均被拒（proxy 403 或站方 403），故問題 1、6 的假設未能以 `dry_run_all_regions.py` 或 WebFetch 即時驗證 HTML 結構，僅基於 `state.json`、`sent_log.json`、`daily_report.json`、GitHub Actions 實際 job log 與程式碼靜態比對得出結論。需在有網路存取權限的環境重新確認。
