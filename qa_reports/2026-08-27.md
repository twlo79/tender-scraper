# 標案爬蟲監測報告 2026-08-27

## 一、狀況總覽

整體狀態：❌ 有異常（今日執行本身正常、推播內容經核對無誤；但「國有財產署」今日已達**連續 3 天 fetched=0**，依 08-26 報告的預告條件由 ⚠️ 升級為 ❌，且今日 Actions log 顯示 4 個子類別皆「連線成功但解析 0 筆」、無任何逾時警告，較先前更支持「parser selector 失效」的假設；另有 5 項已知精準度／基礎設施缺口延續未修，其中 2 項已造成過真實外洩）
最近執行：2026-08-27 01:30（推播 0 筆 / 新增 10 筆 / 抓取 103 筆）

| 檢查項目 | 結果 | 說明 |
|---|---|---|
| 1. 今日已執行 | ✅ | `sent_log.json` 最新 key 為 `2026-08-27 01:30`，即今天 |
| 2. 最近一次 GitHub Actions 結論 | ✅ | `daily.yml` run `33030286208`，2026-08-27T01:30:07Z，`conclusion=success`；近 9 次 run（08-19～08-27）全數 success |
| 3. line_pushed 與 total_notify 一致性 | ✅ | `_summary`：`total_fetched=103`、`total_new=10`、`total_notify=0`、`line_pushed=false`，一致（今日無案可推，未強行推播）；近 7 天逐日核對（08-21～08-27）皆一致 |
| 4. 是否所有來源 fetched=0 | ✅ | 否，13 來源中 12 個抓到資料（合計 103 筆），僅「國有財產署」為 0 |
| 5. 個別來源連續 3 天以上 fetched=0 | ❌ | 「國有財產署」連續第 3 天 0（08-25、08-26、08-27），達 08-26 報告預告的升級門檻，見問題 1 |
| 6. 來源抓取量暴跌/暴增 | ✅ | 「政府採購網」今日由連續 2 天 0 回升至 2 筆（新增 2 筆），非暴跌；其餘來源與 7 天平均相比皆在合理範圍內（Google Alerts 8 筆 vs 7 天均值 4 筆，約 2 倍，未達 3 倍門檻，且無推播，非異常） |
| 7. 應擋卻漏網的案件 | ✅ | 今日推播 0 筆，無新增外洩；但用 `passes_filters()` 對 `state.json` 全庫 823 筆重放，仍有 **6 筆停車類、5 筆廣告標租類、1 筆決標結果類**尚未被黑名單擋下，數字與 08-24～08-26 報告完全一致（未惡化亦未改善，詳見問題 2） |
| 8. 標題為分類名稱／導覽文而非案名 | ⚠️ | 「國家住宅及都市更新中心」快取中仍有「會員中心」「委員名單」「加入會員」3 筆導覽文（與前次持平），比對近 7 天 `sent_log.json` 從未被實際推播，僅為快取雜訊 |
| 9. url 指向列表頁而非個別公告頁 | ❌ | 全庫 823 筆中 462 筆（56.1%）url 為空，與前次 56.9% 持平（差異為 300 筆上限 FIFO 汰換所致）；政府採購網 94.7%（284/300）、台北市都發局 100%（34/34）、國防部政治作戰局 100%（11/11）、教育部學產基金 100%（4/4）、台北自來水處 74.1%（43/58） |
| 10. date 欄位為空 | ❌ | 全庫 823 筆中 461 筆（56.0%）date 為空，與前次 56.8% 持平；台北自來水處 100%（58/58）、土地銀行 100%（37/37）、政府採購網 94.7%（284/300）。程式邏輯確認：`is_within_date_window()`（`scraper.py:1009-1021`）在 date 與 title 皆無法解析出日期時回傳 `True`（放行），此為既有設計行為 |

## 二、異常與處理

### 問題 1（延續，08-25 新發現，今日升級為 ❌）：「國有財產署」連續 3 天 fetched=0

- **現象**：今日 `daily.yml` run `33030286208` 完整 log 顯示，4 個子類別（招標公告／地上權設定／停車場／創業租賃）**全數**回報「0 筆」，且**沒有任何** `GET 失敗` / 連線逾時警告——與 08-26 log（「地上權設定」出現明確 `Connection ... timed out`，其餘 3 類無警告記錄僅隱含成功）不同，今日連 08-26 唯一還有的逾時訊號都消失了，變成 4 個 URL 全部「請求成功、解析 0 筆」。這使根因判斷的天平更加倒向「目標頁面 HTML 結構已改版、CSS selector（`a.message-flex`／含「項次」「功能選項」表頭的 table）失效」，而非單純的網站回應變慢或防爬蟲節流。
  本 QA 執行環境對 `esvc.fnp.gov.tw` 出口仍一律回傳 `EGRESS_BLOCKED` / `403 Forbidden`（今日已用 `dry_run_all_regions.py --debug --source 國有財產署` 與 `WebFetch` 各驗證一次，皆被封鎖），故**無法在此環境開啟頁面核對 HTML 結構是否改版**，與 08-24～08-26 報告記錄的環境限制相同。
- **定位**：`scraper.py:99-106`（`get()`，未記錄 HTTP 狀態碼）；`scraper.py:340-388`（`parse_fnp()`）。
- **建議修法**（診斷用最小 diff，讓下次歸零時能直接從 Actions log 判斷是「非 200」還是「200 但表格空」，與 08-26 報告提出的診斷 diff 相同，尚未套用）：
  ```diff
   def get(url, **kwargs) -> requests.Response | None:
       try:
           r = requests.get(url, headers=HTTP_HEADERS, timeout=20, **kwargs)
           r.encoding = r.apparent_encoding or "utf-8"
  +        if r.status_code != 200:
  +            log.warning(f"  非 200 回應 {url}：HTTP {r.status_code}")
           return r
       except Exception as e:
           log.warning(f"GET 失敗 {url}：{e}")
           return None
  ```
  ```diff
       soup  = BeautifulSoup(r.text, "lxml")
       count = 0
       base_path = page["url"].split("?")[0]

  +    if not soup.select("a.message-flex"):
  +        log.warning(f"  [國有財產署/{page['cat']}] selector 'a.message-flex' 無匹配，頁面長度={len(r.text)}")
  +
       for a in soup.select("a.message-flex"):
  ```
- **是否已修**：未修（預設不自動改動程式碼）。**已連續 3 天 fetched=0，升級為最高優先，建議授權在有網路權限的環境開啟原始網頁核對 HTML 結構（見待決事項 3）。**

### 問題 2（延續，連續第 8 天未修，曾造成真實外洩）：GLOBAL_FILTER 黑名單缺「廣告標租」「停車場」「決標結果」關鍵字

- **現象**：`passes_filters()` 對 `state.json` 全庫 823 筆重放，確認仍有：
  - **6 筆停車類**：國營台鐵 2 筆（臺北車站東西區地下暨西側地上停車場、新北市鶯歌區中正段停車場）、台北市財政局 3 筆（內湖單一停車位、板橋臨時路外停車場、板橋臨時平面停車場）、郵局房地產出租 1 筆（松山區店面及4個停車位）
  - **5 筆廣告標租類**：國營台鐵「臺北車站外東側機房牆面廣告」×3、「臺北站B1層高鐵西進剪票口外牆面廣告」×2（其中 1 筆已於 08-24 真實推播至 LINE）
  - **1 筆決標結果類**：Google Alerts「新北市萬里區...公開標租之決標結果一案」
  三類數字與 08-24～08-26 報告完全一致，未惡化亦未修復。
- **定位**：`scraper.py:69-75`（`GLOBAL_FILTER["blacklist"]`）。
- **建議修法**：
  ```diff
       "blacklist": [
  -        "開標結果",
  -        "自動販賣機", "場地短期出租", "機械類標租", "共享雨傘",
  +        "開標結果", "決標結果",
  +        "自動販賣機", "場地短期出租", "機械類標租", "共享雨傘",
  +        "停車場", "停車位", "停車格", "機械式停車",
  +        "廣告標租", "牆面廣告", "廣告版位",
           "新建工程", "統包工程", "物業管理", "專案管理", "保險",
           "清洗作業", "鑄鐵直管", "延性鑄鐵", "塗裝", "管線",
           "財物採購", "勞務採購",
       ],
  ```
- **是否已修**：未修。**已連續 8 份報告（08-20～08-27）標記為最高優先，其中 08-20（停車）、08-24（廣告）兩次已造成真實外洩。**

### 問題 3（延續，連續第 8 天未修，曾造成真實外洩）：國營台鐵「臺北營業分處」地區篩選誤判

- **現象**：`scraper.py:194` 的 `parse_tra()` 僅以 `"臺北營業分處" in agency+title` 篩選，未核對案件實際座落縣市（該分處轄區橫跨新竹、花蓮等地）。狀態未變。
- **定位**：`scraper.py:193-195`。
- **建議修法**：
  ```diff
       a = item.select_one("div.btn-box a[href]")
       href = urljoin(BASE, a["href"]) if a else URL
       items.append({"title": title, "date": dt, "url": href, "agency": unit})
  -    # 只保留臺北營業分處的標案
  -    items = [i for i in items if "臺北營業分處" in i.get("agency", "") + i.get("title", "")]
  +    # 只保留臺北營業分處管轄「且」案件座落雙北的標案
  +    NON_TAIPEI_COUNTIES = ["新竹縣", "新竹市", "苗栗", "宜蘭", "花蓮", "台中", "臺中", "台東", "臺東"]
  +    items = [
  +        i for i in items
  +        if "臺北營業分處" in i.get("agency", "") + i.get("title", "")
  +        and not any(c in i.get("title", "") for c in NON_TAIPEI_COUNTIES)
  +    ]
       log.info(f"  [國營台鐵] {len(items)} 筆（臺北營業分處）")
       return items
  ```
- **是否已修**：未修。

### 問題 4（延續，連續第 5 天未修）：`log-check.yml` 缺 `actions: read` 權限，`daily_report.json` 已失效超過 30 天

- **現象**：`daily_report.json` 最新條目仍停在 2026-08-26，`sources={}`、`total_new=0`、`total_pushed=0`，與同日 `sent_log.json` 實際數字（`total_new=10, total_notify=0`）不符。今日（03:37 UTC 執行本次 QA 時）`log-check.yml` 尚未觸發（cron 03:07 UTC，近 7 天實際觸發時間 03:52～04:57 UTC，延遲屬歷史常態，非新異常），最近一次成功執行為 08-26T03:59:03Z run `32928478986`（`conclusion=success` 但仍寫入空值）。`.github/workflows/log-check.yml` 的 `permissions:` 區塊確認仍只有 `contents: write`，缺 `actions: read`，根因未變。
- **定位**：`.github/workflows/log-check.yml`；次要定位 `log_checker.py:23-29`。
- **建議修法**：
  ```diff
   permissions:
     contents: write
  +  actions: read
  ```
  ```diff
       r = requests.get(
           f"https://api.github.com/repos/{GITHUB_REPO}/actions/workflows/daily.yml/runs?per_page=2",
           headers=GH, timeout=20,
       )
  +    if r.status_code != 200:
  +        print(f"❌ 無法取得 workflow runs：HTTP {r.status_code} {r.text[:200]}")
  +        return ""
       runs = r.json().get("workflow_runs", [])
  ```
- **是否已修**：未修。套用後需人工手動 `workflow_dispatch` 觸發一次 `log-check.yml` 驗證。

### 問題 5（延續，連續第 5 天未修）：`SOURCES` 中各來源自訂的 `whitelist`/`blacklist`/`regions` 從未生效

- **現象**：`passes_filters(item)`（`scraper.py:796`）僅讀取 `GLOBAL_FILTER`，呼叫處 `scraper.py:1151` 也只傳入 `i`，`SOURCES` 內逐來源設定（如 Google Alerts 的 `"regions": ["台北","臺北","新北","gov.tw"]`）仍是死碼，狀態未變。
- **建議修法**：
  ```diff
  -def passes_filters(item: dict) -> bool:
  +def passes_filters(item: dict, src: dict | None = None) -> bool:
       title  = item.get("title", "")
       agency = item.get("agency", "")
       text   = title + agency
  +    src = src or {}

  -    regions = GLOBAL_FILTER.get("regions", [])
  +    regions = GLOBAL_FILTER.get("regions", []) + src.get("regions", [])
       if regions and not any(k in text for k in regions):
           return False

  -    whitelist = GLOBAL_FILTER.get("whitelist", [])
  +    whitelist = GLOBAL_FILTER.get("whitelist", []) + src.get("whitelist", [])
       if whitelist and not any(k in title for k in whitelist):
           return False

       blacklist = GLOBAL_FILTER.get("blacklist", [])
       if blacklist and any(k in title for k in blacklist):
           return False

       return True
  ```
  並將 `scraper.py:1151` 由 `passes_filters(i)` 改為 `passes_filters(i, src)`。
- **是否已修**：未修。

### 問題 6（延續，連續第 5 天未修）：url/date 欄位大量空白，「政府採購網」疑似 parser 失效

- **現象**：見狀況總覽第 9、10 項。「政府採購網」url/date 空白率仍達 94.7%（284/300）；雖然今日該來源已從連續 2 天 0 回升為 2 筆新增，但既有 300 筆快取中的空白比例未見改善，高度符合「目標表格 selector 長期失效、長期落入 Claude fallback 從整頁雜訊文字亂抓標題」的特徵。因本環境網路出口封鎖 `web.pcc.gov.tw`，此仍為根因推斷而非已確認結論。
- **是否已修**：未修。

### 問題 7（延續，連續第 5 天未修）：土地銀行出租不動產／台北自來水處 date 欄位結構性空白

- **現象**：近 7 天土地銀行 `fetched_sum≈14, notify_sum=0`（date 100% 空白，37 筆）；台北自來水處 `fetched_sum≈22, notify_sum=0`（date 100% 空白，58 筆）。日期窗機制對這兩來源完全失效，狀態未變。
- **是否已修**：未修。

## 三、優化建議

### 建議 1（方向 A）：問題 2、3、5 的 diff 已連續多天待拍板，建議一次套用降低外洩風險

- **依據**：`passes_filters()` 對 `state.json` 全庫 823 筆重放，確認仍有 12 筆會通過現有篩選但屬非目標案件（廣告標租 5、停車類 6、決標結果 1），且已有 2 次（08-20 停車、08-24 廣告）真實外洩紀錄，數字連續 4 份報告未變。三組 diff 彼此不衝突，可一次套用。
- **做法**：套用「二、異常與處理」問題 2、3、5 的 diff。
- **預期效果**：排除已確認的 4 類誤報型態（廣告版位、停車、決標結果、地區誤判），並讓 `SOURCES` 逐來源篩選設定真正生效。

### 建議 2（方向 B）：「國有財產署」今日已達 3 天門檻應立即排查，「政府採購網」結構性空白比例未隨今日回補改善

- **依據**：近 7 天各來源 notify_sum：國營台鐵 2、Google Alerts 1，其餘 11 個來源（含國有財產署、政府採購網）皆為 0。抽查台北市都發局（fetched=30/天）、台北市財政局、國防部政治作戰局，快取標題皆屬雙北不動產標租範疇，屬「近期剛好無新案」，非結構性失效。但國有財產署今日已達連續 3 天 fetched=0（問題 1），政府採購網雖今日恢復抓取但 url/date 空白率仍高達 94.7%（問題 6），兩者皆屬「結構性失效」風險而非單純無新案。
- **做法**：優先處理問題 1（授權在有網路權限的環境核對國有財產署 4 個頁面的 HTML 結構）；政府採購網建議下次比對時同步核對 `target_table` selector 是否仍能匹配「項次」「功能選項」表頭。土地銀行／台北自來水處若短期無法修復 date 解析（問題 7），建議評估是否移除或改抓正確分類頁。
- **預期效果**：把「無新案」與「來源已經失效但看起來像沒新案」區分開，避免長期靜默漏抓真正的雙北租賃案件。

### 建議 3（方向 C，新增來源候選，次要優先，因問題 1、2 仍是更高優先的精準度缺口，暫不重複展開細節；如需完整候選清單請參考 `qa_reports/2026-08-25.md` 建議 4）

## 四、待決事項

1. 是否核准套用「二、異常與處理」問題 2（黑名單新增停車／廣告標租／決標結果關鍵字）與問題 3（國營台鐵地區二次過濾）、問題 5（`SOURCES` 逐來源篩選生效）的 diff？此三項已連續多日待拍板，且已有 2 次真實外洩紀錄。
2. 是否核准套用問題 4 的 `log-check.yml` 權限修復（新增 `actions: read`）？修復後需人工手動觸發一次 `workflow_dispatch` 驗證。
3. 「國有財產署」今日已連續 3 天 `fetched=0`（達升級門檻，已列為 ❌），是否授權在有網路權限的環境開啟原始網頁核對 HTML 結構、修正 parser？今日 Actions log 顯示 4 個子類別皆「連線成功但解析 0 筆」、無任何逾時警告，較先前更支持 selector 已失效的假設，建議優先處理。
