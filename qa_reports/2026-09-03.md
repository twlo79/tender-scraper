# 標案爬蟲監測報告 2026-09-03

## 一、狀況總覽

整體狀態：❌ 有異常（執行層面、抓取層面本身皆正常；品質層面本次首度取得具體證據，確認「國營台鐵」來源的地區篩選對該來源完全失效，已造成 2 筆非雙北案件〔09-01 基隆市、09-03 桃園市〕被真實推播給 LINE 訂閱者——這是本次新發現、業務衝擊最高的問題；另有 6 項延續自 07-29～09-02 報告、連續多日未拍板的已知缺口，程式碼自 08-28 commit `b504d6c` 後未再變動，全數仍未修）

最近執行：2026-09-03 01:30（推播 1 筆 / 新增 2 筆 / 抓取 92 筆）

| 檢查項目 | 結果 | 說明 |
|---|---|---|
| 1. 今日已執行 | ✅ | `sent_log.json` 最新 key 為 `2026-09-03 01:30`，即今天 |
| 2. 最近一次 GitHub Actions 結論 | ✅ | `daily.yml` run `33703888418`，2026-09-03T01:30:09Z，`conclusion=success`；近 30 次 run（08-06～09-03）全數 success，無失敗紀錄 |
| 3. line_pushed 與 total_notify 一致性 | ✅ | `_summary`：`total_fetched=92`、`total_new=2`、`total_notify=1`、`line_pushed=true`，一致；近 7 天（08-28～09-03）7 筆記錄逐一核對亦全數一致 |
| 4. 是否所有來源 fetched=0 | ✅ | 否，13 來源今日皆 >0（4～30 筆不等） |
| 5. 個別來源連續 3 天以上 fetched=0 | ✅ | 無。近 7 天各來源每日 fetched 皆 >0（見 `qa_report.py` 趨勢表） |
| 6. 來源抓取量暴跌/暴增 | ✅ | 近 7 天無單日暴跌逾 50% 或暴增逾 3 倍案例（`qa_report.py` 逐日比對） |
| 7. 應擋卻漏網的案件 | ❌ | **本次新發現**：09-03 推播的國營台鐵「【臺北營業分處】【土地標租】【**桃園市桃園區**桃園段武陵小段99、99－4地號土地】」實際地點在桃園，非雙北；回查 32 天內另一筆真實推播 09-01「【臺北營業分處】【土地出租】【**基隆市安樂區**西定段326之2地號部分土地（供設置電動汽車公共充電樁）】」同樣非雙北——即 32 天內國營台鐵總計推播 9 筆，其中至少 4 筆（2 筆地區錯誤 + 沿用前次報告已知的 2 筆類別錯誤：08-20 停車場標租、08-24 廣告標租）不符「雙北不動產租賃」目標，來源層級誤報率約 44%。詳見「問題 1」 |
| 8. 標題為分類名稱／導覽文而非案名 | ⚠️ | 今日推播內容非此類；但 32 天內 09-01 國有財產署推播「【招標公告】北區分署 115年第17批 招標公告 公告:…」屬批次彙總列，非個別案名，為延續問題（見問題 4） |
| 9. url 指向列表頁而非個別公告頁 | ❌ | `state.json` 全庫抽查數字與 09-02 報告一致：政府採購網 300 筆中 284 筆（94.7%）`url` 空白、台北市都發局 34/34（100%）空白、國防部政治作戰局 11/11（100%）空白、教育部學產基金 4/4（100%）空白。本次進一步定位根因：這些筆全部是 dedup schema 從純字串升級為 `{title,date,url}` 結構**之前**留存的舊記錄（`find_new_items()` 對舊字串項目一律補 `url:""`），且這幾個來源更新頻率極低（32 天新增分別僅 5、3、0、1 筆），至今未被新記錄汰換掉，非目前 parser 持續在產生空 url |
| 10. date 欄位為空、導致日期窗被跳過放行 | ❌ | 台北自來水處 59 筆快取**全數（100%）** `date` 為空、且**全數（0/59）不含任何租賃相關白名單詞**——即此來源迄今從未真正抓到過雙北不動產租賃案件，`News.aspx` 抓的是機關全類別公告（財物/工程/勞務採購皆混在裡面，靠白名單擋下，未造成誤推）；土地銀行出租不動產 39 筆快取同樣 100% `date` 空白。與歷次報告一致，狀態未變 |

## 二、異常與處理

### 問題 1（新發現，本次首度確認，業務衝擊最高）：國營台鐵地區篩選對此來源完全失效，已造成 2 筆非雙北案件真實推播

- **現象**：`scraper.py:195` 的來源內部篩選 `items = [i for i in items if "臺北營業分處" in i.get("agency", "") + i.get("title", "")]` 只保留「臺北營業分處」轄下案件；但實際查證發現該分處轄區**橫跨雙北以外縣市**（鐵路沿線行政區劃分，非地理雙北）。而後續 `passes_filters()`（`scraper.py:796-814`）的地區檢查是 `text = title + agency`、`any(k in text for k in regions)`，由於**每一筆**國營台鐵項目的 `agency` 與 `title` 都固定包含「臺北營業分處」字樣，其中的「臺北」二字恆定命中 `GLOBAL_FILTER["regions"]`，導致地區篩選對此來源**形同虛設、100% 必過**，無論案件實際座落何處。

  已證實造成 2 次真實外洩：
  - **2026-09-03**（本次執行 job log 第 22 行證實）：`【臺北營業分處】【土地標租】【桃園市桃園區桃園段武陵小段99、99－4地號土地】【115.9.3~115.9.17】` → 地點為**桃園市桃園區**
  - **2026-09-01**（`sent_log.json` 證實）：`【臺北營業分處】【土地出租】【基隆市安樂區西定段326之2地號部分土地(土地供設置電動汽車公共充電樁)】【115.09.01~115.09.15】` → 地點為**基隆市安樂區**

  兩者皆已透過 LINE broadcast 推播給所有訂閱好友，是本次監測期間唯一造成真實外洩的異常（其餘延續問題目前 `notify_sum` 皆為 0，尚未造成誤推）。

- **定位**：`scraper.py:194-196`（`parse_tra()` 的分處篩選邏輯）+ `scraper.py:796-804`（`passes_filters()` 地區檢查對 `agency` 欄位的通用假設，在此來源不成立）。

- **建議修法**（scoped 在 `parse_tra()` 內，不影響 `passes_filters()` 對其他來源的既有邏輯，例如「瑠公管理處」刻意用機關名稱代表地區的設計）：
  ```diff
       a = item.select_one("div.btn-box a[href]")
       href = urljoin(BASE, a["href"]) if a else URL
       items.append({"title": title, "date": dt, "url": href, "agency": unit})
       # 只保留臺北營業分處的標案
       items = [i for i in items if "臺北營業分處" in i.get("agency", "") + i.get("title", "")]
  +    # 「臺北營業分處」轄區橫跨雙北以外縣市（已實際發生：09-01 基隆市安樂區、
  +    # 09-03 桃園市桃園區皆因 agency/title 固定含「臺北營業分處」字樣而誤判通過
  +    # GLOBAL_FILTER 地區篩選）。改為檢查標題「地點」括號（第 3 個【】區塊）
  +    # 是否真的落在雙北，而非依賴分處名稱。
  +    def _tra_is_shuangbei(title: str) -> bool:
  +        parts = re.findall(r"【(.*?)】", title)
  +        loc = parts[2] if len(parts) > 2 else title
  +        return any(k in loc for k in ("台北", "臺北", "新北"))
  +    items = [i for i in items if _tra_is_shuangbei(i["title"])]
       log.info(f"  [國營台鐵] {len(items)} 筆（臺北營業分處）")
       return items
  ```
  （`re` 已在檔案頂部 `import re`，無需額外 import。）

- **驗證阻礙**：本次執行環境對 `www.railway.gov.tw` 為 `EGRESS_BLOCKED`（`WebFetch` 與 `dry_run_all_regions.py --debug --source 國營台鐵` 皆已嘗試，見下方指令輸出），無法即時重跑驗證此 diff 套用後的筆數變化，故依規則**未實際修改程式碼**。
- **是否已修**：未修（依規則預設不自動改程式碼；且此為新問題，需人工核准後再套用並於有網路權限環境驗證）。

### 問題 2（延續，自 2026-07-29 起已第 36 天未修）：政府採購網 parser 抓到與雙北不動產完全無關的全國性標案

- 狀態與 09-02 報告完全一致：`state.json` 300 筆快取中仍為大量全國性無關標案（滷肉飯節、艦艇防污漆修補等），`notify_sum`（32 天累計）仍為 0，尚未造成誤推播，但持續佔用 dedup 空間。程式碼自 08-28 後未變動。
- **定位**：`scraper.py:390-455`（`_pcc_fetch_keyword()`）
- **建議修法**：同 09-02 報告，於回傳前加一道標題含關鍵字的防禦性檢查（`items = [i for i in items if kw in i.get("title","")]`）。
- **驗證阻礙**：`web.pcc.gov.tw` 本次仍為 `EGRESS_BLOCKED`。
- **是否已修**：未修，第 36 天。

### 問題 3（延續，自 2026-07-29 起已第 36 天未修）：土地銀行出租不動產 parser 選擇器過寬，抓到全站系統維護公告

- 狀態不變：本次重新抽查 `state.json`「土地銀行出租不動產」快取最新 39 筆，**100% 為系統維護／防詐公告**（如「一卡通票證公司系統維護作業」「偽冒本行App提醒」），僅 1 筆疑似相關「保管箱出租契約」但也非不動產租賃，且該筆因不含雙北地區關鍵字，已被地區層擋下未推播。32 天累計 `fetched_sum=60、new_sum=14、notify_sum=0`。
- **定位**：`scraper.py:592-617`（`parse_landbank()`）
- **建議修法**：同 09-02 報告，收斂選擇器或加標題關鍵字防禦（需標題含「出租」或「不動產」）。
- **驗證阻礙**：`www.landbank.com.tw` 本次仍為 `EGRESS_BLOCKED`。
- **是否已修**：未修，第 36 天。

### 問題 4（延續，長期未拍板）：`GLOBAL_FILTER` 黑名單缺「停車場」「決標結果」「廣告標租」；國有財產署 `skip_global_filter=True` 讓分類彙總列（非個別案名）漏網

- 狀態不變：`scraper.py:69-75` 黑名單仍未加入「停車場」「決標結果」「廣告標租」；`scraper.py:740-746` 國有財產署仍為 `skip_global_filter=True`，`FNP_PAGES`（`scraper.py:328-335`）仍含「停車場」分類頁。09-01 推播的「【招標公告】北區分署 115年第17批 招標公告」即為此問題的分類彙總列樣本（非新增外洩，但性質確認未變）。
- **建議修法**：同 09-02 報告的兩組 diff（黑名單新增關鍵字；移除 FNP「停車場」頁並拿掉 `skip_global_filter`）。
- **是否已修**：未修。

### 問題 5（延續，長期未拍板）：安幸 ERP 匯入 `push_to_anxing()` header 值未 `.strip()`，本次執行再次真實觸發

- **現象**：本次 job log（`100488786413`）第 24 行：`⚠ ERP 匯入異常（不影響 LINE 推播）：Invalid leading whitespace, reserved character(s), or return character(s) in header value: '***\n'`。09-01 曾觸發過一次（當時 `notify=4`），09-02 因 `notify=0` 未觸發，本次 `notify=1` 再度真實觸發，證實 `ANXING_URL` 或 `ANXING_KEY` 這兩個 GitHub Secret 中至少一個結尾仍帶換行字元，且只要有推播就必定失敗（2/2）。
- **定位**：`scraper.py:1131-1132`
- **建議修法**：
  ```diff
       url = os.getenv("ANXING_URL", "")
       key = os.getenv("ANXING_KEY", "")
  +    url = url.strip()
  +    key = key.strip()
  ```
  仍需人工至 GitHub repo Settings → Secrets 重新貼上不含換行的 `ANXING_KEY`／`ANXING_URL`，程式碼端 `.strip()` 僅為防禦性保底。
- **是否已修**：未修。此整合上線以來凡有推播必失敗（2/2）。

### 問題 6（延續，長期未拍板）：`SOURCES` 中各來源自訂 `whitelist`/`blacklist`/`regions` 從未生效（死碼）

- 狀態不變：`passes_filters(i)` 呼叫處（`scraper.py:1188`）仍只傳入單一參數，各來源自訂設定從未被讀取。
- **建議修法**：同 09-02 報告，`passes_filters(item, source_cfg=None)` + 呼叫處改傳入 `src`。
- **是否已修**：未修。

## 三、優化建議

### 建議 1（方向 A）：國營台鐵單一來源誤報率達 44%，優先套用問題 1 的區域判定修法

- **依據**：32 天累計，國營台鐵 `notify_sum=9`，其中可具體指出 4 筆不符「雙北不動產租賃」目標：09-01（基隆市，地區錯誤）、09-03（桃園市，地區錯誤）、08-20（新北市鶯歌區停車場標租，類別錯誤）、08-24（臺北車站廣告標租，類別錯誤）。單一來源誤報率 4/9 ≈ 44%，是目前所有來源中誤報率最高者，且是唯一本月內兩度造成真實 LINE 誤推播的來源。
- **做法**：問題 1（地區判定）+ 問題 4（黑名單補「停車場」「廣告標租」）兩組修法合併套用，可涵蓋此來源目前已知的全部 4 種誤報樣態；套用後需以 `dry_run_all_regions.py --debug --source 國營台鐵` 比對修法前後筆數（目前環境 `EGRESS_BLOCKED`，需於有網路權限環境驗證）。
- **預期效果**：國營台鐵誤報率由 44% 大幅下降，後續若再有基隆、桃園等鐵路沿線非雙北案件，會在地區層被正確擋下，不再依賴分處名稱誤判通過。

### 建議 2（方向 A，延續）：問題 2、3、4、6 的既有 diff 儘速一次套用

- **依據**：與 09-01/09-02 報告相同——政府採購網（32 天 `fetched_sum=68`／`notify_sum=0`）、土地銀行（`fetched_sum=60`／`notify_sum=0`）快取內容分別 100% 為全國無關標案／全站銀行公告，長期佔用 300 筆 dedup 上限；問題 4 的 FNP 分類彙總列已有 1 筆真實漏網推播（09-01）。四組 diff 互不衝突，可一次套用並用 `dry_run_all_regions.py --debug` 做全來源前後對照。
- **預期效果**：政府採購網／土地銀行快取回歸雙北租賃相關內容；國有財產署推播回歸個別案名，不再出現分類彙總列。

### 建議 3（方向 B）：台北自來水處 32 天 0 筆通過，且快取內容 0/59 曾出現租賃相關詞——建議調整抓取頁面而非直接判定為壞來源

- **依據**：本次全量抽查 `state.json`「台北自來水處」現有 59 筆快取，**沒有任何一筆**標題含白名單關鍵字（出租/標租/租賃/招租/房地/不動產等），內容清一色為財物/工程/勞務採購或委辦服務案；`date` 欄位 59/59（100%）空白。32 天累計 `fetched_sum=96`、`new_sum=6`、`notify_sum=0`。目前抓取的 `News.aspx?n=D2818696FF5048B8` 疑似是該處**全類別公告看板**，而非「標租／招租」專屬分類頁。
- **做法**：建議在有網路權限環境確認 water.gov.taipei 網站上是否存在「不動產標租」或「土地招租」的獨立分類連結；若存在，改抓該分類頁網址（可望大幅提升相關案件比例、且該分類頁的公告日期欄位格式可能與目前的 `data-title` 對應不同，可望連帶修正 date 為空的問題）；若確認該處近年並無不動產租賃業務、全類別頁本來就不會有相關案件，則可考慮從來源清單移除以節省每日抓取成本。兩者皆需人工核對，非猜測即可決定。
- **預期效果**：若改抓對的分類頁，此來源終於可能產出真正雙北租賃案件；若確認該機關本來就無此類業務，移除可省下每次執行的一次頁面請求，不影響任何現有推播內容（因為至今從未貢獻任何推播）。

### 建議 4（方向 B，延續 09-02 報告）：台北市都發局／台北市財政局／國防部政治作戰局／農業部瑠公管理處維持現狀，不建議移除

- **依據**：32 天累計 `notify_sum` 皆為 0（都發局 `fetched_sum=720`、財政局 351、政治作戰局 264、瑠公管理處 480），但本次重新抽查 `state.json` 快取內容（如「公告標租本市南港區南港機廠社會住宅1區內4戶商業設施」「公告114年臺北市等6縣市32宗不動產標租」），確認皆為真實雙北不動產標租相關案件，只是官方公告本身低頻更新（都發局 32 天僅 3 筆「new」）。與台北自來水處（0/59 曾出現租賃詞）、土地銀行（快取內容 100% 無關）性質不同。
- **做法**：維持現狀，不移除、不調整篩選條件。
- **預期效果**：避免誤刪仍在正常運作、只是低頻的正確來源。

### 建議 5（方向 C）：本次因網路環境限制，仍暫不提出新來源候選

- 本次執行環境對外部網域（`www.railway.gov.tw`、`web.pcc.gov.tw`、`www.landbank.com.tw`、`www.water.gov.taipei` 等）一律 `EGRESS_BLOCKED`（已用 `WebFetch` 與 `dry_run_all_regions.py` 雙重驗證），且本次方向 A／B 已有明確、可具體量化的問題需優先處理（尤其問題 1 已造成真實外洩），故延續 09-01/09-02 報告的判斷，暫緩新來源調查。建議留待下次有網路權限的環境，或由人工提供候選清單。

## 四、待決事項

1. 【新增，本次首度發現且已造成 2 次真實外洩】問題 1（國營台鐵地區篩選失效，09-01 基隆市、09-03 桃園市案件被誤推播）：是否核准套用 `parse_tra()` 的地點判定 diff？並授權在有網路權限環境用 `dry_run_all_regions.py --debug --source 國營台鐵` 驗證？**建議優先於下列所有延續事項處理，因為是唯一本月內造成真實 LINE 誤推播的項目。**
2. 【長期未拍板，自 07-29 起第 36 天】問題 2（政府採購網 parser 抓到全國性無關標案）：是否核准防禦性 diff？並授權人工核對查詢邏輯與 `target_table` 選擇器？
3. 【長期未拍板，自 07-29 起第 36 天】問題 3（土地銀行 parser 選擇器過寬）：是否核准防禦性 diff？並授權人工核對正確 DOM 結構？
4. 【長期未拍板】問題 4（黑名單缺「停車場」「決標結果」「廣告標租」；國有財產署 `skip_global_filter=True`）：diff 是否核准套用？
5. 【長期未拍板】問題 5（安幸 ERP header 值未 `.strip()`，本次再次真實觸發，累計 2/2 次推播必失敗）：是否請人工至 GitHub repo Secrets 重新設定 `ANXING_KEY`／`ANXING_URL`？程式碼端 `.strip()` 防禦修法是否核准？
6. 【長期未拍板】問題 6（`SOURCES` 逐來源篩選設定從未生效）：diff 是否核准套用？
7. 【新增】建議 3（台北自來水處 32 天 0 筆通過、快取內容 0/59 曾含租賃詞）：是否授權人工核對該處網站是否有專屬「標租／招租」分類頁可替換目前的全類別公告頁？若確認無相關業務，是否核准移除此來源？
8. 方向 C（新來源候選）：是否於下次有網路權限時再行補上，或由人工提供候選清單？
