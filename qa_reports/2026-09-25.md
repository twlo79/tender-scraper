# 標案爬蟲監測報告 2026-09-25

> **重要前提**：本報告產生於 **2026-09-25 01:10 UTC**，`daily.yml` 依歷史模式固定於每日 **01:30 UTC**（台灣時間約 10:37）被 Make.com 觸發，換言之本次監測執行時**今日的排程尚未到執行時間**（早了約 20 分鐘）。因此本報告的「執行層面」與「抓取層面」檢查，是以**最近一次已完成的 run #121（2026-09-24 01:30 UTC，`conclusion=success`）**資料為準，而非今日新資料——這與 09-24 資料完全相同，非新事件。

## 一、狀況總覽

整體狀態：⚠️ 有注意事項（**無新增異常**；既有問題 27、28、3、4、22、25、26 等仍未修復，屬延續風險，非今日新發生）

最近執行：2026-09-24 01:30 UTC（GitHub Actions run #121，`35943184881`，`conclusion=success`）— 推播 8 筆 / 新增 80 筆 / 抓取 188 筆

| 檢查項目 | 結果 | 說明 |
|---|---|---|
| 1. 今日已執行 | ⚠️ | `sent_log.json` 最新 key 仍為 `2026-09-24 01:30`；但本報告產生於 `2026-09-25 01:10 UTC`，早於歷史每日觸發時間 `01:30 UTC` 約 20 分鐘，**屬監測時機過早，非 Make.com 未觸發的故障**。GitHub Actions 端亦確認今日尚無 `2026-09-25` 的 run 記錄（`actions_list` 最新仍是 run #121, 09-24）|
| 2. 最近一次 GitHub Actions 結論 | ✅ | run #121（`35943184881`）`conclusion=success`，`created_at`=`2026-09-24T01:30:04Z` |
| 3. line_pushed 與 total_notify 一致性 | ✅ | `sent_log._summary`：`total_notify=8, line_pushed=true`，逐筆核對 `政府採購網`（6 筆）+ `Google Alerts`（2 筆）＝8 筆，無缺漏無多餘 |
| 4. 是否所有來源 fetched=0 | ✅ | 否，13 個來源皆 `fetched>0`，合計 188 筆 |
| 5. 個別來源連續 3 天以上 fetched=0 | ✅ | 近 7 天趨勢表核對：無來源目前為連續 0；`國有財產署`（0,0,1,1,1,1）、`政府採購網`（0,0,0,1,53,58）、`國防部政治作戰局`（0,11,11,11,20,20）皆已由 0 轉為穩定產出，非斷流 |
| 6. 來源抓取量暴跌/暴增 | ✅ | 09/24 相較 09/23：政府採購網 53→58（+9%）、Google Alerts 9→21（+133%，屬近期正常波動範圍，非首見）；無新增暴跌/暴增個案 |
| 7. 應擋卻漏網的案件 | ❌（延續問題 27，未修） | `sent_log`『政府採購網』第 6 筆「基隆市安樂區西定段326-2地號部分土地(土地供設置電動汽車公共充電樁)標租案」title 已明確標示「基隆市」，非雙北，仍通過篩選並已真實推播（09-24）。程式碼確認：`scraper.py:826-828` 目前僅用 `regions` OR 判斷 `title+agency`，未擋跨縣市案件，**問題原封不動** |
| 8. 標題為分類名稱／導覽文而非案名 | ❌（延續問題 28，未修） | `sent_log`『Google Alerts』第 1 筆「臺北市政府財政局標租市有非公用不動產投標單」為表單頁標題而非案名，`GLOBAL_FILTER["blacklist"]`（`scraper.py:69-75`）確認仍未含「投標單」關鍵字 |
| 9. url 指向列表頁而非個別公告頁 | ⚠️（延續，環境限制） | sandbox 對外連線仍被 agent proxy 阻擋（`curl https://web.pcc.gov.tw/` → 403），本次同樣無法重新抓取驗證列表頁 fallback 比例是否變化 |
| 10. date 欄位為空、導致日期窗被跳過放行 | ❌（延續，結構性） | `state.json` 實測：台北自來水處 103/103 筆 date 空白（100%）、土地銀行出租不動產 44/44 筆（100%）、都發局 1/65、政治作戰局 0/31、學產基金 0/11、政府採購網 174/300（58%，多為修復前舊資料尚未淘汰）——與 09-24 報告數字一致，無惡化也無改善 |

**一項正面更新（非問題，僅記錄）**：問題 22（政府採購網 `r.encoding` 猜測失敗導致亂碼）——09-24 報告記錄為 34/300（11.3%）亂碼，今日實測 `state.json['政府採購網']` **亂碼筆數已降為 0/300**。核對最新 10 筆（`pk=70023347`～`70023622`）皆為正確編碼且 date/url 齊全，判斷是舊亂碼資料已隨 300 筆 FIFO 上限自然淘汰完畢，而非程式碼被修改（`scraper.py:102/420/546` 的 `r.encoding = r.apparent_encoding or "utf-8"` 仍是原樣）。根因（apparent_encoding 猜測邏輯）仍是潛在風險，只是暫無現存受影響資料。

## 二、異常與處理

本次無新增異常。以下為既有未修問題的**延續確認**（今日重新核對程式碼與資料，狀態未變）：

### 問題 27（延續，優先度最高）：政府採購網地區篩選被 `agency` 覆蓋，跨縣市案件仍會被推播
- 現象：見上表項目 7。09-24 已發生 1 次真實誤報（基隆案件）。
- 定位：`scraper.py:820-838` `passes_filters()`：
  ```python
  regions = GLOBAL_FILTER.get("regions", [])
  if regions and not any(k in text for k in regions):  # text = title + agency
      return False
  ```
  `agency` 含雙北字樣即可讓 title 已自報外縣市的案件通過。
- 建議修法（未變更，維持昨日 diff，附可直接套用片段）：
  ```diff
   def passes_filters(item: dict) -> bool:
       title  = item.get("title", "")
       agency = item.get("agency", "")
       text   = title + agency

       regions = GLOBAL_FILTER.get("regions", [])
  +    OTHER_CITIES = [
  +        "基隆市", "桃園市", "新竹市", "新竹縣", "苗栗縣",
  +        "台中市", "臺中市", "彰化縣", "南投縣", "雲林縣",
  +        "嘉義市", "嘉義縣", "台南市", "臺南市", "高雄市",
  +        "屏東縣", "宜蘭縣", "花蓮縣", "台東縣", "臺東縣",
  +        "澎湖縣", "金門縣", "連江縣",
  +    ]
  +    if regions and any(k in title for k in OTHER_CITIES) and not any(k in title for k in regions):
  +        return False
       if regions and not any(k in text for k in regions):
           return False
  ```
- 是否已修：**未修**，待人工核准（見待決事項 1）。

### 問題 28（延續）：Google Alerts 推播「投標單」表單標題
- 定位：`GLOBAL_FILTER["blacklist"]`（`scraper.py:69-75`）未含「投標單」。
- 建議修法：blacklist 新增 `"投標單"`。
- 是否已修：**未修**，待人工核准（見待決事項 2）。

### 問題 3、4、22、25、26（延續，未變動，不重複貼程式碼）
- 問題 3：`FNP_PAGES`（`scraper.py:337-341`）仍含「地上權設定」「停車場」兩非租賃分類頁，且 `skip_global_filter=True`（`scraper.py:769`）使黑名單完全失效，程式碼未變。
- 問題 4：ANXING ERP header 未 `.strip()`（累計已知重現 7 次，今日因無新 run 未再觸發，程式碼未變）。
- 問題 22：如上「正面更新」段落，資料層面已自然清零，但根因程式碼未變，仍建議修，優先度降低。
- 問題 25：國防部政治作戰局 `state.json` 仍保留混入的社宅導覽頁記錄，程式碼未變。
- 問題 26：土地銀行出租不動產近 7 天 notify 仍為 0，`state.json` 44 筆歷史紀錄未變。

## 三、優化建議

### 建議 1（延續，方向 A，依據問題 27）：修正 `passes_filters()` 地區判定
- 依據：09-24 實際推播 8 筆中有 1 筆（12.5%）為明確非雙北案件，是已發生的真實誤報。
- 做法：見問題 27 diff。
- 預期效果：堵住「機關雙北、資產跨縣市」這類全國性來源（政府採購網、Google Alerts）特有誤判。

### 建議 2（延續，方向 A，依據問題 28）：blacklist 新增「投標單」
- 依據：09-24 Google Alerts 2 筆推播中 1 筆（50%）為表單標題而非案名。
- 做法：見問題 28 diff。

### 建議 3（更新，方向 A，依據問題 22 最新數據）：`r.encoding` 猜測邏輯優先度可調降，但仍建議修
- 依據：亂碼資料已隨 300 筆 FIFO 自然歸零（34→0），代表**短期內不影響推播品質**；但根因（`apparent_encoding` 猜測）未修，一旦政府採購網伺服器再次回傳異常編碼標頭，同樣問題會重現。
- 做法：政府採購網固定用 `r.encoding = "utf-8"`（該站已知回應皆為 UTF-8，不需猜測）。
- 預期效果：徹底排除猜測失敗風險，屬低成本高把握修法，可與問題 27/28 一併核准。

### 建議 4（延續，方向 B，依據問題 26）：土地銀行出租不動產建議移除或修 parser
- 依據：近 7 天 notify 總數 0，44 筆歷史紀錄 0 筆命中業務範圍，date/url 分別 100%/11% 空白，判斷 parser 抓錯區塊。

方向 C（新增來源）：既有問題 27、28 尚未核准修復，暫緩評估新來源，維持昨日判斷不變。

## 四、待決事項

1.【延續，優先度最高，已有真實誤報】問題 27（政府採購網地區篩選被 agency 覆蓋）：是否核准 diff（建議 1）？
2.【延續】問題 28（Google Alerts「投標單」）：是否核准 blacklist 新增（建議 2）？
3.【延續，累計第 7 次真實重現】問題 4（ANXING ERP header 未 `.strip()`）：修法僅 2 行，是否核准？
4.【延續】問題 3（`FNP_PAGES` 含「地上權設定」「停車場」+ `skip_global_filter=True`）：是否核准移除兩分類頁？
5.【更新，優先度可降但仍待決】問題 22（`r.encoding` 猜測）：亂碼已自然歸零，是否仍核准固定為 `"utf-8"`（建議 3）以防復發？
6.【延續】問題 25（政治作戰局混入社宅導覽頁）：方案 A（補黑名單）或方案 B（移除該分頁）？
7.【延續】問題 26（土地銀行疑似抓錯區塊）：是否核准先移除來源（建議 4）？
8.【新增，屬本次監測本身】**本 QA routine 的排程時間早於 `daily.yml` 觸發時間**（01:10 UTC vs 01:30 UTC），導致每次執行都只能看到「前一天」的資料而非「今天」的執行結果。建議將此 routine 的排程往後調整至 `daily.yml` 完成後（例如 02:00 UTC 之後），才能在同一天內驗證當日執行結果。
9.【延續，其餘低優先度項目：問題 1、5、6、7、9、11、13、14、15、17、23、24】維持累積待辦，未變動，詳見歷史報告（09-23、09-24）。
