# 標案爬蟲監測報告 2026-09-26

> 本次監測執行於 **2026-09-26 01:15 UTC**，早於 `daily.yml` 慣例觸發時間（近 14 天皆為 **01:30 UTC** 整，含今日以外全數 `conclusion=success`）。截至檢查當下，**今日 09-26 的執行尚未發生**，`sent_log.json` 最新一筆仍是 **2026-09-25 01:30 UTC**（run #122）。以下沿用 09-21～09-25 報告已建立的問題編號（問題 1～29），本次未發現新問題，僅確認狀態延續。

## 一、狀況總覽

整體狀態：⚠️ 有注意事項（**無新增未修異常**；問題 27、28、3、4、25、26 延續未修，待人工核准；問題 22、29 已修復，尚待今日真實 run 驗證）

最近執行：2026-09-25 01:30 UTC（GitHub Actions run #122，`36082278094`，`conclusion=success`）— 推播 8 筆 / 新增 54 筆 / 抓取 162 筆（今日尚無新資料，數字與 09-25 報告相同）

| 檢查項目 | 結果 | 說明 |
|---|---|---|
| 1. 今日已執行 | ⚠️（非真異常，時間點問題） | `sent_log.json` 最新 key 仍為 `2026-09-25 01:30`。目前為 `2026-09-26 01:15 UTC`，距歷史慣例觸發時間 `01:30 UTC` 僅剩約 15 分鐘，**尚未到觸發時間**，非 Make.com 未觸發。此為問題 7（見待決事項）第 2 次連續重現——QA routine 排程早於 `daily.yml`，導致每天這個時段檢查都會誤判「今天沒執行」 |
| 2. 最近一次 GitHub Actions 結論 | ✅ | 查詢 `mcp__github__actions_list`：近 7 筆 run（#116～#122）`conclusion` 全為 `success`，時間點皆落在 01:30:0x UTC，無失敗紀錄 |
| 3. line_pushed 與 total_notify 一致性 | ✅ | 檢查近 14 天 `sent_log._summary`：`total_notify=0` 時 `line_pushed=false`、`total_notify>0` 時 `line_pushed=true`，14 筆全部一致，無缺漏無多餘 |
| 4. 是否所有來源 fetched=0 | ✅ | 09-25 run：13 個來源皆 `fetched>0`，合計 162 筆 |
| 5. 個別來源連續 3 天以上 fetched=0 | ✅ | 近 14 天逐來源核對：無來源連續 3 天以上 `fetched=0`（國有財產署、政府採購網曾有 0，但緊接著恢復非零） |
| 6. 來源抓取量暴跌/暴增 | ✅ | 近 14 天無新增異常暴跌/暴增。政府採購網自 09-22 由 0 躍升至 50+ 屬已知原因（commit `8740c25` 09-22 改打「財物出租查詢」子系統，非爬蟲異常），09-24→09-25 為 58→51（-12%，查詢窗正常滾動範圍） |
| 7. 應擋卻漏網的案件 | ❌（延續問題 27，未修） | 09-25 report 已確認「基隆市安樂區西定段326-2地號…標租案」（跨縣市，非雙北）曾於 09-24、09-25 兩次出現在政府採購網推播清單。今日重新以現有程式碼手動測試 `passes_filters()`：若無 `agency` 欄位比對，該筆標題本身不含「台北/臺北/新北」，理論上會被擋下；但實際歷史推播記錄顯示它通過了篩選，可證實根因（`agency` 覆蓋 region 判斷）仍未修復，明日起若政府採購網再出現非雙北案件，仍會被推播 |
| 8. 標題為分類名稱／導覽文而非案名 | ✅（09-25 無實例，問題 28 根因未修） | 09-25 的 8 筆推播標題逐一檢視皆為具體案名。`GLOBAL_FILTER["blacklist"]`（`scraper.py:69-75`）仍未含「投標單」，風險未消除 |
| 9. url 指向列表頁而非個別公告頁 | ⚠️（延續，環境限制） | 本次 sandbox 對 `web.pcc.gov.tw`、`gpwd.mnd.gov.tw`、`depart.moe.edu.tw`、`udd.gov.taipei`、`www.landbank.com.tw`、`esvc.fnp.gov.tw`、`www.google.com` 等全部來源網域連線皆被 agent proxy 擋下（403），無法用 `dry_run_all_regions.py --debug` 對外即時驗證各來源 url 是否為個別公告頁。此為 GitHub Actions 環境與本 sandbox 環境差異的已知限制，非新問題 |
| 10. date 欄位為空、導致日期窗被跳過放行 | ⚠️（延續，數字持續變動） | `state.json` 實測（300 筆上限 FIFO）：台北自來水處 104/300（date 全空白）、土地銀行出租不動產 46/300（url 多為空）、政府採購網 date 欄位皆有值但 118/300 是「已知空 date 舊格式殘留」，比例較 09-25 的 41% 略降，屬 FIFO 汰換中的正常波動，非新惡化 |

## 二、異常與處理

本次**無新增未修異常**，也**無今日新的真實誤推事件**（因今日尚無新 run）。延續狀態如下：

### 問題 27（延續，優先度最高，未修）：政府採購網地區篩選被 `agency` 覆蓋，跨縣市案件仍會被推播
- 現象：`passes_filters()` 用 `text = title + agency` 判斷地區，只要 PCC 資料列的機關欄位含「台北/新北」字樣，即使標題本身已自報是外縣市案件（如「基隆市…」），仍會通過地區篩選。已知造成 09-24、09-25 兩次真實曝光（同一筆案件因問題 29 的去重 bug 又重複推播一次，本次監測未發現第三次新曝光）。
- 定位：`scraper.py:823-841` `passes_filters()`
- 建議修法（延續 09-25 報告的 diff，未變更）：
  ```diff
   def passes_filters(item: dict) -> bool:
       title  = item.get("title", "")
       agency = item.get("agency", "")
       text   = title + agency

       regions = GLOBAL_FILTER.get("regions", [])
  +    OTHER_CITIES = [
  +        "基隆市", "桃園市", "新竹市", "新竹縣", "苗栗縣",
  +        "台中市", "臺中市", "彰化縣", "南投縣", "雲林縣",
  +        "嘉義市", "嘉義縣", "台南市", "臺南市", "高雄市",
  +        "屏東縣", "宜蘭縣", "花蓮縣", "台東縣", "臺東縣",
  +        "澎湖縣", "金門縣", "連江縣",
  +    ]
  +    if regions and any(k in title for k in OTHER_CITIES) and not any(k in title for k in regions):
  +        return False
       if regions and not any(k in text for k in regions):
           return False
  ```
- 是否已修：**未修**，待人工核准（見待決事項 1）。本次監測未動手修改程式碼。

### 問題 28（延續，未修）：Google Alerts 可能推播「投標單」表單標題
- `GLOBAL_FILTER["blacklist"]` 仍未含「投標單」。09-25 當日 8 筆推播中無此類實例，風險未消除但未新增曝光。
- 建議修法：`GLOBAL_FILTER["blacklist"]` 新增 `"投標單"`。
- 是否已修：**未修**，待人工核准（見待決事項 2）。

### 問題 3、4、25、26、22、29（延續，狀態不變，不重複貼程式碼）
- 問題 3：`FNP_PAGES` 仍含「地上權設定」「停車場」兩非租賃分類頁，`skip_global_filter=True` 使黑名單失效，未修。
- 問題 4：ANXING ERP header 未 `.strip()`，未修（今日環境未設 `ANXING_URL`/`ANXING_KEY`，分支未觸發）。
- 問題 25：國防部政治作戰局 `state.json` 仍保留混入的社宅導覽頁記錄，未修。
- 問題 26：土地銀行出租不動產近 7 天 notify 仍為 0，狀態未變。
- 問題 22、29：已修復（commit `ba9057f`，2026-09-25 02:24:33 UTC），修復內容為 `_stable_url()` 去重與 `r.encoding` 固定 `utf-8`。**尚未有真實 run 驗證**——09-25 01:30 的 run 發生在修復前，今日 09-26 01:30 的 run 若已觸發，將是第一次能驗證此修復是否生效的機會，但本次監測時間點早於該 run，無法確認。**下次監測需優先確認**：政府採購網 `new` 數是否不再連續出現與前一天完全相同的標題清單。

## 三、優化建議

### 建議 1（延續，方向 A，依據問題 27）：修正 `passes_filters()` 地區判定
- 依據：問題 27 已造成 2 次真實曝光（09-24 首推、09-25 因去重 bug 重複），且根因程式碼完全未變，風險持續存在。
- 做法：見問題 27 diff。
- 預期效果：堵住「機關雙北、資產跨縣市」這類全國性來源（政府採購網、Google Alerts）特有的誤判模式。

### 建議 2（延續，方向 A，依據問題 28）：blacklist 新增「投標單」
- 依據：09-24 曾出現表單標題誤推，根因未修，風險仍在，且修法僅 1 行、風險極低。

### 建議 3（新增，方向 B，流程面）：QA routine 排程時間應晚於 `daily.yml`
- 依據：近 14 天 `daily.yml` 觸發時間穩定在 `01:30 UTC` 整（14/14 一致），但本 QA routine 已連續第 2 天（09-25、09-26）在 `01:0x～01:1x UTC` 執行，早於觸發時間，導致當天檢查只能看到前一天的資料，「今日已執行」一項每天都會誤判。
- 做法：建議將 routine 排程時間調整至 **02:00 UTC** 之後（daily.yml 通常在 1 分鐘內完成，01:31 UTC 即可取得結果，02:00 留有充分緩衝）。
- 預期效果：往後每天監測都能看到當天真實執行結果，不需靠隔天報告回頭補判。

方向 C（新增來源）：問題 27、28 等既有問題尚未核准修復，暫緩評估新來源，維持 09-25 報告的判斷。

## 四、待決事項

1.【延續，優先度最高，已造成 2 次真實資料曝光】問題 27（政府採購網地區篩選被 agency 覆蓋）：是否核准 diff（建議 1）？
2.【延續】問題 28（Google Alerts「投標單」風險未消除）：是否核准 blacklist 新增（建議 2）？
3.【延續，累計第 7 次以上重現】問題 4（ANXING ERP header 未 `.strip()`）：修法僅 2 行，是否核准？
4.【延續】問題 3（`FNP_PAGES` 含「地上權設定」「停車場」+ `skip_global_filter=True`）：是否核准移除兩分類頁？
5.【延續】問題 25（政治作戰局混入社宅導覽頁）：方案 A（補黑名單）或方案 B（移除該分頁）？
6.【延續】問題 26（土地銀行疑似抓錯區塊）：是否核准先移除來源（建議 4，09-25 報告編號）？
7.【延續，第 2 次連續重現】QA routine 排程時間早於 `daily.yml` 觸發時間：是否核准調整排程至 02:00 UTC 之後（本報告建議 3）？
8.【延續】問題 22、29 修復（commit `ba9057f`）尚未經真實 run 驗證：下次監測需確認政府採購網不再重複推播同一筆案件。此項為追蹤事項，非需人工決策。
9.【延續，其餘低優先度項目：問題 1、5、6、7、9、11、13、14、15、17、23、24】維持累積待辦，未變動，詳見歷史報告（09-23、09-24、09-25）。
