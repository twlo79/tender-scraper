# 標案爬蟲監測報告 2026-08-07

## 一、狀況總覽

整體狀態：❌ 有異常（Anthropic API 組織停用已連續 **11 天**，3 個核心來源持續斷線；本次新增 2 項「本週已實際發生」的使用者可見錯誤：國營台鐵再次外洩非雙北案件推播、國家住宅及都市更新中心推播 url 指向列表頁）
最近執行：2026-08-07 01:30（推播 2 筆 / 新增 4 筆 / 抓取 62 筆）

| 檢查項目 | 結果 | 說明 |
|---|---|---|
| 1. 今日已執行 | ✅ | `sent_log.json` 最新 key 為 `2026-08-07 01:30`，即今天 |
| 2. 最近一次 GitHub Actions 結論 | ✅ | run `31138222229`，2026-08-07T01:30:13Z，`workflow_dispatch` 觸發，conclusion=`success`；近 6 次執行（08-02～08-07）全部 `success` |
| 3. line_pushed 與 total_notify 一致性 | ✅ | 今日 `total_notify=2`、`line_pushed=true`，一致；job log 確認 `LINE broadcast 成功（3 則）` |
| 4. 是否所有來源 fetched=0 | ✅ | 否，本次共抓到 62 筆 |
| 5. 個別來源連續 3 天以上 fetched=0 | ❌ | 教育部學產基金、台北市都發局、國防部政治作戰局 連續 **11 天**（07-28～08-07）`fetched=0`（詳見問題 1，今日 job log 逐字確認錯誤訊息不變） |
| 6. 來源抓取量暴跌/暴增 | ✅ | 國營台鐵已於 08-07 恢復抓到 1 筆（前 2 天 08-05、08-06 為 0，已解除觀察）；其餘來源穩定（財政局 13、瑠公 15、郵局 10、URC 11、國產署 3、政府採購網 2、土地銀行 2）；Google Alerts 本週 8/1/5/1/12/1/2 筆屬 RSS 自然波動 |
| 7. 應擋卻漏網的案件 | ❌ | **今日新發現**：國營台鐵推播「新竹縣新豐鄉新興路202號房地（新豐舊站）」，非雙北案件，實際推播給使用者（詳見問題 2，這是已知漏洞第 **2 次**造成真實推播） |
| 8. 標題為分類名稱／導覽文而非案名 | ⚠️ | 國有財產署延續合成批次標題（如「【地上權設定】北區分署 115年第4批...公告:115年07月06日 開標:115年08月24日」，非單一案名）；另新查得 URC 快取中有標題「會員中心」的非案名髒資料（尚未實際推播，詳見問題 5） |
| 9. url 指向列表頁而非個別公告頁 | ❌ | **今日新發現**：08-07 推播的「臺北市信義區三興段二小段507地號等17筆土地租賃案」（URC）url 為 `https://www.hurc.org.tw/hurc/procurement`（列表頁），非個別公告頁，已實際推播給使用者（詳見問題 3） |
| 10. date 欄位為空導致日期窗被跳過放行 | ⚠️ | `state.json` 全站 741 筆快取中 454 筆（61%）`date` 為空（延續，架構性問題）；但本週實際推播的 4 筆項目 `date` 皆非空，本週無立即風險 |

---

## 二、異常與處理

### 問題 1：Anthropic API 組織已停用，3 個來源連續 11 天斷線（延續自 07-28，優先度最高）

- **現象**：教育部學產基金、台北市都發局、國防部政治作戰局 連續 **11 天**（07-28～08-07）`fetched=0`。今日 GitHub Actions job log（run `31138222229`）逐字確認錯誤訊息完全未變：
  ```
  01:30:43 WARNING   [政府採購網] Claude API 錯誤：invalid_request_error — This organization has been disabled.
  01:30:46 WARNING   [教育部學產基金] Claude API 錯誤：invalid_request_error — This organization has been disabled.
  01:30:49 WARNING   [台北市都發局] Claude API 錯誤：invalid_request_error — This organization has been disabled.
  01:30:51 WARNING   [國防部政治作戰局] Claude API 錯誤：invalid_request_error — This organization has been disabled.
  ```
  （政府採購網原生 PCC 解析不依賴 Claude，今日仍正常抓到 2 筆，僅此來源已自行恢復，與前次報告一致。）
- **定位**：`scraper.py:675-698` `parse_with_claude_fallback()`。`git log -- scraper.py` 最後改動早於 07-26，排除程式碼變更導致，根源為 API 金鑰／帳務問題。
- **建議修法（非程式碼問題，需人工處理）**：
  1. 登入 [Anthropic Console](https://console.anthropic.com) 確認組織停用原因並恢復，或更換新的可用 API Key，更新 GitHub repo secret `ANTHROPIC_API_KEY`。
  2. 金鑰恢復後執行 `dry_run_all_regions.py --debug --source 教育部學產基金`（依序 3 個來源）驗證原生 selector 是否仍有效。
  3. 中期治本：強化這 3 個來源的原生 selector 容錯，降低對 Claude fallback 的依賴。
- **是否已修**：未修，已連續 11 天。

### 問題 2：國營台鐵地區篩選漏洞——今日再次造成實際外洩推播（延續，第 2 次實錘）

- **現象**：`parse_tra()` 只依「臺北營業分處」字樣篩選，未排除該分處管轄但實際在外縣市的地點。今日（08-07）推播：
  ```
  【臺北營業分處】【房地標租】【新竹縣新豐鄉新興路202號房地（新豐舊站）】
  date: 2026/08/07  url: https://www.railway.gov.tw/tra-tip-web/adr/rent-tender-1-detail?pk=6490&activePage=1
  ```
  這與 07-28 外洩的「新竹縣新豐鄉」案為同一漏洞、同一地點再次觸發，確認漏洞持續存在且**已造成 2 次真實推播**（非僅理論風險）。
- **定位**：`scraper.py:194-196`（`parse_tra()`）、`passes_filters()` 第 796-814 行（`text = title + agency`，`agency` 恆含「臺北」二字使 region 篩選失效）。
- **建議修法**：
  ```python
  # scraper.py，parse_tra() 第 194-196 行，「只保留臺北營業分處的標案」之後加一段
  NON_TAIPEI_HINTS = ["新竹市", "新竹縣", "桃園", "苗栗", "台中", "臺中", "彰化",
                       "南投", "雲林", "嘉義", "台南", "臺南", "高雄", "屏東",
                       "宜蘭", "花蓮", "台東", "臺東", "基隆"]
  items = [i for i in items if "臺北營業分處" in i.get("agency", "") + i.get("title", "")]
  items = [i for i in items if not any(h in i["title"] for h in NON_TAIPEI_HINTS)]  # 新增
  log.info(f"  [國營台鐵] {len(items)} 筆（臺北營業分處）")
  ```
- **是否已修**：未修（diff 已連續多日提出，尚待人工核准套用；鑑於今日已第 2 次實際外洩，建議優先度提升為與問題 1 並列最高）。

### 問題 3：國家住宅及都市更新中心（URC）url 幾乎全數指向列表頁，今日已實際推播錯誤 url（新發現，具體案例）

- **現象**：`parse_hurc()` 從「案名」儲存格找 `<a href>`，找不到（或為 `javascript:`）時 fallback 回列表頁 URL。核對 `state.json` 全部 45 筆快取：僅 **1 筆（2.2%）** url 為真正的個別公告頁（連到政府電子採購網），其餘 44 筆（97.8%）url 為空（21 筆）或直接等於列表頁 `https://www.hurc.org.tw/hurc/procurement`（23 筆）。今日（08-07）推播的「臺北市信義區三興段二小段507地號等17筆土地租賃案」url 即為列表頁，使用者點擊後看到的是全部標案清單，而非該筆公告內容——**本週已實際發生，非僅快取殘留**。
- **定位**：`scraper.py:284-323`（`parse_hurc()`），第 312-319 行：
  ```python
  a = tds[2].find("a", href=True)
  if a:
      href = a["href"]
      href = href if href.startswith("http") else urljoin(BASE, href)
      if href.startswith("javascript"):
          href = URL
  else:
      href = URL
  ```
- **建議修法**：實測顯示「案名」儲存格本身多數沒有可用連結，需改抓同一列（`tr`）中其他欄位（常見為「領標資料」欄，程式註解列出的欄位順序中排最後）是否含實際下載/詳情連結；或改抓案號建構查詢 URL。確切結構需連上 `https://www.hurc.org.tw/hurc/procurement` 實際頁面核對（本沙盒環境對外連線持續被 proxy 回傳 403 `Tunnel connection failed`，需在有網路權限環境執行，見待決事項）。暫定方向：
  ```python
  # scraper.py:312 附近，改為掃描整列而非只掃 tds[2]
  a = row.find("a", href=True)  # 掃整列，而非只掃「案名」欄
  ```
  此為暫定改法，套用前仍需連上實際頁面確認連結真正落在哪個欄位。
- **是否已修**：未修。

### 問題 4：Google Alerts 混入房產新聞報導，非官方公告（延續，本週再現 1 筆）

- **現象**：本週（08-01～08-07）推播中，08-03「北市議會舊址無償提供使用2323坪促參招商中議員：勿淪為商場｜大台北 - 聯合新聞網」為新聞媒體報導而非官方標案公告。
- **定位**：`parse_google_alerts()`（`scraper.py:627-663`）未對來源網域做任何過濾。
- **建議修法**：完整 diff 見 `qa_reports/2026-08-04.md`（`NEWS_DOMAIN_BLACKLIST` 過濾常見新聞媒體網域，如 udn.com、聯合新聞網等）。
- **是否已修**：未修。

### 問題 5：URC 快取中出現非案名髒資料、國有財產署標題為批次合成字串（延續＋新例證）

- **現象**：
  - URC `state.json` 快取中發現標題僅「會員中心」（date/url 皆為空）的紀錄，代表 `parse_hurc()` 選中的 table 有時會誤抓到非資料列（如站台選單/會員中心相關區塊）。目前尚未實際推播（已被既有 dedup 擋下），但代表 table 選擇邏輯（「找 td 數 ≥6 的第 2 張 table」）不夠穩定。
  - 國有財產署延續合成標題問題，例：「【地上權設定】北區分署 115年第4批 國有非公用不動產 公告:115年07月06日 開標:115年08月24日」——標題是批次描述而非單一案件名稱（來源結構限制，非程式錯誤，延續前次報告）。
- **建議修法**：`parse_hurc()` 的 `data_table` 選取邏輯建議加上「標題須含日期或案號格式」的二次驗證，排除「會員中心」等明顯非公告文字（可比照 `_is_junk()` 的正則，但先決條件是要先解決問題 6 讓 `_is_junk()` 真的被呼叫）。
- **是否已修**：未修。

### 問題 6：`_is_junk()` 防呆函式定義後從未被呼叫（死代碼，延續）

- **現象**：`scraper.py:113-114` 定義 `_is_junk(item)`，但 `main()`（第 1142-1157 行）從未呼叫。今日 `grep -n "_is_junk("` 全檔掃描確認僅 1 處（定義本身）。
- **建議修法**：
  ```python
  # scraper.py:1149-1152，notify_items 篩選條件加入 not _is_junk(i)
  notify_items = [
      i for i in new_items
      if (skip_filters or passes_filters(i)) and is_within_date_window(i) and not _is_junk(i)
  ]
  ```
- **是否已修**：未修。

### 問題 7：`SOURCES` 內各來源自訂 `whitelist`/`blacklist`/`regions` 為死代碼（延續）

- **現象**：`SOURCES`（`scraper.py:703-793`）多個來源定義了自訂 `whitelist`/`blacklist`/`regions`，但 `passes_filters(item)` 簽名只吃 `item`，未讀取這些欄位；`main()` 呼叫處（`scraper.py:1151`）`passes_filters(i)` 未傳入 `src`，今日再次核對現況未變。
- **建議修法**：讓 `passes_filters` 接受來源層級覆寫（完整 diff 見 `qa_reports/2026-08-04.md`）。需先決定「來源 `regions`/`whitelist` 為空列表」的語意（見待決事項）。
- **是否已修**：未修。

### 問題 8：國有財產署「停車場」類別繞過黑名單（`skip_global_filter=True`，延續，風險未實現）

- **現象**：`scraper.py:744-745` 該來源整體設為 `skip_global_filter=True`，連帶讓黑名單失效；另注意 `GLOBAL_FILTER["blacklist"]` 本身其實**未含「停車」關鍵字**，即使關掉 `skip_global_filter` 也擋不住停車場類。今日該類別仍 `fetched=0`（[國有財產署/停車場] 0 筆），風險為理論性。
- **建議修法**：暫不變更（風險未實現）；若日後該類別開始有資料，應同時（a）改為僅 region 判斷、保留 blacklist，且（b）在 `GLOBAL_FILTER["blacklist"]` 補上「停車場」「停車位」等詞，兩者缺一則仍會漏放。
- **是否已修**：不適用（觀察中）。

### 問題 9：土地銀行出租不動產抓到站內公用公告，非租賃清單（延續）

- **現象**：`parse_landbank()`（`scraper.py:592-618`）selector 範圍過寬。今日核對近 7 天穩定 `fetched=2`、`new=0`（本週無新增，前次報告 08-06 新增的 2 筆站內公告已被 dedup），無新增污染。
- **建議修法**：詳見 `qa_reports/2026-08-06.md` 問題 7，需連上實際頁面確認容器 class/id（本沙盒無網路權限）。
- **是否已修**：未修。

### 問題 10：台北自來水處抓到一般採購公告頁，非「出租/招租」專屬頁面（延續）

- **現象**：本次核對近 7 天 `fetched` 穩定 2~3 筆、`notify=0`，內容型態與前次診斷一致。
- **建議修法**：詳見 `qa_reports/2026-08-04.md`。
- **是否已修**：未修。

---

## 三、優化建議

### 建議 1（優先度提升）：問題 2（國營台鐵地區篩選）今日已第 2 次造成實際錯誤推播，建議立即套用既有 diff

- 依據：07-28 與 08-07 兩次都推播「新竹縣新豐鄉新興路202號房地」給雙北標的訂閱者，同一漏洞、同一物件，證明不是偶發而是每次該物件出現在清單中就會外洩；diff 已連續 4 份報告（08-01～08-07）提出但未套用。
- 做法：套用問題 2 的 `NON_TAIPEI_HINTS` diff，並用 `dry_run_all_regions.py --debug --source 國營台鐵` 驗證過濾後仍保留台北/新北案件、剔除新竹案件。
- 預期效果：消除單一已知漏洞造成的重複誤推，且驗證方式明確、風險低（僅新增排除清單，不影響既有邏輯）。

### 建議 2：修正 URC 個別公告 url 抓取邏輯，避免使用者點擊推播後看到的是列表頁而非公告內容

- 依據：`state.json` 中 URC 45 筆快取，僅 1 筆（2.2%）有效個別 url，其餘 44 筆（97.8%）為空或等於列表頁；本週（08-07）已有 1 筆這樣的紀錄被實際推播給使用者。
- 做法：改抓整列 `<tr>` 而非僅「案名」儲存格找連結（暫定 diff 見問題 3），需在有網路權限環境核對實際 HTML 後套用。
- 預期效果：使用者點擊推播訊息後能直接看到對應公告內容，而非需要自行從列表頁搜尋，提升可用性與信任度。

### 建議 3：本週低推播來源分析——12/13 個來源 notify=0，非篩選過嚴所致

- 依據：本週僅國營台鐵（1 筆，且為誤推）、URC（1 筆）有 `notify>0`；其餘 11 個來源合計抓取約 49 筆（教育部/都發局/國防部因 API 斷線本週合計為 0，不計入）、全部 `notify=0`。逐筆核對本週 4 筆 `new` 項目（TRA 誤推 1 筆、URC 正常案件 1 筆、Google Alerts 新聞 1 筆、另 1 筆 Google Alerts），未發現因篩選過嚴造成的漏網案件——判斷方向與 08-06 報告一致（低推播量的根本原因是「近期真的沒有新公告 + 問題 1 斷線」，非篩選過嚴），暫不建議放寬 whitelist/regions。

### 方向 C：新增來源

本次持續略過。問題 1（Anthropic API，已達 11 天未解）與問題 2、3（本週已造成 2 類真實使用者可見錯誤）優先度更高，在既有問題解決並觀察至少一週成效之前，新增未驗證來源的邊際效益低於修復既有問題。

---

## 四、待決事項

1. **【最優先，已連續 11 天】Anthropic API 組織已停用**（問題 1）：需人工登入 Anthropic Console 確認並恢復，或更換新的可用 API Key，並更新 GitHub repo secret `ANTHROPIC_API_KEY`。
2. **【今日已第 2 次實際外洩】問題 2（國營台鐵地區篩選）diff 是否核准套用**：已連續 4 份報告提出，本次確認漏洞已造成 2 次真實錯誤推播，建議儘速拍板。
3. 問題 3（URC url 指向列表頁）的具體修法需先在有網路權限環境確認 HTML 實際結構後才能定案，本沙盒對外連線持續被 proxy 回傳 403（`Tunnel connection failed`），無法即時比對。
4. 問題 2、4～10 的修法 diff（詳見 `qa_reports/2026-08-04.md`、`2026-08-05.md`、`2026-08-06.md` 與本報告）是否核准套用——已連續多日提出但尚未有人拍板。
5. 問題 7（`SOURCES` 死代碼欄位）處理方向：(a) 直接移除從未生效的欄位，或 (b) 讓 `passes_filters` 真正套用來源層級覆寫（需先決定空列表的語意）。
6. 國有財產署「地上權設定」類別是否納入業務範圍，需人工判斷。
